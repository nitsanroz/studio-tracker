@AGENTS.md

# Studio&more Tracker

Custom task + time tracker for the **Studio&more** design studio (~8 active designers, Tel Aviv, Sun–Thu work week). Replaces Everhour + Asana + a Google Sheets weekly plan. This app is now the **single source of truth**, full stop — **every live sync was removed on 2026-07-29 (v0.99.35): no Everhour sync, no Google-Sheet plan sync, no crons at all.** The one standing exception to "don't re-pull": **Asana is being re-pulled once, deliberately**, for the pre-Everhour comment history that was never imported (2026-07-28 entry). That is not a reason to re-pull anything else — the deleted executive-team boards in particular stay deleted.

**Owner:** Nitsan (nitsan@studionmore.com).
**Keep this file current:** when you make a meaningful change (deploy, migration, new convention, resolved gotcha), update the relevant section below and the "Working log" at the bottom, so the next chat picks up where you left off.

## Stack & running
- **Next.js 16** (App Router) + **Tailwind v4** + **Supabase** (Postgres + Auth + Storage) + **Vercel**.
- `AGENTS.md` warns this Next.js differs from training data — read `node_modules/next/dist/docs/` before writing Next-specific code.
- **Node:** system Node is 18, but the project needs **Node 24** at `~/.local/node/node-v24.18.0-darwin-arm64/bin`. The `dev` script prepends this to PATH; the `preview` launch.json (`.claude/launch.json`, config name **`studio-tracker`** — don't rename) runs `npm run dev`. For build/deploy/scripts, prepend that PATH manually.
- Dev: `npm run dev` (or `preview_start` with name `studio-tracker`). Recover from stale CSS: kill port 3000 + delete `.next`.
- Known npm optional-deps bug: needed a clean reinstall for `@tailwindcss/oxide-darwin-arm64`.

## Domain model
- Hierarchy: **Clients → Sections → Tasks** (the Projects layer was removed 2026-07-11 by migration 0007; `projects` table + `project_id` columns remain in DB for legacy import mapping only — the app never reads them).
- Every **time entry requires a description** (mandatory).
- Per-client non-billable **"«Client» keys"** task: admin moves hours into it before Sunday client reports.
- **Billable** is driven by `clients.billable` (migration 0009). Marking a client "Internal" (Settings→Clients) flips its existing tasks non-billable; new tasks inherit the client flag. Internal clients: **Studio, OFFF tlv, Imported / Unsorted**.
- **⚠️ Client Reports is BETA — Nitsan has not reviewed or used it yet (as of 2026-07-30).** Don't treat its current behaviour as intended design, and don't "fix" it against assumptions: the summary-tile mismatch logged under v1.0.2 is knowingly left alone for that reason. Ask before changing anything here.
- **Client reports are published snapshots**: `report_links.snapshot` (jsonb) is frozen on Publish — permanent URL per client, clients never see live hours. Payment periods are fully custom per client (`client_billing_periods`: label/from/to/hour_cap/advance_hours/paid).
- Past employees (Liza, Sofia) stay in data as **deactivated**.

## App structure
- Routes under `src/app/(app)/`: `my-tasks`, `feed` (timesheet is default view), `clients` (admin-only), `plan` (weekly grid), `reports` (analysis, admin-only), `client-reports` (published reports, admin-only), `team`, `settings`, `intake-queue`.
- Public: `src/app/report/…` (client report), `src/app/intake/[token]/…` (client intake form), `login`, `auth`, `reset`.
- API: `src/app/api/` — `intake/[token]`, `admin/users`, `avatar`, `member-image`, `me/hr`, `task-attachments`.
- Key lib: `src/lib/version.ts` (**bump `APP_VERSION` on every deploy** — currently the team is pre-v1.00; shown on login + sidebar), `store.tsx` (client state incl. undo/redo cmd/ctrl+Z, 10-action history), `db.ts`, `aggregate.ts`, `report-snapshot.ts`, `report-links.ts`, `date-ranges.ts`, `format.ts`, `types.ts`, `period-math.ts` (home period/bucket arithmetic, extracted from `dashboard.tsx` so it can be tested — every function takes `now` as its last arg), `report-sanitize.ts` (`sanitizeSnapshot`, moved out of the report page for the same reason), `uploads.ts`, `members.ts` (`loggableMembers` — who a time entry may be attributed to, for the admin log-for-someone-else pickers).
- **Tests:** `npm test` (Vitest, `src/**/*.test.ts`, node environment, `@/` aliased). Covers `period-math.ts` and `report-sanitize.ts` — the date boundaries the KPI deltas depend on, and the assertion that a hidden report column can't be recovered by arithmetic. The recovery-script parser has its own suite: `node --test scripts/lib/legacy-hours.test.mjs`. Nothing else is covered; `store.tsx` and the components are still verified in the browser.
- Key components: `task-list-row.tsx` (shared TaskTable), `resizable.tsx` (drag-resize columns, localStorage `colw.*`), `report-table.tsx`, `weekly-plan.tsx`, `dashboard.tsx`, `client-view.tsx`, `app-shell.tsx`, `notifications-bell.tsx` (admin header queue: intake + sync issues), `account-inactive.tsx` (shown instead of the app to an archived member — see Offboarding), `week-timesheet.tsx` (admin home: who logged hours this week), `user-day-details.tsx` (the day popup, shared by Time Feed and the week timesheet).
- **UI conventions:** pages cap at `max-w-[1500px]`; admin nav items render last under a divider; English UI with Hebrew/RTL content via `.bidi-auto` (`unicode-bidi: plaintext`); icons = lucide-react 20px strokeWidth 1.75; weekly-plan shades Fri+Sat as weekend.

## Migrations (`supabase/migrations/`)
`0001_init`, `0002_plan_columns_hidden`, `0003_intake`, `0004_batch`, `0005_finance` + `0006_finance_seed` (finance tables — see Finance note), `0007_flatten_projects`, `0008_member_hr` (admin-only HR table: ID/address, RLS-protected; the salary column was later dropped by 0013), `0009_client_billable`, `0010_periods_paid` (`client_billing_periods.paid`, `clients.invoice_note`, `report_links.custom_weeks`), `0011_harden_task_writes` (BEFORE UPDATE trigger `enforce_task_member_columns` — non-admins may only change `brief/figma_url/tag_id/position`; all other task columns are admin-only, enforced in the DB not just the UI. **Must be applied.**), `0012_member_photo_and_onboarding`, `0013_drop_member_salary` (drops `member_hr.salary` + its data — the studio no longer wants salary in this system. **Must be applied.** Does NOT touch `finance_salaries`, the separate finance-product expense table), `0014_retire_timers_and_sync_issues` (deletes never-stopped timer rows `minutes is null`, drops the `timer_or_minutes` constraint, makes `time_entries.minutes` NOT NULL; adds the admin-only `sync_issues` queue. **Must be applied.**), `0016_legacy_history` (recovered pre-Everhour history: `time_entries.legacy` + `asana_story_gid`, `task_comments.asana_story_gid`/`author_name` and `user_id` made nullable, `tasks.legacy_hours`/`legacy_title`/`activity_from`/`activity_to`, `sections.legacy_hours`/`estimate_hours`/`legacy_name`/`closed_on`. **Should be applied** — the app falls back gracefully without it, but the hour recovery needs it), `0017_legacy_entry_authors` (lets a recovered pre-Everhour entry name an author who has no account: `time_entries.user_id` becomes nullable FOR LEGACY ROWS ONLY, guarded by a CHECK, plus `legacy_author_name`. Needed because `profiles.id` is a FK to `auth.users`, so a profile requires a login — and most pre-Everhour comment authors left the studio years ago. **Must be applied** with 0016 before the re-home SQL), `0015_occasions` (`occasions` table for CUSTOM home-page occasions only — birthdays/anniversaries/Jewish holidays are all derived and need no rows. Read by any signed-in member, admin-only writes. **Optional**: without it everything except custom entries works, and both APIs degrade gracefully).
- `0021_harden_profile_writes` (**MUST BE APPLIED — privilege escalation.** `own profile update` had no `with check` and no column scoping, so any member could `update profiles set role='admin'` on their own row and inherit every admin-only table. Adds a BEFORE UPDATE trigger in the shape of 0011: members may change only `name`/`avatar_url`/`photo_url`. **Unlike 0011 it exempts the service role** (`auth.uid() is null`), because /api/avatar, /api/member-image and /api/admin/users write profiles with the service key after checking the caller's session.)
- `0022_links_notes_and_start_date` (**MUST BE APPLIED — applied by Nitsan 2026-08-06.** Adds the `links` table (titled reference links on a task OR a client, one owner enforced by a CHECK), `clients.notes`, and `tasks.start_date` (the left edge of a Timeline bar). ⚠️ **It also re-declares `enforce_task_member_columns` to add `start_date`** — 0011's trigger enumerates protected columns BY NAME, so any new task column is member-writable until it's named there, and without this a designer could re-schedule any task through the API. The app degrades gracefully without 0022 (the task select steps down a rung, `links` falls back to `[]`), but start dates and links can't be saved.)
- `0023_timeline_order_and_client_icon` (**MUST BE APPLIED.** `tasks.timeline_position` (a Timeline row order kept SEPARATE from `tasks.position`, which is per-section and drives the Tasks tab — sharing it would mean dragging a Timeline row could silently move the task into another section), plus `clients.icon` (a preset glyph name from CLIENT_ICONS) and `clients.icon_url` (an uploaded mark; wins over `icon`). ⚠️ **Re-declares `enforce_task_member_columns` again to add `timeline_position`** — see 0022 for why every new task column needs that. Degrades gracefully: the task select steps down a rung and clients render their initial.)
- `0024_task_types` (**MUST BE APPLIED.** `task_types` (name + colour + position, read-all/admin-write) seeded with the seven Nitsan named — Design, Wireframe, Content, Image making, Development, QA, Client side — plus `tasks.type_id` (FK, ON DELETE SET NULL). A task's KIND OF WORK, deliberately NOT the existing `tags`, which are workflow STATES ("in design", "Client approval", "Approved"); folding them together would force a task to say it's a QA job OR that it's awaiting approval, never both. ⚠️ `type_id` is deliberately ABSENT from the 0011 trigger's protected list — describing the work is collaborative like `tag_id`, unlike scheduling it. Degrades gracefully: task select steps down a rung, `task_types` falls back to `[]`, bars stay brand blue.)
- **`data/*.sql` are NOT migrations and must never be numbered like them.** They are one-off data scripts the user runs in the SQL editor. Three of them used to be `0019_/0020_/0021_`, which collided head-on with `supabase/migrations/0019_estimated_entry_dates.sql` — Nitsan reasonably reported "I ran 0019" meaning the migration while the data file was still pending. They are now named for what they do: `data/task-titles-cleanup.sql`, `data/merge-clients-inreach-quadream.sql`, `data/finance-2016-import.sql`. Keep it that way.
- `0028_intake_seen` (**APPLIED by Nitsan 2026-08-11.** `task_requests.seen_at` / `seen_by` / `client_notified_at`, for the "we've seen your brief" receipt. Three plain columns and nothing else: `task_requests` is admin-only for ALL operations under 0001's `admin all` policy, so there is no policy or trigger work here — and no 0011 trigger amendment, because that trigger guards `tasks`, not `task_requests`. Two stamps rather than one on purpose: a failed send must still leave the request marked as read. Degrades gracefully — before it is applied `mapTaskRequest` reads the missing keys as null, which renders as "nothing acknowledged yet", though `/api/intake/seen` itself 500s because the columns are in its select list.)
- **Migrations are applied by the user in the Supabase SQL editor** — not by Claude. When you add one, tell the user to run it (and note whether it must run *before* deploying).

## Deploy
- Vercel project linked (`.vercel/`, team `studiomore`). Deploy: `npx vercel --prod --yes` with the Node 24 PATH.
- Live at **tracker.studionmore.com** (+ studio-tracker-nu.vercel.app).
- Prod env vars set: Supabase URL/anon/service-role, RESEND_API_KEY, INTAKE_FROM_EMAIL. **There is no `vercel.json` and no cron any more** (v0.99.35, verified by `vercel crons ls` → none); `EVERHOUR_API_KEY`, `WEEKLY_PLAN_CSV_URL` and `CRON_SECRET` were **deleted from the Vercel project by Nitsan 2026-07-29**, and the weekly-plan sheet's published CSV link was unpublished. They remain in local `.env.local` only for the hand-run import scripts.
- Git: private remote `github.com/nitsanroz/studio-tracker`, branch `main`. `data/` (real client data) is committed → **keep repo private**.
- **Pushing uses an SSH DEPLOY KEY, not HTTPS** (set up 2026-08-09, after the keychain's HTTPS credential expired and `gh` isn't installed). `origin` is `git@github.com:…` and the repo sets `core.sshCommand` to `ssh -i ~/.ssh/id_ed25519_studio_tracker -o IdentitiesOnly=yes`. A deploy key was chosen over a token deliberately: it reaches **only this repo**, not Nitsan's account or his other repos, and is revoked with one click at the repo's Settings → Deploy keys. ⚠️ **The auto-mode classifier BLOCKS writing `~/.ssh/config` and running `git remote set-url` / `git config core.sshCommand`** — Nitsan ran those two commands himself. Don't burn attempts on them; a one-off push can always go through `GIT_SSH_COMMAND="ssh -i ~/.ssh/id_ed25519_studio_tracker …" git push git@github.com:… main`, which is NOT blocked.
- **`.vercelignore` gotcha:** must NOT contain a bare `supabase` / `/supabase` line — it also drops `src/lib/supabase/`, breaking the prod build while local build passes.

## Email (Resend)
- `RESEND_API_KEY` + `INTAKE_FROM_EMAIL="Studio&more Tracker <notifications@studionmore.com>"` in `.env.local`. Domain `studionmore.com` verified in Resend (DNS at GoDaddy).
- Intake form emails admins on submission; recipients come from Supabase `app_settings` row `key=intake_notify_emails` (not env).
- **One mail leaves the building: the intake read-receipt** (`/api/intake/seen`, sent only when an admin presses "Tell client we've seen it"). Its subject and body are edited in **Settings → Intake form** and stored in `app_settings` row `key=intake_seen_email`; `SEEN_EMAIL_DEFAULT` in `lib/brief.ts` is the fallback. `reply_to` is the admin who pressed it, not the notifications mailbox — a client answering should reach the person handling their brief.

## Import / sync scripts (`scripts/`)
- `sync-everhour.mjs [from] [to]` — incremental Everhour sync (matches `everhour_id`; API key in `.env.local` still valid). Everhour times are in **seconds**; task notes live in `descriptionJson` (rich-text JSON), not `description`.
- `enrich-asana.mjs` — fills brief/figma_url/due_date/assignee from `data/asana/<gid>.json` (only fills empty fields, never overwrites; idempotent).
- `import-everhour.mjs`, `import-plan-sheet.mjs`, `import-weekly-plan.mjs`.
- **Pre-Everhour recovery set** (see the 2026-07-28 working-log entry): `lib/legacy-hours.mjs` + its `node --test` suite, `fetch-asana-stories.mjs` (needs `ASANA_ACCESS_TOKEN`), `reconcile-legacy-hours.mjs`, `build-rehome-sql.mjs` (reads the reconciler's `data/legacy-entries.json` verbatim — never re-derives entries), `audit-rehome.mjs`. Run order: 0016+0017 → fetch → reconcile → REVIEW THE CSV → build → run `data/0018_rehome_unsorted.sql` → audit.

## Brand / design system
- Fonts: **Saans** (Displaay; Regular=380, Medium=570) + serif accent **PP Fragment Glare Regular Italic**. Commercial — `@font-face` wired at `public/brand/saans/…`, falls back to Rubik until font files arrive. The old nmore.co fonts (Hurme Geometric Sans) are **outdated — don't use**.
- Text color "Studio Black" **#06112f**. Colors: `#0b43ed` primary, `#1d32c8` dark, `#6181e8` light blue, `#74f3ef`/`#00f2ff` aqua accent, `#001e6c` navy. Theme: "electric" (blue sidebar).
- Wordmark SVG at `public/brand/logo-1.svg` via CSS mask (`.brand-wordmark`).
- **Gotcha:** Turbopack/lightningcss silently drops rules using `mask:` shorthand — use longhands.

### Drawing product mockups (the "what's new" visuals, and anything like them)
Eleven corrections from Nitsan produced these. He is a designer; **any
approximation gets caught**, so treat a mockup as a claim about what the product
looks like rather than as decoration.
- ⚠️ **Draw at LIFE SIZE, then scale the whole thing down.** A phone is a real
  375×812 screen using the app's own values (`h-14` header, `p-4`, 13px labels,
  `rounded-2xl`); one transform shrinks it. **Proportions cannot be guessed one
  element at a time** — a hand-sized canvas got every value wrong in a different
  direction. The render height then IS the scale factor: check a real 13px label
  doesn't land at 7px. `scripts/build-whatsnew-phone.mjs` is the worked example.
- ⚠️ **Real assets.** Icons read out of `node_modules` (lucide) at the app's size
  and stroke — and **scaling an icon scales its stroke, so divide the weight back
  out**. Real photos, not silhouettes.
- ⚠️ **Real text, never grey placeholder bars.** A rule can't be read as "254.5h"
  or as a column renamed to STATUS, which is usually the point of the picture.
- ⚠️ **Every surface keeps its own background and chrome** — rows floating on
  colour lose the grid, the pinned column, the ruler. Draw the card, not the
  contents.
- ⚠️ **Show the gesture, not the outcome** (the marquee and its cursor, not
  arrows implying a result).
- ⚠️ **Crop a device; never crop a card.** A phone bleeding off the bottom reads
  as a product shot; a table sliced at its edge reads as a fault. Watch both
  causes: geometry outside the viewBox, and a clip path narrower than the art.
- ⚠️ **Picture the AUDIENCE's screen with a colleague's data** — Nitsan is an
  admin, the readers are designers, and role-gated chrome differs (their bottom
  bar has four slots, not five).
- ⚠️ **A CSS custom property in an SVG comment kills the whole file** — the two
  hyphens in `var(--brand)` are illegal in an XML comment. Symptom: HTTP 200,
  balanced tags, `naturalWidth 0`, alt text in place of the picture. Also: an SVG
  loaded via `<img>` **cannot fetch external files**; inline images as data URIs.
- He would rather have **real screenshots** generated automatically. Chrome 151
  headless is on the machine; the blocker is auth, and the clean route is a
  fixture page rendering the real components (not a minted session, which would
  bake live client names into an image the whole studio sees).

## Related / out of scope
- **Finance was split out (2026-07-13) into a separate product** at `~/Documents/Claude/finance-admin/` — page/nav/lib deleted here. The `finance_*` tables + migrations 0005/0006 remain in the shared DB, untouched.
- Executive-team Asana boards ("Clients", "Studio") were **deleted from the DB** — that data lives in Asana only; never re-import.

## Access control (intentional design)
- **Studio-wide READ visibility is intentional** (decided 2026-07-24): every signed-in member can read all tasks/time entries/clients/plan (RLS `read all`). Members seeing each other's hours is accepted for this 8-person studio — the `/reports` "member sees own hours" and admin-only nav are UX scoping, not security boundaries.
- **WRITES are enforced in the DB, not just the UI:** admins = full; members = own time entries (`own time *` policies) + only the collaborative task columns (migration 0011 trigger). Privileged task fields (billable/estimate/due/title/status/assignment) are admin-only server-side.
- **Genuinely sensitive tables stay admin-only** (no `read all`): `member_hr`, `member_notes`, `finance_*`, `client_billing_periods`.
- **Public routes** are token-gated + service-role only: `report/[token]` ships a snapshot that is **sanitized server-side** (hidden tasks/columns physically removed — never sent to the browser); `intake/[token]` uploads go through an extension allowlist (`src/lib/uploads.ts`, no HTML/SVG/JS) with a server-forced Content-Type + a DB-based rate limit.

## Offboarding
**Archiving a member is a UI gate, not a security boundary.** `active` (and the `end_date` that forces it, 0020) is checked in `src/app/(app)/layout.tsx`, which renders `AccountInactive` instead of the app. But every RLS policy keys off `auth.uid()` alone — `is_admin()` reads `role`, and the shared read policies only ask `auth.uid() is not null` — so a departed member holding a valid token can still reach the API directly.

**To actually remove access, ban or delete the `auth.users` row** (`auth.admin.updateUserById(id, {ban_duration})` or `auth.admin.deleteUser(id)`). Since 0018 dropped `profiles.id → auth.users`, the profile and all of its history survive that: hours, comments and attribution stay exactly where they are. Do this in addition to setting `end_date`.

Gating RLS on `active` too (`and exists (select 1 from profiles where id = auth.uid() and active)` on the shared policies) is possible and was deliberately *not* done — it would lock out anyone whose `active` flag is wrong, including an admin, and the dates are being filled in by hand.

## Session gotcha (PII)
Once sensitive PII from the HR sheet (national IDs, salaries, addresses) enters a session, the auto-mode safety classifier blocks all of Claude's Bash/preview tool calls for the rest of that session — not fixable via permissions. Fix: start a fresh session. In such a session the user must run build/deploy/git themselves.

## Working log
Append notable changes here (newest first): `YYYY-MM-DD — vX.YZ — what changed`.
- 2026-08-12 — **v1.15.1 — the intake form was silently eating client attachments.** No migration. Reported as "it says 5 files, only 1 attaches" — and the plumbing was innocent: the input had `multiple` and the route already looped `form.getAll("files").slice(0, 5)`. ⚠️ **A bare `<input type="file" multiple>` REPLACES its selection on every pick**, so a client choosing files one at a time kept only the last, and nothing rendered the list to reveal it. `FileRows` (modelled on the existing `LinkRows`) now holds `File[]` in state with the input demoted to a hidden trigger; ⚠️ its `onChange` must reset `e.target.value = ""` or re-adding a file you just removed fires no `change` event at all. ⚠️ **`handleSubmit` builds `new FormData(e.currentTarget)`, so it must `delete("files")` and re-append from state** — exactly the override `links` already performs on the next line.
  ⚠️ **The deeper bug was that EVERY refusal was a bare `continue`.** Wrong type, over 10MB, storage error — the client was thanked and the studio never learned an attachment was meant to exist. `describeUpload` (new, in `uploads.ts`) is now called by **both** the browser and the route, so the form refuses exactly what the server would and says why *before* submitting; anything still dropped server-side is listed under a **`FILES THAT DID NOT ARRIVE`** block in the brief and in red in the notification email. The overflow past 5 is reported too rather than being eaten by `slice` — verified live with a 7-file submission (3 stored, 4 reported with reasons), then deleted with its storage objects and the queue confirmed back to the same 4 real rows.
  **The allowlist widened**: `heic`/`heif` above all — every iPhone photo, so a client attaching four photos and a PDF got exactly one file through — plus `tif`, `tiff`, `eps`, `indd`, `sketch`, `fig`, `mov`, `mp4`, `rar`, `7z`, all forced to `octet-stream`. ⚠️ **`svg` stays rejected** (active content in a public bucket on our own domain — the point of the allowlist), and ⚠️ **`IMAGE_TYPES` was deliberately NOT widened**: it backs avatars and client icons, which render inline, and browsers display neither HEIC nor TIFF. ⚠️ `extOf` returns the whole filename when there is no dot, so a dotless name refused with ".screenshot files aren't supported" — caught by a test, not by eye.
- 2026-08-12 — **v1.15.0 — the mobile pass: the intake round on a phone, and Clients arrives there.** No migration. Desktop was signed off first, per the standing rule; everything here is phone-only and nothing above `md` changed.
  **The intake surfaces first.** Only what the v1.14.0 batch touched, and only where a phone can reach it — `/settings` and `/intake-queue` are both in the phone drawer, and the public form is filled on a phone more often than not. ⚠️ **The client form's link row was genuinely broken**: title and URL side by side at 375px gave the title 235px and left `flex-1` a **26px** URL field. They stack below `sm` now. The form's `p-8` also spent 64px of a 375px screen on padding (`p-5 sm:p-8`), and the queue's action row was a `flex-wrap` that stranded Approve on its own line under a Reject it has nothing to do with — it is two explicit rows on a phone and one right-aligned row from `sm`. Delete, notify, Reject, Approve, "Show brief" and the link controls are all 44px targets below `sm` and revert to tight desktop sizing above it. ⚠️ The card's title block had `min-w-0` but **no `flex-1`** — so it could shrink but never claimed the leftover space, and "Show brief" + delete took what they liked. That clipped the title **on desktop too** and squeezed the submitter line to ~230px on a phone, where it broke after nearly every word ("No / Traffic" on two lines). `flex-1` plus dropping the controls to their own row below `sm` fixed both at once. ⚠️ **The phone drawer KEEPS its avatar/name/sign-out block** even though the desktop sidebar dropped it: the desktop reason was that the header already names you top-right, and a 375px header has room for the wordmark, the sync dot and the bell and nothing else — remove it there and a phone names the signed-in person nowhere and cannot sign out. It gained "Latest updates" as its own row (a third item on the `About · v1.14.0` line overflows 375px). The 6th Settings tab needed nothing — `Tabs` is already `overflow-x: auto` (525px of tabs scrolling inside 343px).
  **The About panel became a TABBED popup on a phone.** It was drawn for a 5xl card and stacked to 1,039px inside an 812px viewport; because `Modal` is `fixed` + `-translate-y-1/2`, that overflowed off BOTH ends, carrying the ✕ above the screen with nothing to scroll. Now `max-h-[92dvh]` + three `md:hidden` tabs (`The studio` / `Timeline` / `Who built it`), every pane `md:block` so a laptop renders exactly what it did before. All three fit with **no scrolling** (537/617/637px); the `overflow-y-auto` is a safety net for when the generated figures grow, not the plan. ⚠️ The release rail is a percentage layout with 128px captions — fine across 900px, hopeless across 375, where eight milestones printed on top of each other — so below `md` it becomes a vertical list. Two overflow bugs fell out of this: the ledger column is a GRID ITEM (`min-width:auto`, so it refused to shrink and pushed both bars off the card — fixed with `min-w-0`), and hidden CONTAINERS still hold their padding and row gaps, which left ~80px of dead white under one tab.
  **Clients and one client's task list now work on a phone** (`client-mobile.tsx`), and both `desktop-only.tsx` entries are gone. ⚠️ These are SEPARATE components chosen in JS via `useIsNarrow`, not the desktop ones made responsive: `client-view.tsx` is ~2,900 lines of drag-and-drop, marquee selection, context menus and three view tabs that assume a pointer and ~900px, and rendering it behind `md:hidden` would still mount all of it. The phone build is the **task list only** — Timeline and Board are simply not offered, since the Gantt pins a 238px column before the calendar starts. ⚠️ It does **not** reuse `TaskTable` — that was the first attempt, and it cost ~850,000 iterations per render: `TaskTable` walks EVERY time entry in the studio inside its own `useMemo`, which is fine for the one instance on My Tasks but not for the ~18 this page mounts (one per group and per section), each scanning ~24,000 entries, and again on every 60s background refresh. `client-mobile.tsx` has its own one-line `TaskRow` reading the store's `taskMinutes`, which is the same total computed once — and `task-list-row.tsx` keeps zero props it added. Section → group → task nests with the same defensive rule as every other reader — a task counts as grouped only when its group belongs to its own section. Creating/reordering sections, dragging between them and inline editing are deliberately absent: those are desk jobs, and tapping a task opens the normal pane, which IS phone-built. The admin bottom bar swaps **My Tasks → Clients** (a designer, who has no `/clients`, keeps Tasks); the loser is still one tap away in the drawer.
  ⚠️ **The client page's card is ONE line, no "+ Time".** There the card is a row in a list you scan — section, group, twenty tasks — so the two-line card with its own action button turned one section into a lot of scrolling; title left, hours and date right (`items-baseline`, so a wrapped title keeps its figures level with the first line). Seven tasks now sit where four did. **My Tasks keeps the full card**, and that asymmetry is the point: that page exists to log time against your own work, while the client page answers "what is the state of this client's work". Sections AND groups both fold (`g:` namespaced keys, as on desktop), and **only the headings indent — the cards run the full width.** Indenting cards spent ~24px of every title, and 24 more inside a group, restating what the heading directly above already said.
- 2026-08-11 — **v1.14.0 — the intake round: the client's side of the form, and the reply back.** Migration **0028 APPLIED by Nitsan** (`task_requests.seen_at` / `seen_by` / `client_notified_at` — three plain columns; `task_requests` is already admin-only for ALL operations via 0001's `admin all` policy, so no policy or trigger work was needed).
  **Files and client links become REAL `links` rows on approval.** `approveRequest` reads `task_requests.answers`, and every uploaded file and every "+ Add link" row the client added is inserted into `links` (0022) against the new task — the same rows the studio's own link editor writes, so they render and edit identically. The brief copied onto the task is then **re-assembled without the FILES/LINKS blocks**: `assembleTaskBrief(a, attach?)` takes its attachments optionally, and the submission keeps them as text (the queue card and the notification email are the only way to reach them before approval) while the task does not. Leaving both would put every 150-character Supabase storage URL in the brief twice, which is the noise 0022 exists to remove. The link insert is deliberately **best-effort**: the task exists and the request is approved by then, and failing the approval over one link row would leave the queue and the task list disagreeing.
  **Three required fields, not twelve** — Name, Email, Task Name, which is exactly the server's own floor. Everything else is optional; `assembleTaskBrief` already lists gaps under MISSING INFORMATION, so nothing is silently lost, and a form that refuses to submit is a client who phones instead.
  **Repeat submitters are remembered in `localStorage`, NOT server-side.** The obvious design — an endpoint taking an email and returning the name/company behind it — was rejected: this form is unauthenticated and its URL is pasted into client emails, so that endpoint hands anyone who guesses an address the name and employer attached to it. The browser copy covers the case that actually recurs (same person, same laptop, brief after brief) and one click clears it. ⚠️ Never store the brief itself — a shared machine at the client's office would leak one client's request into the next person's form.
  **The client read-receipt is a BUTTON, never automatic.** Nitsan asked for mail "once a request is seen by an admin"; firing on render would mean opening the queue to triage one submission quietly emails every other client waiting in it. `/api/intake/seen` is admin-authed (cookie session → `profiles.role`, the `member-emails` pattern), and it is a route rather than a store write for two reasons the browser can't cover: `RESEND_API_KEY` is server-only, and the send-once guarantee needs the `client_notified_at` check and stamp inside one request. ⚠️ **`seen_at` is stamped even when the mail fails** — the admin did read it, and leaving the row untouched because a mail server was down would make the queue lie; `client_notified_at` stays null so the button remains.
  **The wording lives in Settings → Intake form**, in `app_settings.intake_seen_email` (key/jsonb, admin-write by RLS — no schema of its own). `renderSeenEmail` fills `{firstName}`/`{name}`/`{task}`/`{company}`/`{studio}` **before** escaping and escapes the whole result: escaping the template first and splicing raw values in is how `Smith & Sons` reaches a client as `Smith &amp; Sons`. The subject is not escaped (it is a header) but is stripped of newlines. Covered by `brief.test.ts` — 12 tests including the `javascript:`/`<script>` cases and the empty-task-name dangling dash.
  Also: approving now **opens the new task's pane** (`approveRequest` returns the id); every queue card has a delete, which leaves uploaded objects in the `intake` bucket on purpose — an approved request's files are already live links on a task, and orphaned objects are much the cheaper mistake; and the sidebar foot dropped the avatar/name/role block for textual **About · Latest updates · Logout** (the signed-in person is already named top-right), with "Latest updates" re-opening the what's-new panel via `whatsNewSince(null, …)` so it works for someone already up to date.
  ⚠️ **Untested end to end: the actual Resend call for the receipt.** Everything either side of it is verified, but firing it would have emailed a real No Traffic contact. The template renderer is unit-tested and the submission-notification mail already proves the account and `notifications@studionmore.com` work.
- 2026-08-11 — **v1.13.0 — task GROUPS: a subject inside a section.** Migration **0027 APPLIED by Nitsan.** Hierarchy is now **Client → Section → Group → Task**, one level only. A section is a phase ("Website"); a group is a subject — the several tasks that make up one webpage. **Nothing is ever logged against a group**, which is the whole reason it is its own table.
  ⚠️ **`task_groups`, NOT `tasks.parent_id` + an `is_group` flag.** The Asana model would have given the Timeline bar for free and been the wrong trade: ~30 files iterate `tasks` (My Tasks, the Board, autocomplete, Reports, KPI tiles, intake, `taskMinutes`) and every one would have silently started counting container rows as work. Keeping groups out of `tasks` means `tasks` still means what it meant yesterday. 0007 flattened the Projects layer for the same reason.
  ⚠️ **0027 re-declares the 0011 trigger to protect `group_id`** — the fourth migration to need that (0022 `start_date`, 0023 `timeline_position`). The trigger names its protected columns, so a new task column is member-writable by DEFAULT, and `section_id` has always been admin-only.
  **The one invariant: a task's group must belong to the task's section.** It is NOT a DB constraint — a composite FK would need `task_groups(section_id, id)` unique, which forbids the null-section case — so it lives in **`withGroupInvariant`** in `store.tsx`, applied to the PATCH before it is recorded, so `inversePatch` sees both keys and one ⌘Z restores both. Every reader is defensive to match: a task whose group sits in another section renders **loose**, never disappears. `updateTasksBulk` clears `groupId` whenever it sets `sectionId`/`clientId` — the only shape a uniform patch can carry the rule in, and correct rather than merely safe.
  **Store gets the section quartet one level down** — `addTaskGroup`/`updateTaskGroup`/`deleteTaskGroup`/`reorderTaskGroup`, same dense 1..n, one undo step, `wrote()` tails. Two deliberate differences: a group's position is scoped to its **section** (two sections' groups are independent runs), and **deleting one DISSOLVES it** — tasks move up to the section — rather than refusing while occupied like `deleteSection`. `deleteSection` now also refuses on groups, and `sectionIsEmpty` counts them, or the trash would be enabled and then fail. ⚠️ **`reorderTask` and `addTaskNear` are now scoped by `(clientId, sectionId, groupId)`** — a group's children densify among themselves, and renumbering the section's loose run from inside a group would shuffle rows nobody touched.
  **Order within a section: groups first, then loose tasks — NOT interleaved.** Interleaving needs one position space across two tables and a reorder that renumbers rows it doesn't own. Told Nitsan; his to overrule.
  **Timeline: a group draws a read-only STACKED BAR, not a section's bracket.** A bracket claims "everything under here falls between these dates"; a group is a thing in its own right, so it gets a bar across its children's span with two shims peeking out behind the top edge. ⚠️ **Read-only by Nitsan's choice** and `pointer-events-none`, so a marquee begun over it still reaches the rows beneath. Dragging it (moving every child by the same working days via the existing `updateTasksVaried`) is a noted follow-up. ⚠️ **Elements, not `box-shadow`** — CSS allows exactly ONE box-shadow declaration, which is why `barShadow` exists.
  ⚠️ **THREE places walk the rows by height** — the `bodyH` reduce, the marquee's `rowBands`, and the render loop — and all three had to learn the third row kind or the marquee selects the wrong tasks. Verified by measurement: a box over three rows *below* a folded group selected exactly design/visuals/mobile; an unaccounted `GROUP_H` would have shifted it by exactly one row.
  ⚠️ **`GroupHeaderRow` pins `STICKY_W`, not `leftW`** — same trap `SectionHeaderRow` hit, where an opaque 666px band sat on top of the calendar and swallowed the summary bar whole.
  **"🧮 Section totals" in the Show menu**, per tab, **on by default for the Timeline** (a folded group is a bar with no numbers otherwise) and off for the Tasks tab. ⚠️ **It must NOT light the Show trigger** — the trigger reports what you are NOT SEEING, and a total being off hides nothing; verified the button stays grey/muted with totals on. One helper, **`src/lib/task-rollup.ts`**, so four surfaces cannot disagree; it absorbs the Timeline's old inline group min/max. ⚠️ **`Section.estimateHours` (recovered from the old section name) BEATS the sum of its tasks**, and **`Section.legacyHours` must never be added** — `taskHoursDone` already folds each task's own `legacyHours` in, so adding the section's counts the same hours twice. Pinned by a test.
  **Timeline totals fill the real columns (Dates/Duration/Actual/Budget); the Tasks tab gets a compact strip instead** — that header has a `flex-1` name and no column grid, and forcing one would pin every section name to a fixed width to make room for numbers that read fine as a line. ⚠️ The Tasks strip counts plain Sun–Thu working days; the Timeline passes the studio's holidays, so a duration there can be a day shorter. **The Timeline's is the one to trust** — it is the one drawn against a calendar.
  **Public client Gantt shows groups too**, as folded summary bars in the client's own colour, and both container kinds now carry a rolled-up **budget** in the tasks' own column (a blank there beside rows that all have a number read as missing data). ⚠️ **Budget only, never logged hours** — verified in the served payload: the only fields present are id/title/sectionId/groupId/startDate/dueDate/typeName/typeColor/order/budgetHours, and the two `:26,` hits are `"order":26`, not the 26h logged on that task. Ordering is copied tiebreak-for-tiebreak from the studio chart, since a client comparing the two must not see a different order.
  **The task panel gained a Group row** under Section — dragging is fine on the client table and hopeless from a task opened via the plan or the feed. ⚠️ It offers **only the groups of the task's own section**: a group belongs to one section, so offering another's would move the task twice in one control. Hidden entirely when the section has no groups.
  ⚠️ **A `night`-only contrast failure, caught by measuring all four themes.** The group bar is `color-mix(--foreground 58%)`, so it composites DARK in the three light themes and **LIGHT under `night`** — where a white folded-group label measured **3.0:1**. `--background` fixed night and dropped `electric` to 4.09; **`--surface` clears both ends (4.61 / 4.61 / 4.97 / 6.2)** and is the principled choice anyway: the label is punched out of the bar, showing the row's own background through it. Same rule as v1.9.0 — a tint of a token and that token travel together; only a FIXED accent needs a fixed ink.
  ⚠️ **Method note, cost me two false conclusions: the browser tool's `key` action needs the literal `"Enter"`. `"Return"` dispatches `{key:"", code:"", which:0}`** — an empty event that matches nothing. I concluded from it that implicit form submission was broken and wrote that "verified against the DB" into a code comment; it was verified against a keystroke that never arrived. **Probe `e.key` before believing a keyboard test.** (The explicit Enter handler on `AddGroupRow` stayed anyway — it is the convention `InsertTaskRow` set in v1.9.1 and cannot be wrong.) Also: `read_console_messages` accumulates across reloads, so a pile of 400s can be entirely pre-migration — navigate, then read.
  Verified in the browser signed in as admin on Anchor, with 0027 applied: group created, renamed, folded, filled via the panel, dissolved; `group_id` and `section_id` both persisted across a hard reload; ⌘Z restored a cleared group (⚠️ **focus must leave the `<select>` first** — the undo binding deliberately defers to the browser's native undo inside a field); section/group/task figures agreed exactly (`26h / 100h`); marquee correct; public link correct. **All test data removed and the client verified clean** (0 groups, 0 grouped tasks, "Branding" back to its original section, dates and budget untouched). **No time entry was written.** Gates: tsc clean, eslint 0 errors / 36 warnings (unchanged), **73 tests** (was 59), production build clean.
  **Second round, all from Nitsan reading the shipped version:**
  - **Right-click gained "New group…" and "New section…"** on both task tabs. ⚠️ The group item is CONTEXT-AWARE: right-click a row that is part of a multi-selection and it becomes **"Group the N selected tasks…"**. Showing both every time would make the common case pick from four items.
  - **"Group…" in the selection bar**, put in **`TaskBulkControls`** rather than in each bar — the client table's `SelectionBar` and the Timeline's both render it, and the whole point of that component is that the two cannot drift. New store method **`groupTasksIntoNew`**: ⚠️ it REFUSES a selection spanning two sections (a group belongs to one) and says which axis disagrees, rather than silently re-sectioning tasks — which on a client page would move work between phases. Surfaced through the new **`showNotice`**, not `noteWriteError`: being told "that isn't possible" is not a failure to save. ⚠️ Creating the group is not undoable and the move is one `updateTasksBulk` step, so ⌘Z takes the tasks back out and leaves an empty group — visible, and one click to remove.
  - **Drag-into-a-group already worked**; verified both directions with synthetic `DragEvent`s (drop on the group sets `group_id`, drop on the section header clears it).
  - **Timeline group bars are BLUE and the folded stack is OPAQUE** — the neutral `--foreground` mix read as black, which put the chart's heaviest mark on the one row that isn't work. ⚠️ **`--brand` is `#0b43ed` in ALL FOUR themes**, and on `night`'s `#141519` surface that is **2.64:1** — survivable for a filled button with white text, not for a 3px rule and an 11px caption. Hence a new **`--group-bar`** token that `night` lifts to the palette's light blue `#6181e8`. Measured 6.91 / 6.91 / 6.91 / **5.05**, and the folded label is `--surface` (punched out of the bar) so ink and bar invert together.
  - **Two shapes for two states**, his direction: **open** = a thin blue rule with the name ABOVE it and **no end tips** (the rows are the caption, and repeating the section's tips one level down made two brackets of the same shape in different sizes); **folded** = the stacked bar, because the rows are gone and the group has to stand in for them.
  - **A group's task rows are indented on the Timeline** (`TL_INDENT`, padding on the pinned block so `STICKY_W` is unchanged). ⚠️ Only the CHILDREN move — a loose task and a group are really siblings, so aligning those exactly would be more correct, but it would re-indent every row on every client's chart for a feature most sections don't use.
  - **Removing a group asks WHICH removal you meant** — a real dialog, not a `confirm()`, because `confirm()` has two buttons and both would have to be "yes" while one reading CASCADES to time entries. "Remove the group only" (tasks move up, keeping every hour and date) vs "Delete the group and its N tasks", and ⚠️ **the destructive option is DISABLED when any task carries logged hours**, with the figure in the copy — the same rule `deleteTasksBulk` already follows. Verified: with 26h on Branding it came up disabled and explained.
  ⚠️ **Method, twice over.** (1) **The browser tool's `key` action needs the literal `"Enter"`; `"Return"` dispatches `{key:"", code:"", which:0}`** — an empty event matching nothing. I read a failed keystroke as a broken form submission and wrote "verified against the DB" into a comment; it was verified against a keypress that never arrived. **Probe `e.key` before believing a keyboard test.** (2) **The stale-CSS trap is real and silent**: `--group-bar` was correct in `globals.css` and simply absent from the served bundle. `curl`ing the CSS chunk and finding `brand-soft` 40× and `group-bar` 0× is what proved it; `rm -rf .next` + a dev-server restart fixed it. Also `read_console_messages` accumulates across reloads, so a pile of 400s can be entirely pre-migration.
  **Announced to the team**: a `RELEASES` entry with **`audience: "admin"`** (groups live on `/clients`, which designers cannot open — telling them about a screen they have no route to is the noise that teaches people to dismiss the panel unread). Its visual is **generated at life size** by **`scripts/build-whatsnew-group.mjs`**: 372×206 drawn with the client table's own values, which the panel shows at ~318px — **0.85, so a real 13px label lands at 11.1px**. ⚠️ The crop is chosen FOR that scale: drawing the Timeline's pinned column plus a useful slice of calendar is ~570px and would have scaled to 0.55, putting those labels at 7px — the exact failure the phone mockup was rebuilt to avoid. The hierarchy is the news and the hierarchy is in the name column. Verified in the panel with `naturalWidth 372` (not the silent-failure 0). The second item gets **`build-whatsnew-totals.mjs`** — the Show menu at `show-menu.tsx`'s own values (372×422, so 0.85 and a 14px label at 12px, standing 360px inside the 440px panel). ⚠️ **The WHOLE menu, type list included**, not the switches with the rest sliced off: a card cut at its edge reads as a fault, which is what makes this one taller than its sibling. ⚠️ And the trigger is drawn **grey, not lit** — totals being on hides nothing, and a picture showing it brand-coloured would teach the opposite of what the control does.
  **NOT done, deliberately:** mobile is untouched — the client page is already desktop-only on a phone, and the standing rule is desktop first, then ask.
- 2026-08-11 — **v1.12.1 — milestones finally appear on the shared client Gantt.** No migration. ⚠️ **The v1.10.0 entry below claims they were "always visible on the shared client Gantt". They never were** — `timeline_marks` was neither fetched in `gantt/[token]/page.tsx` nor rendered in `public-gantt-view.tsx`. The intent was real and recorded: migration 0026's closing comment says "No anon policy: the public /gantt/[token] page reads via the service role", so the table was designed for this read and only the wiring was missing. **Treat a "shows everywhere" claim in this log as a claim to re-check, not a fact.**
  - **The layer is a read-only copy of `MarkLayer`, not a shared component.** The studio's version is 170 lines of drag, rename, delete and empty-title handling, all of which is `canEdit`-gated and none of which the client may have; the drawing itself is ~20 lines. Extracting it would have meant threading eight no-op callbacks through a public page to reuse a rectangle.
  - ⚠️ **Milestone dates widen the chart's date window.** `from`/`totalDays` was computed from task dates plus today, and a launch is routinely set AFTER the last task's due date — so the one date the client most wants to see would have been drawn off the right-hand edge with no scroll reaching it.
  - ⚠️ **Unnamed marks are dropped server-side.** The studio's chart creates a mark empty and names it after, rendering the gap as "Untitled" — which there is a prompt to finish the job. A client can't act on it, so it is only a nameless line on their plan.
  - ⚠️ **The flags sit at `z-[15]`/`z-[14]`, NOT the studio chart's 30/20.** At 30 a flag rides OVER the pinned task-name column as you scroll — measured: "brand begins" overlapped the name column by 50px. Below the pinned column's `z-20` it slides under instead. Verified by hit-test, not by eye: `elementFromPoint` at the flag's own position returns the pinned section header. The studio can live with the overlap; the page a client reads should not have it.
  - Lines still sit ABOVE the rows (z-14 > the rows' `auto`) for the v1.10.0 reason — under them, 40 `border-b`s cut the line into a dashed column.
  - **Known, pre-existing, and NOT introduced here:** a line scrolled behind the pinned column peeks through the 1px gaps between pinned cells (the row border sits outside the cell's box). The **today line does exactly the same** — both measured inside the pinned column at the same moment — so the mark lines match what already ships rather than adding a new artefact. Fixing it means an opaque backdrop behind the pinned column on both charts.
  Verified on Anchor's real link at 1440×900 and 375×812: 3 flags and 3 full-height lines at all three zooms, flags sticking exactly at the ruler's bottom edge (123px against a ruler bottom of 124) after scrolling 900px down. Gates: tsc clean, eslint 0 errors / 36 warnings, 59 tests, prettier clean, production build clean.
- 2026-08-11 — **v1.12.0 (DEPLOYED + committed `d777223`, pushed to main; prod verified serving v1.12.0 on tracker.studionmore.com) — the tracker works on a phone.** No migration. Scope chosen with Nitsan and deliberately partial: **log time, see my tasks, approve/triage**. Six routes stay desktop-only. Every change is `md:`-prefixed or gated on a new `useIsNarrow()`, so **nothing at ≥768px moves** — verified by measurement at 1440 (sidebar 208px, content margin 208px, `main` padding 24px, header one row, all unchanged).
  ⚠️ **iOS Safari zooms the page whenever a focused field is under 16px, and every field in this app is `text-sm`.** That looked like it needed an `Input` primitive and 45 edits before any mobile work could start. It didn't: **`globals.css` is UNLAYERED and Tailwind v4 emits utilities inside `@layer utilities`, so an unlayered rule beats a layered one whatever the specificity** — six lines in a `max-width:767px` block override `text-sm` at all 146 field sites. Verified in the built bundle: the rule lands at byte 72096, `.text-sm` at 42201, no enclosing block. The `maximum-scale=1` viewport hack is deliberately NOT used (iOS honours it inconsistently and it breaks WCAG 1.4.4).
  **Shell = bottom bar + drawer** (`app-shell.tsx`). Bar: Home · Tasks · **+ Log time** (an action, not a route) · Inbox (admin) · Menu. The sidebar is `hidden md:flex`; the content margin is `md:`-prefixed on BOTH branches, or it indents by the width of a rail that isn't there.
  ⚠️ **`--sb-active-fg` IS NOT USABLE AS FLAT TEXT ON THE SIDEBAR BACKGROUND.** It is the ink for text on `--sb-active-bg` (the white pill), and under `electric` it is `#0b43ed` — the exact blue of `--sb-bg`. The bar's active item rendered at **1.00:1 in BOTH `electric` and `gallery`**, i.e. invisible, while looking fine in `night` and `default`. Caught in a screenshot, then confirmed arithmetically across all four. The pair that always works on a bar with no pill is **`--sb-fg` (active) vs `--sb-muted` (inactive)** — 6.9–20.0:1 and 4.2–5.7:1.
  **Six routes are desktop-only** — `/plan`, `/clients`, `/clients/[id]`, `/reports`, `/client-reports`, `/feed` — hidden from the drawer via `NAV`'s new `mobile` flag AND given an explaining card in `desktop-only.tsx`. **Not a redirect**: several are linked from elsewhere and a redirect destroys the URL somebody was sent. The card renders INSIDE the shell, so nav still works. **Keep the two lists in agreement** — hidden with no entry means a phone renders the real, broken page.
  **Log time, two shapes.** From a task (row or panel) the task is known, so it is one bottom sheet. From the bar's "+" it is two steps — full-screen search with recents, then the form — because cramming search + results + duration + description + date into one sheet leaves the search two rows. ⚠️ **The phone picker cannot reuse `TaskAutocomplete`**: its list is `absolute` and the sheet body is an `overflow-y-auto` scroller, which clips BOTH axes (fifth instance). The rule is shared as **`useTaskMatches`** and the phone renders it inline. Quick chips 30m/1h/2h, `md:hidden`.
  **Task panel: three tabs (Details/Time/Talk) and READ-ONLY fields on a phone**, Nitsan's call. ⚠️ The panes are wrapped in **`contents` / `hidden md:contents`**, not rendered conditionally — `display:contents` removes the wrapper from layout so at ≥768px the children stay direct flex items and the existing `gap-5` is untouched; conditional rendering would also discard the comment draft on every tab switch. Read-only reuses the **member** branches that already existed at all six `isAdmin ?` sites. ⚠️ **`QuietSelect` (Assignee/Type/Status) had NO read-only branch** — those are member-editable by design — and was the only editor to survive the gate; it now renders the chosen option's label as text.
  ⚠️ **`PeriodStepper` must own its own overflow.** Four pills + the stepper need ~500px, so it wrapped to three rows at 375px. The first fix made it `flex-nowrap` and leaned on the home page's scrolling filter strip — which pushed **/team's document to 637px wide**, because /team and /reports host the same control with no scroller. It now scrolls itself (`-mx-4 … overflow-x-auto … md:overflow-visible`), which fixes all three hosts and can't be broken by a future caller.
  **Admin home header: ~440px → 136px**, at Nitsan's request ("don't waste place, stack them better"). Greeting, tenure, billable switch, range pills, stepper and client select each took a row because one `flex-wrap` row simply wraps six items at 375px. Now two lines: identity with the tenure counter pulled up beside the name, then billable + client with the stepper `order-last basis-full` below it. **First KPI tile moved from y≈570 to y=224** — all four tiles and the week timesheet now fit the first screen. ⚠️ `order-last` rather than reordering the JSX: DOM order IS the desktop order.
  **Also:** `Tabs` (`ui.tsx`) gained `overflow-x-auto` + `shrink-0` — it had neither wrap nor scroll, so Settings' five tabs ran off the edge with no way to reach the last two (reaches all 8 call sites); member "My week" scrolls below `sm` (5 cards × 63px fits neither a weekday nor an hours figure); the hours-by-client **donut is `md:` only** (drawn at a literal 300×300 with a legend beside it — at 375px the client names become one character each) while the trend chart stays, since it already renders through a `viewBox` at `width="100%"`; `TaskTable` renders **client-grouped cards** below `md` and the `colw.*` widths are simply not consulted there, so a dragged desktop layout survives a phone visit.
  **New files:** `src/lib/use-is-narrow.ts` (⚠️ built on **`matchMedia` + its `change` event**, never a `resize` listener — same source of truth as the `md:` classes, and the browser tool does not dispatch `resize`), `mobile-sheet.tsx`, `mobile-log-time.tsx`, `desktop-only.tsx`, `primitives.tsx` (Button/Field/Card — every variant emits the class string that already ships, so a converted call site is provably a no-op; **no weight prop**, because globals.css collapses medium/semibold/bold onto one weight and that is how 23 button strings grew from ~5 real buttons).
  Verified in the browser signed in as admin at 375×812, at the 767/768 boundary (both flip together — `matchMedia` and the `md:` classes agree, no dead zone) and at 1440×900. Log-time verified end to end **without writing a row** (the dev server points at the live studio DB). Gates: tsc clean, eslint 0 errors / 36 warnings (+3, all the pre-existing `set-state-in-effect` advisory shape), 48 tests, production build clean.
  **Follow-ups from Nitsan reading it on the phone, same release:** "Coming up" (Celebrations) is off on mobile — ⚠️ the gate goes on a WRAPPER with `md:contents`, because the pane's own div carries `empty:hidden` and adding `hidden md:block` beside it would let `md:block` win at desktop and leave an empty box on quiet days. The studio roster is a **list** below `sm` (two columns of portrait cards gave each ~150px for an avatar, a name, an hours figure and a bar stacked — four rows of chrome and two people per screen; on their side it is one row each). Team-card photos go **124 → 64px** on a phone, which more than halves the card — it's a `size` NUMBER, so it needs `useIsNarrow`, not an `sm:` prefix. Team and intake headers stack: both were one no-wrap `justify-between` row, so the subtitle and the checkbox squeezed each other and every label broke mid-phrase.
  ⚠️ **On the intake header, `flex-wrap` alone did nothing.** The title block is `flex-1`, so flex shrank IT to min-content rather than wrapping anything — leaving all three items on one line with "Intake Queue" broken across two rows. **`basis-full` on the checkbox** is what forces the break. The three are flex SIBLINGS reordered with `order` rather than nested in two groups, which is what lets the button ride beside the title on a phone **without a second instance** — it prefetches its URL on mount, so two would mean two fetches. Its label is "Form link" below `sm` and "Copy form link" above (two spans, not a JS branch, so there's no hydration flash), and the h1 drops to `text-xl` because at `2xl` the title plus the button need 7px more than a 375px screen has.
  **Also new: a "Version X is out" panel — an onboarding walk-through on a laptop, a plain list on a phone.** `src/lib/whats-new.ts` holds the notes; `whats-new-modal.tsx` renders them.
  - **It shows everything you MISSED, not just the newest release.** `whatsNewSince(seen, isAdmin)` returns every release newer than the version you last dismissed, so somebody back from leave is caught up in one panel. If it only ever announced `APP_VERSION`, two releases in a week would mean the first reached whoever happened to sign in that day and was silently lost for everyone else. ⚠️ **Compare with `compareVersions`, never as strings** — `"v1.9.0" > "v1.12.0"` alphabetically, which would hide every release between them. Pinned by a test.
  - ⚠️ **`seen === null` means BOTH a brand-new account and the whole team the first time this ships**, so it shows the entire list. That is deliberate: it is what catches the studio up on v1.10–v1.11, which predate the panel. **Trimming `RELEASES` is the only lever for stopping a new hire reading history** — a date cutoff would go stale silently.
  - **Each item carries its own `image` and becomes one STEP** with a Next button (`MAX_STEPS` 5, `MAX_RELEASES` 3; the remainder is summarised as "+ N more", and ⚠️ that count must cover items dropped by BOTH caps — a test pins it). An item with no picture falls back to the studio wordmark on the blue. **Draw images 200×400 (a phone) or 1:2** — the blue panel crops the bottom so the device bleeds off the edge.
  - **A phone gets the plain list instead** — no stepper, no pictures: five Next taps is a lot for something nobody asked to read, and the pictures are phone mockups being shown on a phone.
  - **The `audience` tag is the whole point** — an admin-only change shown to a designer is noise that teaches them to dismiss the panel unread, which costs you the release where it mattered. A release whose every item was for the other role is dropped entirely rather than shown empty.
  - ⚠️ **Dismissal is localStorage, not `profiles`** — the repo's convention for per-user UI preferences (v1.1.0), and 0021's trigger lets a member write only `name`/`avatar_url`/`photo_url`, so a `seen_version` column would need an amended trigger or a service-role route for "I've read this". Cost: laptop and phone each show it once. ⚠️ **Marked seen on DISMISS, not on open** — a reload mid-read would otherwise lose it permanently. Suppressed on `/welcome`, which IS somebody's first sign-in.
  - ⚠️ **Two layout traps this hit:** `className="p-0"` on `Modal` does nothing (needs **`p-0!`**), and **`ModalClose` is not absolutely positioned** — dropped into a flex row it becomes the first column and shoves the blue panel off the card's edge.
  - **To announce a release: add an entry whose `version` is newer than the last.** No entry, or no items for that role, means no panel. Verified: admin got 5 steps, `?viewAs=Nadav` got the member subset, dismiss persisted across a reload. 57 tests (was 48).
  **Second round of phone follow-ups (Nitsan, same release) — the member home fits two screens instead of four.** Every change is `md:`-prefixed or gated on `useIsNarrow`; measured unchanged at 1440 (header 52px, hero `mt-9`/`pr-32` intact, both hidden rows back at 278px and 366px, My week five columns).
  - **"Log my hours" and "My tasks" are `md:` only now** — on a phone they duplicate the bottom bar's "+" and Tasks, and the two of them WERE the whole second screen. ⚠️ **That orphans the hero's `#log` anchor**, which is the CTA the release was built around: the pane it scrolls to no longer exists there. The hero now renders **two buttons** — a `md:hidden` one opening `MobileLogTimeSheet`, the `hidden md:block` anchor above it — rather than a JS branch, so there is no hydration flash. The sheet is self-contained, so `Dashboard` owning a second instance beats threading a callback down through `AppShell`'s children.
  - **My week shows today onwards below 768px.** ⚠️ **The fallback is load-bearing**: the studio week is Sun–Thu, so on a Friday or Saturday every card is in the past and filtering leaves an EMPTY pane — it falls back to the whole week there. ⚠️ **Its layout breakpoint had to move `sm` → `md` to match `useIsNarrow`**: the card count now varies below 768, and `sm:grid-cols-5` would have laid two remaining days out in fifths of the width. Cards gained `grow` so a short week fills the row; with all five the basis already exceeds the screen, so nothing grows and it scrolls as before.
  - **Header: four rows → two.** The member's "Tasks assigned" counter and the client select are both `md:` only — the bottom bar already carries Tasks, and scoping your own hours to one client is desk work. Identity + the period stepper is what's left.
  - **The gaps around the blue hero were mostly two `mt-9`s that a phone never needed.** ⚠️ **`mt-9` is the portrait's headroom and the portrait is `hidden sm:block`** — so below `sm` it was 36px of nothing above the hero; the tiles' own `mt-9` only earns its place at `lg`, where they are the column BESIDE the hero and must start level with it. Same cause, same file: ⚠️ **`pr-32` is the portrait's COLUMN**, reserving 128px of a 295px line on a screen with no portrait, which is what wrapped the hero heading onto three lines. All three are now breakpoint-gated, and the page gap is `gap-2 md:gap-4`. Result: 8px between every pane, hero heading on two lines, the whole member home ending at 929px.
  - **The phone wordmark is `text-brand`.** It inherited the foreground and so rendered Studio Black — and below `md` it is the ONLY place the mark appears. **The DRAWER's wordmark is deliberately left white**: it sits on `--sb-bg`, which under `electric` IS brand blue, so brand-blue ink there is invisible — the same 1.00:1 trap as `--sb-active-fg` on the bottom bar.
  - ⚠️ **Method: `resize_window` dispatches NEITHER `resize` NOR a `matchMedia` `change` event.** `mq.matches` flips but nothing fires, so anything gated on `useIsNarrow` keeps its old shape while the `md:` classes around it have already switched — My week read three cards at 1440 until a reload, which looks exactly like a broken hook. Measured: an armed `change` listener took **0 hits** across 375 → 1440. **Reload after resizing.** The hook's own comment claimed the opposite and has been corrected.
  Verified in the browser signed in, at 375×812 (`?viewAs=Nadav` for the member view) and 1440×900. The hero's phone CTA was confirmed to open the log-time sheet; **no row was written** — the dev server points at the live studio DB. Gates: tsc clean, eslint 0 errors / 36 warnings (unchanged — the new `useMemo` hoists `weekStartIso` out of its dep list rather than adding a suppression), 59 tests, production build clean.
  **NOT done, deliberately:** global search is desktop-only on a phone (it wants ~400px and the 375px header already holds the wordmark, sync dot and bell — the log-time sheet has its own task search); the intake-queue detail screen and the read-only member summary are still the desktop layouts, which already respond acceptably.
- 2026-08-10 — **v1.11.1 (DEPLOYED + committed `040ddfb`, pushed to main; prod verified serving v1.11.1 on tracker.studionmore.com) — an edit no longer jumps back when a background refresh was already in the air.** No migration. Nitsan: *"sometimes when I change something it jumps back"* — he guessed undo, other users, or the sync. It was the sync, and the diagnosis is worth keeping because the guard that failed looks correct.
  - Every store mutation is optimistic, and `refresh()` guards against clobbering one — but it tested `writesBusy() || focusInEditor()` **only before fetching, never again after the response landed**. A refresh already in flight when the user edited something returns rows read BEFORE that edit; by the time they land the write has usually settled, so every "is a write in flight?" test says no and `applyHot` paints the pre-edit row over the user's own change. `mergeTasks` preserves only `brief`, so everything else is wholesale. The next tick then fetches the real value and it jumps forward again — which is why it reads as a flicker rather than as lost work. **The server was never wrong.**
  - ⚠️ **A busy-counter cannot answer this question.** `writes` goes back down when the write settles, and the settle usually beats the response home. New **`writeSeq`** counts writes ISSUED and never decrements; sampled before the fetch and compared after, it's the only test that can tell the response is older than the screen. Rule extracted as **`refreshVerdict()`** in [snapshot.ts](src/lib/snapshot.ts) — `apply` / `stale` (superseded by a newer refresh; drop silently) / `deferred` (older than local state; drop AND refetch) — with a test file pinning the settled-write case.
  - The same race existed in **`loadTaskExtras`**, the open task pane's lazy re-fetch of comments, attachments and hours — the most-edited surface in the app. Guarded too, but **only when `refetch: true`**: the first open has nothing to fall back on, so bailing there would leave the pane permanently empty.
  - Known and accepted: if a deferred response's write had already settled, nothing drains `refreshQueued` until the next 60s tick. Staleness is capped at the normal cadence, so it isn't worth a retry timer that would poll every 1.5s during heavy editing.
  - **NOT verified at runtime** — reproducing the race needs a signed-in session and Claude can't enter credentials. Verified by construction, 48 tests (was 42), tsc, lint and a production build.
- 2026-08-10 — **v1.11.0 (DEPLOYED + committed `cebabf6`, pushed to main; prod verified serving v1.11.0 on tracker.studionmore.com) — the Timeline's marks get calibrated, and the Board becomes the studio's statuses.** No migration (0025 + 0026 already applied). Eighteen commits, nearly all from Nitsan reading the shipped v1.10.0 in front of him.
  - **The two verticals were the wrong way round.** The milestone lines were solid and the today line was half-strength, so a mark the studio invented outranked the one fact the chart can't be wrong about. Swapped, then today went **black** rather than brand-blue: with type colours on every bar, a blue line reads as one more category. Today is now a single marker — a black date chip in the ruler with a downward tail meeting the line, per Nitsan's reference — drawn **beneath** the bars, so it locates work rather than crossing it out. The past is shaded; the chip, the line and the shading all land on the same pixel (checked at 532px on a day-zoom chart).
  - **Section brackets: 3px, dropped low in a 36px row, dark not pink.** `SECTION_BAR_H` was `BAR_H * 0.3`, which at 6px still read as a bar of its own — a section is a bracket over its rows, so the tips talk and the line just joins them. `SECTION_H` went 30 → 36 to leave room for the name **above** the bracket, which is what a folded section shows in place of its rows.
  - **Task bars: 27px in a 34px row, with a soft two-layer shadow, and an optional plain (white) mode.** ⚠️ **CSS allows exactly ONE `box-shadow` declaration**, so the selection ring, the plain-mode outline and the drop shadow have to be composed into one string — see `barShadow` in [client-timeline.tsx](src/components/client-timeline.tsx). The multi-select ring is `inset 0 0 0 1px var(--brand)`; 2px was Nitsan's first complaint about it.
  - **The Show menu's three view settings became emoji + switch rows**, while the type filter stayed checkboxes — those are a list you tick items out of, these are settings you flip. Only the type filter lights the trigger, so the resting state of a client page can't look filtered.
  - **The Board's columns are now the studio's statuses (`tags`), and its cards drag between them.** They used to be three hardcoded pillars — To do / In progress / Done — that existed nowhere else in the app and could only be set by dragging, so the board was a second, private status system. The status chip came off the cards (the column already says it), a leading **"No status"** column holds the unassigned, and the rail scrolls sideways rather than wrapping when the statuses outgrow the width.
  - **Board became its own tab** — `Task list · Board · Timeline · Overview` — which retired the list/board segmented control. Both task arrangements **stay mounted** when hidden: column widths, selection and collapsed sections are local state a round trip would throw away. Each keeps its own Show settings, following the per-tab rule.
  - ⚠️ **The board's horizontal scrollbar was thousands of pixels down the page**, because the rail was as tall as its tallest column (117 cards on Anchor). The rail is now measured to the window (`window.innerHeight - top - 24`, floor 320) and each column scrolls internally, so the only scrollbar on the rail is the horizontal one and it's always on screen. `min-h-0` on both the column and its card list is load-bearing.
- 2026-08-09 — **v1.10.0 (DEPLOYED + committed `239d427`, pushed to main; prod verified serving v1.10.0) — the Timeline gets multi-drag, marquee selection and milestones; both task tabs get one Show menu and one layout.** Migrations **0025** (`gantt_links`) and **0026** (`timeline_marks`) APPLIED by Nitsan. ⚠️ **After a `create table`, PostgREST serves a STALE schema cache** — every insert came back *"Could not find the table in the schema cache"* with a 404 for several minutes while the table plainly existed. It self-heals, or `notify pgrst, 'reload schema';` forces it. Don't debug the code first. ⚠️ And a Turbopack **parse error can outlive the file that caused it**: the dev server replayed one for a file `npm run build` compiled cleanly. `rm -rf .next` and restart before believing it.
  **Multi-drag.** Grab a bar that is in the selection and the whole selection shifts by the same number of working days, each task keeping its own duration. MOVE only — a 2-day task and a 3-week task do not want the same edge nudge. New store method **`updateTasksVaried`**: per-task patches as ONE history entry, the varied sibling of `updateTasksBulk`. Looping `updateTask` would have written the same rows and left ten undo steps behind. Verified: 3 bars +2 days, committed, one ⌘Z restored all three.
  **Marquee.** Press empty calendar, drag a rectangle. Hit-tests the BAR, not the row, so a box over August takes the tasks that run in August rather than everything whose row it crossed. Starts only from `data-chart-bg`, so a press on a bar is left to the bar's own drag.
  **Milestones** (`timeline_marks`): click the ruler to place one on the exact day under the pointer at any zoom, type its name, drag it to move, trash to delete. ⚠️ **Deliberately NOT snapped to a working day** — task dates are, because work happens Sun–Thu, but a launch can be a Friday. ⚠️ **The line and its label need OPPOSITE depths**, and that needs TWO layers: the line under the bars (a milestone marks the work, it doesn't cut through it) and the label above everything. One wrapper with a `z-index` creates a stacking context the label can never escape, which is how the name field ended up covered by the section bar it sat on. ⚠️ **The drag must NOT `preventDefault` on pointerdown** — that suppresses the click that follows, which is why the trash did nothing; the drag records whether it moved and the rename click bails on that instead. ~~Always visible on `/gantt/[token]`, so a mark is client-visible the moment it is saved.~~ **← WRONG, corrected 2026-08-11: the public page never fetched or drew `timeline_marks` at all. Shipped in v1.12.1.**
  **One "Show" menu** for completed / undated / type, on both task tabs, **per tab**: Undated wants opposite defaults (the Timeline hides dateless rows, the table shows them) and one value cannot have two. ⚠️ **The trigger reports state** — folding filters into a menu is how a filtered chart comes to look like a quiet month. Only the TYPE filter lights it; completed/undated being off is these views' resting state, and a badge on open is noise everyone learns to ignore. **The legend is gone entirely** — the Type column shows each row's colour beside its name, which is the same information per row rather than a key you have to hold in your head. The Show menu is the only filter control, and ~130 lines went with it.
  **Both tabs now share one toolbar**: identity and Share in the header, tabs left, the view-mode switch centred (day/week/month, or list/board), Show + Columns right. Client-report buttons removed from the client page; the edit pencil moved beside the client name, unboxed.
  ⚠️ **`bg-background/40` over a `bg-background` canvas is the same colour** — the row hover tint was invisible the moment it reached the calendar. And a translucent tint REPLACES `bg-surface` rather than layering over it, so the pinned column went 40% transparent on hover and the calendar showed through the task's own name. Pinned blocks now mix an opaque tint; the chart area keeps a translucent one, because it paints over the grid and an opaque tint blanks that row's weekend shading.
  Also: week rules 7%→10% and month rules 15%→18%; month markers 12px uppercase tracked (AUG); today line 1px→2px.
- 2026-08-09 — **v1.9.5 — the app's Timeline card takes the height the page leaves it.** No migration. Same complaint as v1.9.2, now on the studio's own Timeline rather than the client's: `max-h-[min(70vh,640px)]` put a 1679px-tall plan in a 640px box with 175px of empty window underneath at 1800×1000, and far more on a 27".
  **MEASURED, not `h-dvh` + `flex-1` as on the public page.** That page owns its whole layout; this card is one tab of three inside the app shell's `main`, and giving the shell a definite height would change how Tasks and Overview scroll. The card reads its own top offset on mount and on resize instead, which costs one layout read and leaves every other page alone.
  ⚠️ **Subtract what is BELOW the card as well.** Taking only `innerHeight − top − padding` left the document 29px too tall — a scrollbar on a layout whose whole point is not to have one — because the "N tasks with no due date aren't shown" note sits under the card inside the same panel. It now measures the panel root's bottom minus the card's bottom. Result at 1440×900: card 762 → the footnote sits exactly on the fold, `scrollHeight === innerHeight`. At 1600×1400: 1162px, 33 rows visible instead of 18. Floor of 320px so a very short window scrolls the page rather than showing a sliver of chart.
  ⚠️ Method, again: **`resize_window` does not dispatch a `resize` event** — hand-fire `new Event("resize")` or the listener looks dead. And a measurement read in the same tick as the state change reports the OLD value; read it in a later call.
- 2026-08-09 — **v1.9.4 — "Add time" on a task row opens a real popover again.** No migration. Nitsan pressed it on My Tasks and got a collapsed strip with a clipped form in it.
  ⚠️ **`AddTimePopover` was `absolute` inside the table's `overflow-x-auto` wrapper — and a scroll container clips BOTH axes.** So the panel was cut off at the wrapper's edge and took the row's height with it. It is now `fixed`, PORTALLED to `document.body`, positioned from the button's rect, flipping above when the window runs out below (verified at a 300px-tall window: opens upward, fully on screen). **This is the FOURTH time this wrapper has caught something** — the timeline Dates popout (v1.5.0), the assignee list (v1.6.0), the task-search dropdown (v1.1.2) and now this. **Anchored panel in this app ⇒ `fixed` + measured + flipped. There is no case where `absolute` is right.**
  ⚠️ **`e.currentTarget` read INSIDE a `setState` updater is null.** React clears it when the handler returns and the updater runs later, so `addPopoverPos(e.currentTarget)` threw on every click. Capture the element in the handler body first. Same family as the v1.2.0 drag bug that committed from inside a `setDrag` updater — **updaters are not handlers; nothing about the event survives into them.**
  **Second, independent bug the fix exposed:** the stacked `LogTimeForm`'s control row needs ~388px and the panel gave it 262, so the DATE was cut mid-value (visible in Nitsan's screenshot as "09/08/20"). ⚠️ **That row's width is not a constant — the member `<select>` is sized by the LONGEST NAME IN THE STUDIO**, so hiring can break it. The row is now `flex-wrap` (it degrades instead of overflowing, and this helps every one of the six surfaces sharing that form), and the popover went **w-72 → w-[27rem] (288 → 432px)** at Nitsan's request, which is wide enough that the row fits on one line and the description gets the full 406px.
  Gates: tsc clean, eslint 0 errors / 33 warnings, 42 tests, production build clean.
- 2026-08-09 — **v1.9.3 — the About panel becomes legible: the (i) tooltip stops spilling out of its own card, the greys pass contrast, and the (i) glyph is drawn rather than typeset.** No migration. All from Nitsan reading the shipped panel.
  ⚠️ **The tooltip's text ran 1,277px out of the side of its 240px card.** Not a width or a position bug — **inherited `white-space`**. The two dots by the donut sit inside legend captions carrying `whitespace-nowrap` (added in v1.9.0 to stop the captions wrapping), so the card kept its box while the sentence refused to wrap. `InfoDot` was already re-stating `normal-case`/`tracking-normal`/`leading-relaxed` for exactly this reason — **a card of prose has to re-state every text property its host may have set for a LABEL** — and `whitespace-normal` was simply missing from that list.
  **`text-faint` is 2.62:1 on white and the panel used it at 10–11px.** Every instance became `text-muted` (**5.92:1**) — there is now no `text-faint` left in the panel. Same pass: the blue hero's secondary text was `white/70` (**4.11:1**) → `white/85` (5.86:1), and the (i) dots were `opacity-60`, compositing to ~2.5:1 on a 14px target, under the 3:1 floor for a control → `opacity-80`. **Checked under `night` too**, where `faint` and `muted` INVERT (faint is the darker one there): the change helps rather than hurts, 3.06:1 → 7.12:1.
  ⚠️ **The circled "i" is now a lucide `Info`, not a bordered circle with a letter in it.** `leading-none` makes the line box shorter than the font's own content box, so the ascent overflowed upward and the glyph sat **1px high**, touching the ring (measured: 1.5px clearance above, 2.5px below). Any pixel nudge fixing that would be **a fact about Rubik**, and this app falls back to Rubik only until the real Saans arrives — at which point the nudge would be wrong again. **An icon centres by geometry**; verified 0px vertical asymmetry across all 18 dots. The Timeline's hint (i) in `client-view.tsx` was already an `Info` icon and only needed the `text-faint` → `text-muted` fix; those two are the ONLY (i) affordances in the app.
  **Also:** the rail's milestone dates went 11px/380 → 13px/570. Note that `font-semibold` here resolves to the SAME 570 as `font-medium`/`font-bold` (globals.css collapses all three), so **size is the other half of the hierarchy, not decoration** — the same lesson as v1.7.0. Tightest same-side gap between rendered captions is 21px, nothing runs off the card, and the panel still fits with no scroller at 1440×900 and 1280×720 (612px, unchanged). The hero's "years covered · 2016–2026" dropped its range — "11 years" already says it.
  Gates: tsc clean, eslint 0 errors / 33 warnings, 42 tests, production build clean.
- 2026-08-09 — **v1.9.2 — the public client Gantt (`/gantt/[token]`) uses the screen it is given.** No migration. Nitsan reported it "not responsive" with a 27" screenshot: the card sat capped in the middle of an empty window while the chart scrolled inside it.
  ⚠️ **`min-h-screen` is not a height, and that is why the first fix didn't take.** Removing the card's `max-h-[min(78vh,760px)]` and giving it `flex-1` made it grow to its own CONTENT (1700px of Anchor rows) and the PAGE scrolled — exactly what the card's own scroller exists to prevent. A flex child only caps against a DEFINITE height, so `main` is **`h-dvh`** (`dvh`, so a phone's collapsing URL bar doesn't leave a dead strip) and the card is `flex min-h-0 flex-1`. `min-h-0` is the half that lets it shrink below its content.
  **The width cap is deliberately 2200px, not the app's 1500px.** Every other page is reading-width text and tables; this one is a single chart that is ALWAYS wider than the window (91 days × 26px at day zoom = 2.4k), so every pixel is another day the client sees without scrolling — capped at all only so it doesn't stretch on an ultrawide. Measured at 2560×1400: **69 days visible, up from 43**, card 1242px instead of 760, no document scroll on either axis.
  **It was also genuinely broken on a phone**, which matters because this is the link CLIENTS open. At 375px the header's three grid tracks collided — the client's own name squeezed to nothing behind the zoom control with the wordmark on top of both — so below `sm` it wraps to two rows (name, then zoom and wordmark at either end). The pinned column narrows 316 → 206px (`useStickyWidth`, server-rendered wide and corrected on mount) and a narrow screen **opens on week zoom**, because day zoom left ~40px of calendar, about a day and a half, which reads as an empty plan. On MOUNT only — rotating the device must not overrule a zoom the reader picked. **That pinned width is the ONLY responsive dimension**: the bars stay at a fixed px-per-day so the chart reads the same on every screen and scrolls instead.
  ⚠️ **Method note: the browser tool's `resize_window` does NOT dispatch a `resize` event**, so the pinned column appeared not to respond until a hand-fired `new Event("resize")` proved the listener works. Don't conclude a resize listener is broken from that tool alone.
  Gates: tsc clean, eslint 0 errors / 33 warnings (+1: the mount effect reading `innerWidth`, same advisory shape as the existing ones), 42 tests, production build clean.
- 2026-08-07 — **v1.9.1 — right-click a task to insert one above or below it, a "Show undated" toggle on the Timeline, and the Timeline hint tooltip stops painting under the table.** No migration.
  **New store method `addTaskNear(anchorId, where, title, opts)`.** `addTask` always appends, which is the whole reason this exists. It slots the new row into **both** orderings — `position` (client table, per section) and `timeline_position` (Timeline, per client) — so it lands beside its anchor whichever view created it, rather than adjacent in one and at the bottom of the other. ⚠️ It **densifies** the section rather than opening one gap: every task the imports created has `position = 0`, so a section can be entirely zeros and there is no gap to open — renumbering 1..n is what makes the placement mean anything (same reason `reorderTask` does it). Like `addTask` it is deliberately **not** undoable; nothing that creates a task is.
  ⚠️ **The Timeline insert passes `copyDates`, seeding start/due from the anchor.** That view only draws tasks that HAVE a due date, so a dateless insert would vanish the instant it was created and read as a failed command. The client table doesn't need it — it lists undated tasks fine.
  **Both views open an inline composer rather than creating a row named "New task".** Changing your mind costs an Escape instead of a delete, and delete here cascades to time entries. ⚠️ **Enter is handled explicitly, not by implicit form submission** — the input sits inside a wrapper div and the form has no submit button, and Enter silently did nothing until the `onKeyDown` was added. The pre-existing `AddTaskRow` has the same shape; if it ever misbehaves, this is why.
  **Gating follows each view's own rules, which differ:** the client table's menu is open to everyone, matching its "Add task…" row, which was never admin-gated; the Timeline's is admin-only, matching every other editable thing in that view.
  **"Show undated" toggle (Timeline only).** Lists the tasks with no dates at all as rows with **no bar**, so they can be scheduled from the Dates cell instead of being invisible with a footnote. `Row` gained `undated`; an undated row borrows today's date so the row shape stays valid and **every drawing branch checks `row.undated`, never the dates**. Four places needed that guard and one was missed first: a task with a start but no due still printed a real duration ("1 day") because the read-only fallback tested `row.hasStart` alone. The Tasks tab doesn't get the toggle — it already lists undated work.
  ⚠️ **The Timeline's (i) hint card was painting UNDER the table.** It was `absolute z-40` inside the client page's header bar, which is `sticky top-14 z-10` — **a positioned ancestor with a z-index is a stacking context, so nothing inside it can rise above the Timeline's own `z-20` sticky header**, no matter how high its z-index. Fixed by portalling to `document.body` and positioning `fixed`, exactly as `HoverTip` and the weekly plan's search dropdown (v1.1.0) already do. **This is the third time this bug has appeared in this codebase — check for a positioned ancestor before raising a z-index.**
  Verified in the browser with Nitsan signed in, on Anchor: menu opens in both views; a Timeline insert landed directly after "Branding" with its 9–18 Aug dates copied, and appeared right after Branding in the client table too, proving both axes slotted; a client-table insert above "Home page - design" landed first in the section. **Both test tasks were deleted afterwards and the client verified clean (0 remaining).** Gates: tsc clean, eslint 0 errors / 32 warnings, 42 tests, production build clean.
- 2026-08-07 — **v1.9.0 — an About panel behind the sidebar version number: what the tracker holds, and what it cost to build.** No migration.
  **The panel is a POSTER, not a dashboard.** It fits on one screen with **no scroller** — that is a design constraint, not an accident, and it is what the 13 (i) tooltips are for: prose that would sit under a section lives in the dot instead. If you add a figure, take one out; don't drop the type below 11px to make room (the first draft did, and 10px is smaller than anything else in the app).
  **Every number is GENERATED — `node scripts/build-about-stats.mjs --apply`** rewrites `src/lib/about-data.json` from three places at once: the studio figures from Supabase, the build figures from `git`, and the effort figures from the local Claude Code transcripts. Nothing is typed in by hand, so nothing silently rots. Opening the panel costs one import and **no queries** — the JSON is a build artefact. ⚠️ The transcripts are machine-local: on any other machine the script keeps the existing `effort` block rather than zeroing it.
  **Three bands:** a blue hero (87,577 hours · 11 years · 33 days, plus the 7 retired tools as pills), a **full-bleed tinted timeline**, then the white body. The timeline is a rail with **one tick per release at its true date** — 49 of them, and 27 July's 13 show as a visible cluster, which is the honest picture of how this was built (bursts, not a drip). 8 editorial milestones alternate above/below so neighbours can't collide; the first and last anchor to their edge, because a centred caption at 0% hangs half its width off the card.
  **Two split bars beside a donut.** The bars are the two facts that are both divisions — 42,953h recovered vs 44,624h logged, and 36h Claude vs 21h Nitsan. The models are a **donut, not a third bar**: three shares of ONE whole, where three stacked bars each totalling 100% would invite comparing rows that measure different things. `$5,868` sits in the hole.
  ⚠️ **The cost figures are two different claims and the panel says so.** `effort.costUsd` is a **list-price valuation** of the compute (per-model tokens × current API rates, with the **2× 1-hour cache-write multiplier read from the `ephemeral_1h` usage field rather than assumed**). `ACTUAL_SPEND` in `src/lib/about.ts` is the **bill actually paid** — one $25 seat, apportioned to the build's real span, so it stays true as the project runs. ~$27 against $5,868 is ~214×, and the gap is prompt caching: cached reads bill at a tenth of normal.
  ⚠️ **`className="p-0"` on the shared `Modal` SILENTLY DOES NOTHING.** Tailwind orders `p-4` after `p-0` in the stylesheet, so class order in the string never decides it — the card kept a 17px white frame for several rounds. It needs **`p-0!`**. Worth knowing for any modal that wants to bleed to its own edges.
  **Three fixes to shared components, all additive:** `Modal` gained `3xl`/`4xl`/`5xl` widths and now focuses with **`preventScroll: true`** (a card taller than the viewport made the browser scroll to reveal it, and on a COLD open — web fonts still loading — the content is briefly taller, so the panel opened part-way down and stayed there). `InfoDot` gained **`side="up"`**, because a tooltip on the bottom row opening downward is clipped, and an explanation you can't finish reading is worse than none.
  ⚠️ **Two colour pairs that passed in `electric` failed in `night`**: `bg-brand-soft` + `text-brand-dark` landed at **1.5:1** (brand-soft flips to a dark navy), and `bg-aqua` + `text-foreground` at **1.2:1** (foreground goes near-white while aqua stays bright). The rule both fixes follow: **a TINT of brand + the foreground token move together and work in both themes; a FIXED accent like aqua needs a FIXED ink** (`text-[#06112f]`). Lowest ratio is now 6.9:1. The same reasoning is why the timeline band is `bg-brand/[0.055]` rather than `bg-brand-soft`.
  Verified in the browser with Nitsan signed in at 1440×900, 1280×800 and 1280×720: 612px card, fits every one, no tooltip clips off-screen, all 8 stat labels unclipped, and the donut's amount clears its hole by 10.6px (it cleared by **0.2px** at the first size — fine on the Rubik fallback, an overflow the moment the real Saans lands). Gates: tsc clean, eslint 0 errors / 30 warnings, 42 tests, production build clean.
- 2026-08-07 — **v1.8.0 (DEPLOYED + committed `baad3e9`, pushed to main; prod verified serving v1.8.0, `/gantt/<token>` 200 and a bad token 404) — a shareable, LIVE Gantt link for clients.** Migration 0025 (`gantt_links`) APPLIED by Nitsan before the deploy. (`gantt_links`) Before it was applied `/gantt/[token]` 404'd cleanly (the query errors, `data` is null, `notFound()` fires) rather than 500ing — worth knowing for any future table-backed public route.
  **Also in this release:** "Show completed" became a toggle button, Share sits right of the pencil in the header (the cluster swaps per tab — report actions on Tasks/Overview, the schedule link on Timeline), and the public page got a black client mark (`filter: brightness(0)` — these marks are drawn WHITE for the coloured tile they sit on in the app, so on a page with no tile they were white on white), foldable sections, no Dates column, a centred zoom, the studio wordmark, and a Budget column. ⚠️ **`main` on a public page needs `w-full min-w-0`**: `body` is `display: flex`, so it is a flex item whose automatic minimum size is its MIN-CONTENT — the 4000px chart. Without it the page grew to 1500px on a 1180px screen and scrolled as a document.
  **Why its own table and not a second path on `report_links`:** the two must be revocable independently, and sharing a schedule must not hand out the hours. A report is a PUBLISHED, FROZEN statement of money — `report_links.snapshot` exists precisely so a client never sees live numbers. A Gantt is the opposite: a plan is only worth reading if it is the current one, so `/gantt/[token]` is `force-dynamic` and re-reads every request.
  ⚠️ **The narrowing happens in the `select`, not in the view.** LOGGED hours, assignees and status never leave the database; `status` is read ONLY to exclude completed work. The BUDGET is shown (column + tooltip) — it is the scope the client agreed to and says nothing about how much of it has been spent, which is the thing they must not see. Nothing to leak through a prop, a serialised payload, or a later edit to the component. Same service-role, no-anon-policy pattern as `/report/[token]`.
  **`src/lib/gantt.ts` is new**: every pure date/geometry helper (`parseISO`, `shiftDays`, `daysBetween`, `toISO`, `dateRangeLabel`, the working-day calendar, `ticksFor`, and the bar/row constants) moved out of `client-timeline.tsx`, which now imports them. There are two charts drawing the same picture and a client reading a published plan has to see the same bars on the same days — copying the helpers would have been an invitation for the two to drift a day apart.
  **The Timeline tab drops the Client-report buttons** (the report is about hours; that tab is about dates) and gains **Share**, which copies the link. The URL is prefetched on mount so `clipboard.writeText` stays synchronous — after an `await`, Safari has already lost the user-gesture that permits it.
- 2026-08-07 — **v1.7.0 (DEPLOYED + committed `975985e`, pushed to main; prod verified serving v1.7.0 on tracker.studionmore.com) — a second round on the same batch: the Timeline loses a row of chrome, gains instant tooltips and named bars, and the type hierarchy is fixed system-wide.** No migration.
  ⚠️ **globals.css collapses `.font-medium`, `.font-semibold` AND `.font-bold` onto ONE weight (570).** Saans is used at 380 and 570, nothing else. So a "make this heavier than that" request cannot be answered by changing the class — both the Timeline's and the client table's section headers were rendering at exactly the same size AND weight as their own task rows. The fix in both places is to move the ROW down to the body weight (380) and leave the heading at 570, plus one step of size (12→14 in the Timeline, 14→16 in the table). **Check the computed weight before trusting a font class in this app.**
  **Chrome removed.** The how-to line above the chart became an (i) beside the client name (a real `<button>`, opening on hover AND focus, no `title` delay). The legend and Columns button moved onto the tab strip — by `createPortal` into a slot ClientView hands down, so `hiddenCols` and the used-type set stay in ClientTimeline. The tab strip became a **grid** in the process: the zoom control was `absolute left-1/2`, out of flow, so the legend slid straight under it at every width. Three tracks with `auto` in the middle keeps it centred and gives the legend a box of its own. Legend labels are cut to 10 characters with the full name in `title`.
  **Bars carry their task's name**, truncated by the bar, dark on the tint and white once the fill passes 55%; `BAR_H` 16 → 20 to fit the line, track alpha 0x3d → 0x52 so the bar doesn't vanish under the text.
  **The bar tooltip has three bands**, not five identical lines: a heading washed in a weak tint of the task's own type colour (name + type, no dot — the wash says it), then label/value fact rows, then the drag hint smaller and fainter. No dividers: the tint already ends where the facts begin. `HoverTip` takes NODES rather than a `\n`-joined string, which is what made the flat version inevitable.
  ⚠️ **`EditableTextCell` starts in its RESTING state, so a pencil used to hand you a second "Click to edit" target** for the intent you had just declared — true in the Timeline's two pencils as well as the client table's new one. It now takes `startEditing` (mount into the input) and `onExit` (fired on commit AND on Escape — without it, Escape left the caller's `renaming` flag stuck true and the editor undismissable). All three call sites pass both.
  **Section renaming is the pencil's job in the client table too**, next to the title rather than flung to the far right: the name truncates inside a `flex-1` box that also holds the count and the pencil, instead of being `flex-1` itself and eating the row's spare width.
  **Tooltips are instant.** The bars' `title` attributes are gone — the browser sits on one for about a second, long enough that you click the bar instead, which is a write. `HoverTip` is `fixed` and portalled to `document.body`, positioned from the pointer and flipped near an edge, for the usual reason: the chart's scroller clips both axes.
  **Sticky shadows.** One full-height gradient sticking at the name column's right edge, NOT a box-shadow per row — per-row, every row's bottom border cut across it and it came out as a column of smudges. The header keeps a box-shadow because it is a single element. Both appear only when something is actually behind them, and both are deliberately faint.
  **The month band is gone from the Timeline header** — 25px on every visit to print six words. The first tick INSIDE a month prints the month's name in place of its date, in the emphasis weight, with a rule down its left edge that GridLayer continues through the rows. The year is stated once, where it changes, and only where it fits (a day-zoom tick is 26px). Three grid weights now: month `foreground/15`, week `foreground/7%` (the Sat→Sun boundary at day zoom), day `border/40`. ⚠️ **`border-strong` is not usable for a divider** — it computes to oklab lightness 0.87, indistinguishable from the weekly rules at 0.93/40%. Default zoom is now **day**.
  **The legend folds to a stack** of overlapping swatches and unfolds on hover (`max-w` transition, the usual trick for animating from `auto`). Because it can't grow at rest it can never reach the centred zoom control, which is what the `lg:` breakpoint had been papering over. Its rings are `ring-background`, NOT `ring-surface`: this toolbar sits on the page, so #fff read as a bright stroke against #f0f1fa.
  **The (i) sits on the Timeline tab itself**, via a new `after` slot on `TabItem` — it can't go in the label, because a button cannot contain a button.
  **Client Overview** is now `lg:grid-cols-[7fr_3fr]` — notes and links take the width, the figures are a 30% sidebar.
  ⚠️ **Column resize: never make one column of a resizable table `flex-1`.** The client table's Name cell was `flex-1` with its stored width as `minWidth`, and that produced BOTH of Nitsan's complaints at once. Total row width was fixed, so dragging any other column's handle right took the width out of Name — the handle stayed under the cursor and the column grew to the LEFT, which reads as backwards. And dragging Name's own handle did nothing, because it moved a minimum that sat far below the width flex had already given it. Name is now a fixed 320px like every other column (clamped to a 160px floor, because a drag that "did nothing" still WROTE a width to `colw.client-tasks` and it would otherwise arrive crushed), and the table scrolls.
- 2026-08-07 — **v1.6.1 — the Timeline gets a Type column, a hover affordance for opening a task, and section bars that look like summary brackets instead of work.** No migration. Browser-checked on Anchor with Nitsan signed in. (Superseded by v1.7.0 before it shipped; the changes below are in production as part of it.)
  **Type column.** `TL_COLS` gains `type` (96px, between Who and Dates), so the thing that decides a bar's colour finally has a per-row answer — until now the only key was the legend on the right, and you counted swatches. `EditableSelectCell` with all eight studio types; 91px of usable control inside the 96px cell, unlike the 44px assignee `<select>` that had to become a real dropdown. Admin-gated like every other editor in this table, even though 0024 leaves `type_id` member-writable — members still set a type from the client table.
  **Open-details icon.** A hover-only `Maximize2` in the name block, beside the pencil. The name already opened the task, but the only signal was a hover underline; on a Gantt where every other click drags something, that isn't enough. Verified: opacity 0 across all 28 rows at rest, 1 on the hovered row, and clicking it opens the pane.
  **Section bars are brackets now.** `SECTION_BAR_H = round(BAR_H * 0.3)` = 5px, radius 1, with a 6×9px downward tip at each end (the CSS border-triangle: a 0×0 box whose coloured top border mitres into a transparent side border). Tips are suppressed under `TIP_MIN_W` or the two would meet and the span would read as a chevron. Built from Nitsan's description — the reference image wasn't in context — so the shape is the standard Gantt summary bracket.
  ⚠️ **The bug this uncovered: `SectionHeaderRow` was pinning `leftW`, not `STICKY_W`.** v1.6.0 changed the task rows to stick only grip+checkbox+name (238px) but left the section label sticky across the whole 666px left table. Scroll the chart sideways and that opaque band sat on top of the first 666px of calendar and swallowed the summary bar — present in the DOM, correctly positioned, invisible for most of the scroll range. The label now pins `STICKY_W` and the rest follows as a plain filler that scrolls away with the columns it belongs to. **When the left table's sticky rule changes, change it in BOTH row types.**
- 2026-08-06 — **v1.6.0 (DEPLOYED + committed `3a5fb01`, pushed to main; prod verified serving v1.6.0 on tracker.studionmore.com) — the Timeline becomes editable end to end, the tables get columns you can choose, and the sidebar folds.** No migration. Two rounds of Nitsan's feedback in one session, all browser-checked with him signed in.
  **⚠️ The date-cell bug and its whole family.** `EditableDateCell` closed the editor `onBlur`, and the browser's calendar is SEPARATE CHROME: clicking its month name or ‹ › arrows blurs the input, and unmounting the input took the picker down with it. There is now **no blur handler** — it closes on Escape, on a committed change, or on a pointerdown elsewhere in the DOCUMENT, and clicks inside the native picker never reach the document, which is exactly why that test works where blur did not. Clicking a cell no longer auto-opens the picker either (you land in typing); the calendar is a button beside the field. The editor is also a POPOUT rather than the cell: a date input needs ~120px and these cells are 72px, which is what made pressing one look broken.
  ⚠️ **Anchored panels here must be `fixed` and must flip.** Three of them (timeline Dates, the assignee list, the client-table date popout) live inside `overflow` scrollers that clip BOTH axes, so `absolute` is cut off on the last rows; and `fixed` alone still runs off the bottom of the window, which is how the Dates panel's Done button became unreachable. Each measures its anchor and flips above it when the window runs out. The pane's Dates popover is anchored `right-0` instead, because it sits in the right column of the two-column grid.
  **Timeline table:** only the NAME block sticks (grip + checkbox + name, 238px) — pinning the whole 566px table spent half a laptop's width on columns you weren't reading. Admins edit name (pencil; the name itself still opens the task), assignee (a real list with faces, not a 44px `<select>`), budget, section name, and **Duration — which moves the DUE date only, and only where the task has a start**; a deadline has no duration to change and inventing a start is a different decision. The completion circle became a **selection checkbox** with shift-ranges and a batch bar. Zoom moved to the tab strip (centred), Show completed to the left of Client report, hint left / legend right, and a **Columns** menu per table (persisted, validated on read).
  **Bars redrawn to Nitsan's reference:** one 16px shape, `borderRadius: 4` (~1:4 of the height — a full pill stopped short bars reading as spans), the colour at 24% as the track, hours-against-budget filling it solid, `overflow-hidden` squaring the fill's inner edge. The old 2px completion rule is GONE — it read as a hairline floating above the bar and said what the fill already says. Hours moved outside the bar, to its right. **A live date chip** appears the instant a drag starts and updates per day, beside the edge being dragged (verified: 18 Aug → 21 Aug at +3 → back, nothing written when the delta returns to 0).
  **Elsewhere:** Start/Type columns on the client table + a resizable name column; batch set of status/type/start/due; table dates lose the year (`formatDayMonth` — `formatDate` keeps it for anything historical); number cells are typed fields, no spinner arrows; the pane's Due row became **Dates** showing "9/8 – 18/8"; an EMPTY date seeds from the other end so the picker opens on the right month (a starting point, never a saved value); links get a trash icon and a **+ Add link** on the Brief heading line; the sidebar folds to a 64px icon rail with no-delay tooltips, the ampersand centred on the rail and `«`/`»` pinned right.
- 2026-08-06 — **v1.5.2 (DEPLOYED + committed `4476e15`, pushed to main; prod verified serving v1.5.2 on tracker.studionmore.com) — the mark becomes editable from Settings, and any colour is allowed.** No migration. Three asks from Nitsan in one turn: a bigger mark on the client page (28 → **40px** against the 24px title — on their own page the mark leads rather than annotates; the sticky bar still pins flush, checked on Studio at 287 tasks), logo edit/remove from Settings, and a hex field in the client-record popup.
  **`ClientMarkPicker` moved out of `client-info-modal.tsx` into its own [client-mark-picker.tsx](src/components/client-mark-picker.tsx)**, because two surfaces now need the whole thing. A second copy would drift on the three rules that have to agree: the palette, the `/api/client-icon` route (where the admin check and the Content-Type allowlist live — a browser-side upload with the anon key would have neither), and "an uploaded image wins over the glyph". The file also exports `ClientMarkModal` for callers that aren't already a client form.
  **Settings → Clients: the mark IS the button.** Clicking it opens the picker over the list — a 25-glyph grid plus an upload has no place inline in a list row. ⚠️ **The native colour swatch was REMOVED from that row on purpose**: it sat immediately beside the new mark and two coloured squares in a row read as two different things. The inline hex field stays for quick edits; the colour wheel moved into the popup. Don't "restore" the swatch.
  **Any hex, not just the twelve** — a colour wheel + a text field in the picker, so it reaches the pencil modal and the Settings popup at once. ⚠️ **The field commits only a valid `#rrggbb` and snaps back otherwise**: a half-typed `#0b4` written to the row paints that client's chips transparent everywhere. Both paths tested on Anchor (`#0b4` → snapped back, no write; `#123456` → committed and repainted the row, mark and preview live) and **reverted to `#ef0bd0`**.
- 2026-08-06 — **v1.5.1 (DEPLOYED + committed `1c6203d`, pushed to main; prod verified serving v1.5.1 on tracker.studionmore.com) — the client mark actually shows up.** No migration. Nitsan reported that the icons and uploads added in v1.3.0 still rendered as letters on the clients list. Cause: [clients/page.tsx](src/app/(app)/clients/page.tsx) drew its own 32px square from `client.name[0]` on `client.color` — it predates `ClientAvatar` and was never switched over, so `icon`/`icon_url` had nowhere to render. Replaced with `<ClientAvatar size={32}>`.
  **Then the same mark replaced the colour dot inside `ClientChip`** (16px at `sm`, 18px at `md`, both round), which reaches all 12 chip sites at once — feed, reports, my-tasks, task pane, dashboard, autocomplete, member page, day details. `ClientAvatar` gained `radius: "lg" | "full"` for it: `lg` is the standalone tile, `full` the mark inside a pill. Two competing radii — an 18px squircle inside a fully-round capsule — was the variant that looked wrong in the preview, so the pill always gets a circle.
  ⚠️ **The mark sits FLUSH to the pill's left edge (`pl-0.5`), not inset like the old dot** — that is what keeps the capsule at its previous height, measured at 22px (`sm`) and 26px (`md`) both before and after, so no feed row, table row or header grew. If a later change re-adds symmetric padding, every chip gets taller and the rows follow. Width grows 4px at `md`; nothing lays out against chip width.
  **Left as dots, deliberately:** global search, the settings client list, client-reports tabs, the weekly-plan filter — all 8–10px, where a glyph is unreadable and the colour is doing the work. An uploaded *logo* is also weak at 16px (Studio's `&` survives because it's one bold glyph; a wordmark wouldn't) — the preset glyphs are what this size is for.
  *Method note: the variants were shown to Nitsan as a throwaway `/chip-preview` route rendering real clients in real styles rather than as a mockup — cheap, honest about the fallbacks, and deleted once the pick was made.*
- 2026-08-06 — **v1.5.0 (DEPLOYED + committed `fe5beef`, pushed to main; prod verified serving v1.5.0 on tracker.studionmore.com) — the Timeline becomes a real project table, and "Tags" are renamed "Status" in the UI.** Migrations 0023 + 0024 were applied by Nitsan before this. ✅ **This one commit carries v1.2.0 → v1.5.0** — all four were built in a single session and none shipped separately; every one was checked in the browser with Nitsan signed in.
  **Timeline rebuilt to the Asana Gantt reference**, in our own tokens: a left TABLE (grip · completion circle · name · assignee avatar · **Dates** · **Duration** · **Actual** · **Budget**) beside the chart, **grouped by the client's sections** with a collapsible header per group and a slim summary bar in the client's colour spanning that group's whole range. Month names sit above the tick row; the today line gained the reference's dot.
  **A task with no start date is now a DIAMOND, not a one-day bar.** That's the substantive change, not decoration: "we owe this on the 20th" and "this takes one day" are different claims, and the old single-day sliver made them identical. Duration reads "—" for those, and dragging the diamond's edge is still how it becomes a scheduled span. The legend says so.
  **Planned vs spent in one bar.** A **thin 2px rule** along the top spans the scheduled dates and its strength tracks completion (opacity 0.28 → 1 across the budget); the band beneath fills with logged hours against that budget. So a column of rules reads as "which of these is actually moving" without comparing fill widths. Over budget overrides both to red. *(First built with a diagonal hatch for the plan — Nitsan found it too loud, and he was right: a texture that busy competes with the bars it's describing.)*
  **The Dates cell is click-to-edit.** Dragging is good for "a bit later, a bit longer" and hopeless for "the 14th", so the cell opens a popover with both ends and a "Clear start" (back to a deadline), committed as ONE `updateTask` so it stays one undo step alongside the drags. ⚠️ It renders `fixed`, positioned from the cell's rect: the chart sits in an `overflow-x-auto` scroller and **a scroll container clips BOTH axes**, so an absolutely-positioned popover would be cut off at the row's bottom edge — the same trap as the task-search dropdown in v1.1.2. A typed date is deliberately **not** snapped to a working day (naming the 14th is explicit in a way nudging a bar isn't); a non-working choice is accepted and labelled instead.
  ⚠️ **Drag-reorder is now confined to one section.** Dropping across a boundary is refused rather than re-sectioning the task — `timeline_position` exists precisely so the Timeline's order can't reach into the Tasks tab's grouping (see 0023), and allowing cross-section drops would have undone that. Move a task between sections from the task pane.
  **Verified live on Anchor** (0023/0024 applied): bar 144px = 16 calendar days at 9px/day for 18 Aug→2 Sep; the deadline diamond sits exactly 6 days right of it, matching 18 Aug → 24 Aug; collapsing a section takes its bars from 2 → 0 → 2; setting a Type drove it into the tooltip, the bar colour and the legend; the top rule measures 2px at opacity 0.28 with zero `repeating-linear-gradient` left anywhere; and the Dates popover opens `fixed`, unclipped, with Start empty and Due 2026-08-23.
  ⚠️ **Method note — scripted `<select>` changes are not reliable against React.** Setting `.value` via the native setter plus a synthetic `change` event reported the right option locally but left the WRONG type persisted twice (a stray "Wireframe", then "Content" on a different task), each found only by reading the row tooltips back after a full reload. Verify any scripted write by reloading and re-reading, or drive the real control. All of this session's test content on Anchor — start dates, brief, client link, stray types — was reverted and confirmed clean after a hard reload.
  **"Tags" → "Status" in the UI.** They always were statuses — "in design", "Client approval", "Approved". Renamed on the task pane, the client table header + sort hint, the My Tasks/home column, and Settings ("Task statuses", now with a line distinguishing it from Task types). ⚠️ **The code still says `tag`/`tags`/`tag_id`** — renaming the table, the column and ~40 references is a large, risky diff for a label change, so the mapping is documented at the top of the `Tag` interface instead. Two collisions worth knowing: `Task.status` (todo/in_progress/done) is a DIFFERENT thing that the UI never calls "status" (it's the completion tick and the board columns), and the client-record modal's "Status" row was renamed "Archive" to free the word.
  Gates: `tsc` clean, `eslint src` 0 errors / 24 warnings, 42 tests, production build clean.
- 2026-08-06 — **v1.4.0 — task types colour the Timeline, the client edit button shrinks to a pencil, notes move where members can read them, and the pane gets a real text hierarchy. MIGRATIONS 0023 + 0024 REQUIRED.**
  **Task types (0024).** Nitsan asked to colour Timeline bars "by type" and named seven: development, QA, design, wireframe, content, image making, client side. Those are KINDS OF WORK; the studio's existing tags are workflow STATES ("in design", "Client approval", "Development", "Approved"). Presented as three options, he chose **a separate Type field** — so a task can be a QA job AND awaiting approval, which one shared list makes impossible. New `task_types` (seeded in the migration, re-runnable via `where not exists`) + `tasks.type_id`, a Type row on the task pane above Tag, an admin editor in **Settings → Studio setup** beside Task tags (colour swatch, inline rename, in-use count, delete that says how many tasks lose the label), and bars drawn in the type's colour: base at ~18% alpha, fill at full strength. **Over budget still overrides to red** — the money reading has to win over the category. A legend lists only the types actually on that client's board; naming seven when two are in use is a key to a map you aren't looking at. `type_id` is deliberately NOT in the 0011 protected list: describing the work is collaborative like `tag_id`, unlike scheduling it.
  **Client info → an icon-only pencil, record only.** The button now opens name / billing note / mark / archive and nothing else, and it's **admin-only** — there was nothing in it for a member to press. **Notes and links moved to the Overview tab** (`client-notes.tsx`), which is where they belonged: they're studio-wide reading, and hiding them behind an admin button hid the one part of that panel that wasn't admin work. Admins edit in place; notes commit on blur (no modal close to hang a save on), and an effect adopts an incoming value on refresh without clobbering half-typed text.
  **Text hierarchy, per the Asana reference.** Every section label was `text-xs uppercase tracking-wide text-faint` — 12px micro-caps at the LOWEST contrast on the page — so "Brief", "Attachments" and "Discussion" (the pane's structure) sat in the same register as a file size, and nothing announced where one part of the task ended and the next began. Now three levels and only three, named as constants (`SECTION_HEADING`, `FIELD_LABEL`): **title 22px/570**, **section heading 14px/570 at full `#06112f`** in sentence case, **field label 13px/380 muted `rgb(92,100,120)`** with its value at foreground. Measured in the browser, not eyeballed. "Time — 0m total" split so the heading is "Time" and the total rides beside it at incidental weight.
  Gates: `tsc` clean, `eslint src` 0 errors / 24 warnings (+1: ClientNotes' adopt-incoming-value effect, same advisory shape as the existing ones), 42 tests, production build clean.
- 2026-08-06 — **v1.3.0 — the pane stops shouting, the discussion becomes a conversation, the Timeline learns the studio's working week, and a client gets a face. MIGRATION 0023 REQUIRED.**
  **Corrections to v1.2.0, at Nitsan's direction:** (1) **a task always opens in the side pane; maximising is an action, not a mode.** Full screen persisted to localStorage, so one click of maximise meant every task afterwards took over the screen — including tasks opened from the plan or the feed, where the whole point is keeping the thing behind it in view. Now reset on each open. (2) **Admins can delete a message** in the discussion. Undo re-inserts the row with its ORIGINAL id AND `created_at`, so a restored comment lands back in its place in a time-ordered thread instead of jumping to the bottom; `author_name` rides along too, since 2,175 of the 2,397 imported comments have no profile and it's the only record of who wrote them.
  **Quiet-until-touched.** Every value in the pane is editable and the UI said so with a permanent box around each one — fourteen outlined controls the eye has to discount before it can find the assignee. `QUIET_FIELD`/`QUIET_SELECT`/`META_ROW` + a `QuietSelect` wrapper: chrome appears on hover AND on `focus-within` (a control whose only affordance is hover is invisible to the keyboard). Native chrome that CSS can't reach is handled by hand — `appearance-none` for the select arrow, `[&::-webkit-calendar-picker-indicator]:opacity-0` for the date input's glyph. The Figma box (a full-width outline on every task that has no Figma file, i.e. most of them) and the attachments dropzone go quiet the same way. Measured on the Branding task: hovered row → chevron opacity 1, border `rgb(229,231,235)`; un-hovered rows → opacity 0, `rgba(0,0,0,0)`.
  **Discussion is its own surface** (the Asana reference). Everything above it is a form — label left, value right, on the pane's own white; the thread is the one part that's a conversation, so it sits in a recessed `bg-background` panel with a real `<h3>` + a count, and its bubbles flip to `bg-surface`. ⚠️ That flip is load-bearing: the old bubbles were `bg-background` ON a `bg-surface` pane, and once the panel behind them became `bg-background` too they would have vanished into it — only the border was doing any work. Timestamp and the (hover-only) delete control share one faint line, so the row costs nothing until you reach for it.
  **Timeline v2.** Actual and Budget hour columns sit between the task name and the chart; the row carries **only the assignee's avatar** (a name would halve the room for the title, and who it's on is the one person-fact a Gantt row needs); rows are **drag-reorderable** by a hover-only grip, persisted to `timeline_position`; a **weak** vertical grid (`border-border/40`, drawn once behind all rows rather than per row) lets you read a date off a bar without competing with it.
  **Working days are excluded from planning, not just shaded** (Nitsan's choice of the three options). Fri/Sat AND any day the weekly plan marks as a holiday (`plan_day_states`, expanded to a date set) are drawn darker and skipped by the arithmetic: `isWorkDay`/`snapToWorkDay`/`addWorkDays`/`workDaysBetween` mean a dragged date always lands on a working day, and a MOVE preserves the bar's length in WORKING days — a three-day job pushed onto a Thursday still gets three days of work and simply stretches across the weekend. Verified live: Branding read "16/8/26 → 31/8/26 · 12 working days", and a move produced "18/8/26 → 2/9/26 · **12 working days**". Shading verified by count: 13 Fridays + 13 Saturdays across the 91-day window. Per-day shading is dropped below 6px/day (at month zoom the stripes would be denser than the data on them).
  **Client identity.** `ClientAvatar` (new `client-avatar.tsx`) renders an uploaded mark, else one of 25 preset glyphs, else the initial — always on `clients.color`. Icons are stored **by name**, so the column survives an icon-library upgrade and an unknown name falls back to the initial rather than a blank square. It now sits before the `<h1>` on the client page and in the Client info header, and the modal gained a colour row + glyph grid + upload. The upload goes through a new **`/api/client-icon`** rather than straight to storage: that route holds the admin check and reuses `classifyImage`'s allowlist with a server-forced Content-Type. ⚠️ It writes `clients` with the SERVICE KEY, which bypasses RLS entirely — **the explicit `role === "admin"` check in `requireAdmin()` IS the access control there**, not a convenience. It reuses the existing `avatars` bucket under a `client/` prefix so no bucket has to be provisioned by hand. The store gained `patchClientLocal` for the same reason `patchProfileLocal` exists: the route already persisted the value, so calling `updateClient` would issue a redundant write and record an undo step for a change the store never made.
  Gates: `tsc` clean, `eslint src` 0 errors / 23 warnings, 42 tests, production build clean.
- 2026-08-06 — **v1.2.0 — the brief becomes editable, links get titles, a task gets a permalink and a full-screen pane, a client gets notes, and the client page gets a draggable Timeline. MIGRATION 0022 REQUIRED — Nitsan applied it before the deploy.** ✅ **Verified in the running app with Nitsan signed in** (he ran 0022 first, so every check below ran against the real schema).
  **The finding that started it:** Nitsan reported the brief field on an Anchor task was "closed to edit". It wasn't a permission bug and it wasn't Anchor — **the brief had never had an editor anywhere in the app.** `task-panel.tsx` rendered it as a static `<div>`, and the only thing that ever wrote `tasks.brief` was the public intake form (`assembleTaskBrief` → `api/intake/[token]`). A task created inside the app read "No brief yet." forever. The write path underneath was already complete (`taskPatchToRow` handles `brief`, and migration 0011 deliberately leaves it member-writable) — only the UI was missing.
  **Migration 0022** (`supabase/migrations/0022_links_notes_and_start_date.sql`): a `links` table (title + url, hung off EITHER a task or a client, `check ((task_id is null) <> (client_id is null))`), `clients.notes`, and `tasks.start_date`. ⚠️ **It also re-declares the 0011 trigger to add `start_date` to the protected list.** That trigger enumerates its columns by name, so a NEW task column is member-writable by DEFAULT — without the amendment any designer could re-schedule any task through the API and the Timeline's read-only gate would be UI-only. Link policies are split for the same reason briefs are member-writable: task links are collaborative material (`write task links`), client links sit beside the billing note and are `is_admin()`.
  **Degrades before the migration, on purpose.** The task select grew a new TOP rung (`client_id, start_date, legacyCols, cols`) rather than folding `start_date` into the existing one — collapsing rungs is how the `legacy` flag gets dropped, which is the mistake that ladder exists to prevent. `links` is fetched with `.catch(() => [])`. Observed live: pre-migration the browser logged 2×404 (no `links` table) and 4×400 (no `start_date`) and the app worked; post-migration a fetch-instrumented refresh logged **zero** failed requests.
  (1) **Brief editor in a modal, not inline.** A brief is paragraphs and the pane is a 576px column with the hours, the time list and the whole comment thread beneath it. `BriefModal` is `layer="raised"` (the pane is z-40/50). It **saves on close** — Escape and backdrop included — because losing a just-typed paragraph is worse than saving an edit someone meant to abandon, and Cmd-Z still undoes it. ⚠️ **It refuses to save until the brief has actually arrived.** The brief is lazily fetched by `loadTaskExtras`; `loadedTaskExtras` is a ref added BEFORE the fetch resolves, so it could not answer "has it landed". New `briefLoaded(taskId)` in the store, set after the detail query applies and deliberately monotonic (`mergeTasks` keeps the brief across refreshes). Without it, opening the editor a beat early and closing it would write `""` over real text.
  (2) **Titled links** (`links-editor.tsx`, shared by the task brief and the client panel). Only the TITLE renders — the studio's links are 200-character Google Doc URLs, and the Branding task's own comment thread has Michal pasting that exact URL twice as raw text, which is the whole argument. Because the title is all you see, `normalizeUrl` checks the scheme before storing (bare `docs.google.com/…` gets `https://`) and `isSafeUrl` checks again before rendering: a `javascript:` URL under a friendly title is a trap nobody could spot. An unsafe row renders struck-through and unclickable rather than being hidden, so an admin can see it.
  (3) **Copy task link + full screen** on the pane's top-right. The link is `/?task=<id>` — the home route, because `/clients` is admin-only and a permalink a designer can't open isn't one. Deep-linking reads `window.location` in a mount effect rather than `useSearchParams`, which would drag a Suspense boundary into the shell for a one-shot read. Clipboard failure (Safari, insecure origin) falls back to a prompt rather than flashing "Copied!" over a clipboard that didn't change. Full screen is the same panel widened (`inset-0`) with the content column capped at `max-w-3xl` — a comment thread set across 1900px is unreadable — and remembered in `localStorage["task.fullscreen"]`, read in an effect.
  (4) **Client info panel** (`Client info`, top-right of the client page): notes and links for everyone, and for admins the client record — name, billing note, archive. **The standalone Archive button moved in here**; two archive buttons on one screen is one too many. **No Delete, by Nitsan's decision** — deleting a client cascades to its tasks, sections, time entries and report links, so those hours would leave every total with no way back. The panel says so.
  (5) **Timeline tab** — a Gantt of the client's dated work, admin-draggable and member-read-only. Only tasks WITH a due date appear (a bar has to end somewhere) and the count of what's excluded is printed underneath, so the view is never quietly partial: Anchor shows 2 bars and "26 open tasks with no due date aren't shown". A bar runs `start_date → due_date`; **no start = a single-day bar, and dragging its left edge is how a start gets set in the first place**. The fill is logged/budget with BudgetBar's own over-budget red. Weeks-across-3-months by default (Nitsan's pick) with Day/Month zoom; every position is computed in DAYS and multiplied by a per-zoom `PX_PER_DAY`, so one drag maths serves all three. Date arithmetic is DST-safe by construction (`new Date(y, m, d + n)`, never `+ 86_400_000`). Move and resize commit in ONE `updateTask`, so re-scheduling is one undo step.
  **Two bugs found by testing, both mine:** (a) the drag committed from inside a `setDrag` updater — updaters run during render, so it called the store's `setTasks` mid-render and React threw *"Cannot update a component while rendering a different component"*. Fixed with a `dragRef` so the commit happens outside any updater. (b) The 8px resize handles covered narrow bars entirely: at week zoom a one-day bar is 9px, so it could be resized but never moved. Handles now only render at `barWidth >= 30px`; narrower bars are move-only.
  **Verified live** (Anchor, admin): left-edge drag set a start date that survived a hard reload; a move drag changed both dates and the Tasks table showed the new due date (31/8/26), proving the timeline writes the same field the table reads; a link added in Client info persisted across reload and renders as "Client branding questionnaire"; a brief typed in the modal saved and rendered; `?task=<id>` opened the pane cold on the home page; and `?viewAs=Nadav` showed "Read-only — only admins can re-schedule", no drag handles, and a Client info panel with no client-record section. A fetch-instrumented drag + refresh window recorded **0 failed requests and 0 JS errors**.
  ⚠️ **Method note:** the browser tools' `get_page_text` and console buffer both served STALE content repeatedly during this session while screenshots were fresh — a "Loading the studio…" text read and a list of long-dead 400s both sent me chasing nothing. Trust a screenshot or a live `javascript_tool` probe over either. Likewise, reloading immediately after an edit can execute the PREVIOUS Turbopack bundle: the deep link looked broken until a second reload, and the giveaway was a runtime probe returning null while the served chunk already contained the new code.
  Gates: `tsc` clean, `eslint src` **0 errors / 23 warnings** (baseline 22; +1 is the pane's localStorage effect, the same advisory shape as the existing ones), 42 tests, production build clean.
- 2026-08-04 — **v1.1.2 (DEPLOYED + committed `e071f71`, pushed to main; prod verified serving v1.1.2 on tracker.studionmore.com) — the task search dropdown floats, settings split into per-topic tabs, the weekly plan's columns are one width, and a plan entry that isn't a task can be edited.** ✅ **Checked in the app by Nitsan before deploying** ("all works") — the first batch in three releases that didn't ship blind. (1) **`TaskAutocomplete`'s result list is an overlay, not part of the layout.** In flow it grew whatever hosted it: the day-details "add hours" popup stretched as you typed and, because the form row is `flex-wrap` with stretch alignment, the duration/description/Add controls beside it stretched to match. The list is now `absolute top-full` with `w-max min-w-full` capped at `min(34rem, 100vw - 2rem)`, so it hangs OUTSIDE the popup and sizes to its own content — task titles were being trimmed to "Vo…" in a list whose whole job is telling two tasks apart. **Measured in a static harness against the real compiled CSS** (the only way to check this without a signed-in session): card height 205px identical open and closed, panel 522px wide against a 334px input, bottom edge 91px past the card, zero rows truncated; one absurd title caps at 544px and truncates instead of running off-screen. Two details that make a floating list behave: `onMouseDown → preventDefault` on the panel, or blur unmounts the row between mousedown and mouseup and swallows the click; and it only opens while the input has focus, so it can't hover over the rest of the card afterwards. Picking now **fills the input with the chosen title and closes the list** (the log-time form keeps that input on screen, so it was the only record of what you'd chosen), typing after a pick **clears the remembered task** via the new `onQueryEdited` — otherwise hours could be filed against a task the user had typed away from — and the picker is remounted by `key` after an add so it can't keep naming a task that is no longer selected. (2) **Settings' own-account surface splits into three tabs — My pictures / My details / Password — for everyone**, not just for members. Members had no strip at all (their whole page was one tab, which is also what made it the longest page in the app: two picture uploaders, fourteen HR fields and a password field in one scroll); admins keep Clients and Studio setup after those, so five tabs. The same three blocks are the same for both roles, so splitting for one and not the other would have been two page shapes for one page. `settings.tab` remaps the stored `"account"` to `"pictures"` on read, and the **details panel is lazily mounted then kept mounted** — `HrDetailsForm` is controlled state behind ONE explicit Save, so unmounting on a tab switch would silently discard fourteen fields of typed text. (3) **Every person column in the weekly plan is exactly as wide as every other — `w-[175px]`, Nitsan's floor** — and a long chip wraps instead of widening its column: under auto table layout a column was sized by its longest chip, so one designer's column was visibly wider and the width moved around as the week's work changed. `table-fixed` on the table, a width (not a min-width) on the header cells, since under fixed layout the header row's widths ARE the column widths. `w-full` stays and only ever adds: measured in Chrome, columns totalling MORE than the container keep their widths and the wrapper scrolls, and totalling less the spare is split evenly — so no min-width is needed. ⚠️ **A measurement I got wrong first and then re-ran, worth remembering as a method note:** I concluded from a static harness that a fixed table squeezes its columns (128 → 94px) and added a computed `minWidth` to the table to stop it. It doesn't squeeze — the harness was loading a **stale copy of the built CSS in which `.w-32` was absent**, so the cells had no width at all and fixed layout simply divided the table evenly, which reads exactly like a squeeze. Re-tested against a fresh bundle: `w-32` at a 900px container holds 128px and scrolls, with and without the min-width. When a harness measurement contradicts the spec, suspect the stylesheet before believing the browser. Chip labels moved from `truncate` to `break-words` (client names stay truncated — a second line of 9px uppercase is noise), and the column header name truncates with a `title`. **At ~10 columns the grid is now ~1846px wide, so it scrolls sideways on a laptop** — that is the cost of the 175px floor; hiding unused columns in the column manager is the lever. (4) **Clicking an absence or a free-text chip opens an editor** (admins only, `canEdit`), because there was no way to change either one: right-click offered Copy and Delete, so fixing a typo or turning a sick day into a vacation meant deleting and re-adding. `AddEntryModal` became **`EntryModal`**, one component for both jobs — editing IS choosing again, so a separate editor would have been the same three controls with a different verb — and it gained a Remove button, the current absence kind marked, and a prefilled search box when the entry is a note (with the free-text row reading "Save as text" rather than "Add"). **Task chips deliberately keep opening the task pane**, which is where a task is edited. New **`updatePlanEntry(id, patch)`** in the store: one undo step, only the named fields touched, and — the lesson from `updateTimeEntry` — the camelCase↔snake_case mapping stated **once** in `PLAN_ENTRY_FIELDS`, which builds the DB row, the local patch and the inverse from one list. (5) **The plan's search offers COMPLETED tasks, and picking one reopens the task** — Nitsan's rule: work comes back, and a task marked done too early is exactly what someone needs to schedule. `TaskAutocomplete` gained `includeDone` (off everywhere else — in a log-time form a finished task in the list is just a way to book hours against it by accident); done rows sort after the open ones, are struck through and carry a "done" pill, and the 12-row cap counts open matches first so finished work can't bury live tasks. The reopen lives in the **store**, not the modal (`plannedTaskToReopen` → `status: "todo"`, folded into the same undo step as the plan write), because every route a task takes into the plan — add, edit, paste — goes through `addPlanEntry`/`updatePlanEntry`, so a new caller can't bypass the rule.- 2026-08-04 — **v1.1.1 (DEPLOYED + committed `7049374`, pushed to main; prod verified serving v1.1.1 on tracker.studionmore.com) — seven follow-ups from Nitsan's first read of v1.1.0.** ⚠️ **Deployed WITHOUT browser verification** — no signed-in dev session was available in that session, so both v1.1.0 and these follow-ups reached production having passed only tsc / lint / 42 tests / a production build. If anything in this batch is wrong, the likely places are the geometry (absence fill in a tall row, the plan rail handle, the three-pane home row) and the store's rewritten boot path. (1) **"Hours by client" is one pane with a Split / Over-time tab** — the donut and the per-client trend answer the same question, so they were two panes competing for a slot rather than two facts. The scoping logic they share is now `useHoursScope`, so they and "Hours over time" can't disagree about which hours they describe; members see the donut with no tab strip. **"The studio" roster took the freed row and is full width again**, back to 5–7 cards across instead of wrapping at four. (2) **The weekly-plan resize handle sits in the MIDDLE of the gap** (`-left-2 -translate-x-1/2`; it was centred on the rail's own edge, i.e. the right end of the gap) and `ResizeHandle` gained `visibility="always"` — a short grey grip visible at rest, because a pane edge, unlike a table column edge, gives no hint it can be dragged. (3) **Absences really do fill the cell now.** `inset-0` was already reaching the cell's borders; the problem was that `PlanCell` only ever grew to its own content, so in a row made tall by a neighbouring cell the fill covered one chip's worth of height. Fixed with the `h-px` table-cell trick — a `<td>` gives a percentage-height child nothing to resolve against unless it has a height of its own, and the row overrides `h-px` anyway, so `PlanCell`'s new `h-full` finally stretches to the row. (4) **A section whose tasks are all done folds away with them**, and comes back with "Show completed" — measured against ALL tasks, and an EMPTY section stays visible (it has nothing finished to hide behind, and a section you just created must not vanish as you make it). Nitsan first read this as a per-section Hide *button* and asked where it was; he confirmed automatic is what he wants, so the only addition is that **it now says so** — a "N finished sections hidden — show completed" line at the foot of the table, which is also the control. Folding a section away silently reads as data loss. (5) Reports task popup: the task name is the **title** (`text-lg`), not a caption over the rows. (6) Member page: **open tasks moved up under the four tiles**, so the left column fills the height the chart sets beside it; "Most active clients" takes the full-width row below. (7) Team page: the layout toggle shows **"Cards" / "Table" beside its icons**.
- 2026-08-04 — **v1.1.0 — 23 changes from a fortnight of real use: the task pane becomes the single place a task is edited, and the app refreshes itself.** Planned as one batch with Nitsan; **no migrations** (his call, which dropped the "mark a section complete" ask — sections have no completed column). Per-user UI preferences go in **localStorage**, not a `profiles.prefs` column.
  **Shared primitives, extracted first** so nine of the items had something to sit on: **`Tabs`** (renders the strip only, never the panels — the member page needs its HR panel kept mounted; replaces the two copy-paste segmented controls), **`Modal`** + `ModalClose` (the overlay-40/card-50 recipe was in 9 places; adds Escape-to-close and focus-on-mount; `layer="raised"` = z-[60]/[70] for a popup opened from inside the task drawer, which is itself 40/50 and wins on DOM order), **`TaskNameLink`**, **`PeriodStepper`** (lifted from `dashboard.tsx`, generic over the range list — home 4, team 5, reports 4), **`LogTimeForm`** (one implementation of SIX hand-rolled add-time forms; only one of them had the admin member picker, so where an admin could backfill a colleague's hours depended on which screen they were on), **`TimeEntryModal`**, **`useMe`/`useIsAdmin`** (19 inlined `role === "admin"` checks; reading `currentUserId` through the store keeps `?viewAs` working for free), **`taskHoursDone`** in `src/lib/task-hours.ts`, and `period-math` gained **quarters** + `periodRange()` with tests asserting the new arithmetic is byte-identical to the team page's old inline expression.
  **Auto-refresh (no websockets).** `src/lib/snapshot.ts` splits the boot query into **hot** (tasks, plan entries, the 400-row feed window + entry sums, task requests, dev items — every 60s, never while `document.hidden`, focus debounced to 20s) and **cold** (people/clients/sections/tags/columns/periods/day-states — every 10th tick or a focus after 5 min away). **The two `isMissingSchema` column ladders were moved character-for-character** — degrading past `legacy` leaks ~4,000h of backfill into every personal figure. Three things a refresh must never do, each enforced: touch `loading`/`bootError` (would flash the splash over a working app), run `getUser()`/redirect (would navigate someone away mid-edit), or land mid-write. The last one needed a **pending-write registry** — a `wrote(label)` tail replacing `if (error) noteWriteError(...)` at 30 update/delete sites (inserts are left alone: a new row can't be clobbered). An **empty studio is treated as auth, not data**, and thrown away rather than applied. **Undo now expires**: history entries carry an epoch, a refresh whose `fingerprint` differs bumps it, and undoing a stale step clears the history with a neutral notice instead of pushing a pre-change value back over a colleague's edit. `loadedTaskExtras` is cleared and the open task re-fetched, and `openTask`'s three merges became **replace-by-task** so a deleted comment can actually disappear. A faint pulsing dot beside the search box is the only UI.
  **Weekly plan:** dropping a chip in someone else's column now **reassigns the task** as ONE undo step (new `movePlanEntryToCell` + an `asOneStep` helper that saves/restores the suppress flag rather than clearing it — `redo()` already holds it); it reassigns only for a task-linked chip landing in a real `member` column with a profileId, and dragging OUT to Studio/Waiting list deliberately does not clear the assignee. **Absences fill their cell**: the padding moved from the `td` into `PlanCell` so `inset-0` reaches the border (total unchanged, so chips are pixel-identical), and the drop outline gained `-outline-offset-2`. The right rail is **drag-resizable** (`colw.plan-rail`, 150–420, handle on the left edge via new `invert` option).
  **Bug — the search dropdown painted under the plan's sticky header.** Root cause: the app header is `sticky z-20` **and** `backdrop-blur`, so it is its own stacking context and its `z-50` dropdown could never rise above 20, while the plan's `thead` was also 20 and later in the DOM. Header raised to **z-30** with the whole z-scale written down beside it.
  **Client page:** name + actions on ONE line, `sticky top-14 z-10` under the header (must stay outside the `overflow-x-auto` wrapper or sticky dies), with **Tasks / Overview tabs** — Overview finally gives `ClientStats` a home, since it was `hidden xl:flex` and therefore invisible below 1280px. Section select-all is **hover-revealed** (the `group` goes on the header div, not the wrapper, or hovering a row would reveal it). **Sections drag to reorder** (`reorderSection`, dense 1..n, one undo step) with three guards keeping it apart from the task drags that share the table. **"Hrs/budget" split into Hours + Budget — in BOTH task tables**, sharing `taskHoursDone`; the My Tasks/home table keeps its utilisation % in the tooltip. Task name **opens the pane** and the inline rename is gone (with the redundant Maximize2 button) — so **the pane's title became editable**, or nothing could be renamed anywhere.
  **Task pane:** Budget → a prominent **`12 / 24 h`**, numerator computed, denominator inline-editable. Per Nitsan the numerator now **includes the pre-Everhour remainder** so the pane and the client table agree on the ~150 recovered tasks, and the time list carries an explicit "pre-Everhour history (not itemised)" line so the rows still sum to the headline. A square **`+` tile** (37px = a row's rendered height) opens the log-time popup, and clicking a row opens edit/delete — **only** for rows you may change; others keep today's markup exactly, no cursor, no hover, no handler. ⚠️ **Two latent bugs this made reachable, both fixed:** `restoreTimeEntry` dropped `legacy`/`date_estimated`/`legacy_author_name`/`moved_from_task_id`, so deleting a recovered entry and pressing ⌘Z would have resurrected it as an ORDINARY one and leaked its hours into every personal aggregate (delete is also blocked on legacy rows); and `updateTimeEntry` passed its patch straight to `.update()`, working only because minutes/description/date are spelled the same in both cases — it now builds the row explicitly, which is what let `userId` reassignment in (admins only; RLS refuses it for members anyway, since `own time update`'s USING clause constrains the very column being changed). Discussion restyled as a **conversation** — bubbles, own messages right, one avatar+byline per 10-minute run, timestamps under the bubble.
  **Reports:** period arrows + Now. The preset list simplifies to This week / This month / This year / Custom, because "Last week" was a preset that already encoded an offset and stepping back from it had no coherent meaning — ◀ still lands on a period labelled "Last month". **"All time" is deliberately absent**: the query is `.gte/.lte`, so unbounded would pull every entry ever into the browser. The task popup is fully editable via the shared `TimeEntryModal`, its title opens the pane, its client chip navigates, and the `id:""` sentinel is gone now that `addTimeEntry` returns the inserted row. ⚠️ A row **re-dated out of the period is removed from page state** — every total, the breakdown, the stats and the CSV derive from it.
  **Team:** cards ⇄ **table** layout (`team.layout`), columns Member/Role/Email/Started/Tenure/Left/Hours·period/Open/Billable, built from the page's own memos so the two layouts can't disagree; period **stepping** with quarters (and the "Hours · this month" tile label now follows the stepped period instead of lying). Range key persisted, **offset deliberately not** — reopening on "Q2 2025" would present stale hours as current. **Member page:** hours-per-month becomes a `MultiLineChart` (with 12 buckets `LineChart` labels nothing and degrades to a squiggle), tiles left / chart right, **open assigned tasks** in the chart's old slot, and **two tabs with HR details behind the second** — lazily mounted then kept mounted, which both protects `HrFields`' uncontrolled inputs and means `member_hr`/`member_notes` are **not queried at all** until an admin asks, so opening someone's page to check their hours no longer pulls national IDs into the browser. **Settings:** three tabs (My account / Clients / Studio setup); `ChangePassword` renders **once** — it was duplicated purely to balance a hand-split masonry, which is why the masonry went; a member sees no strip at all, and the stored tab is validated against the visible list. **Home:** one row of tiles (6) / week timesheet (4) / **Coming up** (2), the last with `lg:h-0 lg:min-h-full` so it fills the row without stretching it, its list rewritten from a 4-card carousel to a vertical scroller capped at 12 with a "+N further ahead" footer.
  Verified: `tsc` clean, lint **0 errors** and warnings **75 → 21** (all remaining are the pre-existing React-Compiler advisories), **42 tests**, production build clean. **NOT yet verified in the browser** — this session had no signed-in dev session, so every check in the plan's verification table is still outstanding.
- 2026-07-30 — **v1.0.5 — avatars on the week timesheet; an admin can log hours for anyone, on any date, from every entry point.** (1) The timesheet's name column carries an `Avatar` beside the first name (`w-20` → `w-28`); heights still line up with the tiles. (2) **`UserDayDetails` treats its `userId`/`date` props as a STARTING point, not a fixed target.** For an admin the header is now a member `<select>` + a date input that re-fetch on change, which is what finally makes the Time Feed's generic **"Add new hours"** button useful — it opens on today/self, and the studio's actual need is backfilling someone else's Tuesday. Members still get the plain static header (verified via `?viewAs`: no select, no date). (3) The task panel's add-time form gained the matching **date** field beside the member picker, and the date **stays put after an add** rather than snapping back to today, because backfilling is usually several entries on one past day. (4) **New `src/lib/members.ts` → `loggableMembers(profiles, meFirstId)`** — the "who may hold a time entry" rule was about to be copied into a third place. It excludes accountless profiles: since 0018 a profile is a *person*, and the ~24 former staff kept for attribution have no login and must never appear in a picker. (5) **A React subtlety worth keeping:** the popup stores its fetched rows **tagged with the person+day they belong to** (`{key, rows}`) instead of a separate `ready` flag. Switching person used to need `setReady(false)` inside the effect — a lint-flagged cascading render — and without it the previous person's entries flashed up under the new name. Deriving readiness from the key removes both problems. Verified live: switching to Shaked / 28 Jul re-reads 7h 30m with the right tasks, matching the timesheet cell.
- 2026-07-30 — **v1.0.4 — week timesheet trimmed to Time-Feed styling; admins can log hours for anyone from the task panel.** (1) The pane lost its title, its subtitle and the "Click any cell…" footer, and each cell lost its bordered box — cells are now plain `td`s with `hover:bg-brand-soft/60` and a faint `–`, exactly like the Time Feed timesheet. **The missed-day signal moved into the text colour** (`text-danger/60` on the `–`) rather than a tinted outline, so the grid stays flat but the pane still does its one job. (2) The card is `h-full` and the wrapping grid dropped `items-start`, so the two columns stretch together — verified identical at **276px**. (3) The "Time Feed →" link moved into the header row's otherwise-dead corner cell, since there's no title left to hang it off. (4) **`timeForUserId` in `task-panel.tsx`**: admins get a member `<select>` inside the add-time form, defaulting to **"Me"** and sorted with the current user first (alphabetical order buried "Me" between Nadav and Office). `addTimeEntry` already took an optional `userId` — this is the UI that was missing. Members never render the control and always attribute to themselves; verified via `?viewAs` that the select is absent for a designer. The list is active profiles with an account, since an accountless former-staff profile (0018) cannot hold a new entry. **Note the other admin path already worked** — clicking any cell in the week timesheet opens that person's day and logs to them, so this fills the gap for logging against a specific task.
- 2026-07-30 — **v1.0.3 — admin home: KPI tiles 2×2 at half width, week timesheet beside them.** Nitsan's ask, and it closes the studio-manager review's top gap ("nothing surfaces a designer who forgot to log hours") without a notification system. (1) `StatTiles` dropped `lg:grid-cols-4` for a plain `grid-cols-2`, and the admin branch wraps it with the new pane in `lg:grid-cols-2` — each tile keeps roughly the width it had when four spanned the page. (2) New **`week-timesheet.tsx`**: designers × the current week, first names only, cell = that day's hours. **Deliberately not the Time Feed timesheet in miniature** — no per-designer total, no per-day total, no expandable rows, because the question is only whether a cell is filled and a column of sums invites reading it as a performance table. Hover a cell for a native tooltip listing `Client · Task — Xh`; click opens the same day popup as Time Feed. (3) **Three judgement calls worth keeping:** a blank cell is tinted red only when the day is **over** (`iso < todayIso`) — flagging today would nag every morning about hours nobody has had time to log yet; rows are `role === "designer"` **plus anyone who actually logged that week**, since Itay/Michal/Nitsan are admins whose permanently-red rows would read as a problem, but `Office` is a shared *admin* account that does log (10.5h) and hours that exist must never be invisible on a pane whose job is spotting missing ones; and Fri/Sat columns appear only when somebody logged then, so weekend hours aren't hidden by the Sun–Thu default. (4) It reads `entrySums`, not `entrySumsAll` — a 2019 backfill row must never make a designer look like they logged. (5) **`UserDayDetails` + `EntryEditRow` extracted** from `feed/page.tsx` to `src/components/user-day-details.tsx` so both pages share one popup instead of drifting; it now uses the store's `loadDayEntries` rather than opening its own Supabase client, which removes the last direct-Supabase call outside the store. Verified in the browser: tooltip reads "Studio · Voyantis Case Study — 7.5h", the popup opens from the home pane *and* still from the Time Feed timesheet, tsc clean, lint 0 errors (77 → **75**, the extraction removed two), 37 tests, build clean.
- 2026-07-30 — **v1.0.2 — hiding a task or a period is a FOCUS tool, not confidentiality.** Reverts the one behavioural change in v1.0.1 that acted on the opposite assumption. Nitsan: *"if i hide any task or period it doesnt hide it from summary — its just hiding it to focus on current period or current active tasks."* So the security review's "hidden report values are recomputable" finding is **not a finding**: an admin hides a finished period so the client reads the current one, and `total − Σ(visible periods)` giving the hidden number back is harmless because it was never secret. `sanitizeSnapshot` again ships each task's true `totalMinutes` spanning every period, hidden ones included; the test that asserted non-recoverability now asserts the opposite, so nobody re-"fixes" it. **The structural sanitization from v0.99.3 is untouched and still right** — hidden rows/columns are physically absent from the payload rather than hidden with CSS, which is what stops a client reading them at all. **Still open, a real mismatch this exposed:** the public report's summary tiles ("Delivered to date", the current-period tile, cap/remaining) are computed from the *visible* periods and *visible* tasks only, because the sanitizer removes hidden ones before the view sees them — so hiding DOES currently move those tiles, which contradicts the rule above. Left alone pending Nitsan's call, since making them span hidden rows means passing the true totals separately.
- 2026-07-30 — **v1.0.1 — review fixes: privilege escalation, attachment uploads, report leak, first tests.** Four review agents were run against v1.0.0 (ux / code / security / studio-manager). This carries the fixes; **migration `0021_harden_profile_writes.sql` MUST BE APPLIED.** (1) **CRITICAL, any member could make themselves an admin.** `own profile update` (0001) was `for update using (id = auth.uid())` with no `with check` and no column scoping — Postgres reuses USING as the check, and that expression only constrains `id`, so `update profiles set role='admin' where id = <me>` succeeded from the anon key and handed over `member_hr` (national IDs, addresses), `member_notes`, `finance_*`, `client_billing_periods` and every admin-only task column. Both real boundaries were bypassable by escalating *out* of them. 0021 adds `trg_profile_member_cols` in the shape of 0011 — members may change only `name`/`avatar_url`/`photo_url` — plus the explicit `with check`. **It exempts the service role, unlike 0011**, because /api/avatar, /api/member-image and /api/admin/users legitimately write profiles with the service key after verifying the caller's session; a null `auth.uid()` can only be the service key or the SQL editor, since an anonymous request matches neither policy and so never reaches the trigger. (2) **`/api/task-attachments` was the only upload route with no allowlist** — it stored `contentType: file.type`, attacker-controlled, into the public `task-files` bucket, so `brief.html` became a permanent script-hosting URL on the studio's own Supabase origin. Now goes through `classifyUpload` like /api/intake (the two image routes use the stricter `classifyImage`). (3) **A hidden report column was recoverable as `total − Σ(visible periods)`.** `sanitizeSnapshot` now re-derives each task's total from the columns that survived, on whichever axis had one removed (lower bound if both). **REVERTED in v1.0.2 — the premise was wrong, see below.** Moved to `src/lib/report-sanitize.ts` and unit-tested. (4) **Archived members could still use the app** — nothing anywhere gated on `active`. `(app)/layout.tsx` now renders `AccountInactive` (a UI gate; see the new **Offboarding** section — the real fix is banning the auth user, which 0018 made safe). (5) **Generic errors** on the member-facing routes (`me/hr`, `avatar`, `member-image`) instead of raw Postgres messages naming columns and constraints; `/api/me/hr` PATCH gained per-field length + format limits. `/api/admin/*` deliberately still returns the real message — admins are trusted and it's the difference between a debuggable failure and a shrug. (6) **First app tests: `npm test`** (Vitest). `period-math.ts` extracted from `dashboard.tsx` with `now` injectable — 28 cases over the period boundaries the KPI deltas read, including the `<=` end-of-period bug and "day buckets never project"; `report-sanitize.ts` 10 cases, one of which fails on the pre-fix code. **38 tests, and the app's number-crunching had none before this.** (7) **A failed boot no longer renders an empty studio.** The catch only logged; every pane then read zero, which is a claim about the data. New `bootError` in the store → a real error screen with a retry. (8) **The boot query's schema fallback no longer treats a network error as a missing column** — `fetchAll` throws `DbError` carrying the Postgres code and the fallbacks only step down on `42703`/`42P01` (`isMissingSchema`). This mattered: silently degrading past `legacy` leaks ~4,000h of backfill into days-worked, tenure and the feed. (9) `DayLog` no longer opens its own Supabase client and calls `mapTimeEntry` itself — new `loadDayEntries(userId, date)` in the store (the store only holds the recent 400 rows, so the query is real and has to live somewhere). (10) **UX:** `Avatar` gained `emptyTitle` — its own `title="Unassigned"` beat any wrapper's on hover, so a recovered entry's stored provenance was replaced by the word "Unassigned". **Note the review overstated this as Critical**: named ex-staff have had profiles since 0018 and render correctly (verified: "Edor Nisim"/EN), so only rows naming nobody were affected. (11) **`scripts/README.md`** — a status table (read-only / repeatable / done-once / break-glass) for all 26 scripts, plus the bulk-SQL warning and the pending items. The only record was this working log. Verified: tsc clean, lint **0 errors** (77 warnings, none in the new modules), 38/38 tests, and in-browser — KPI figures byte-identical after the period-math extraction (671.5h / 63% / 51 / 83.9h), DayLog steps 29→26 Jul fetching each day, the recovered row's avatar now reads "(date estimated)". **NOT fixed, deliberately:** the public storage buckets (`avatars`/`task-files`/`intake` are world-readable via `getPublicUrl`; needs private buckets + signed URLs and an intake-queue render change), and the `store.tsx`/`dashboard.tsx` monolith split the code review asked for — that is a large unreviewable diff and the lint warnings tied to it are already deferred to that pass.
- 2026-07-29 — **v1.0.0** — Out of pre-release. Nitsan: "you can make it version 1 — we are using it in the team." No code change beyond `APP_VERSION`; this release carries everything logged under v0.99.37–39 below, which were never deployed separately.
- 2026-07-29 — v0.99.39 — **Pre-2022 periods showed no people at all.** Nitsan: "why it doesn't show any members when I select a year before 2022?" Because **every** time entry before 2022 is recovered pre-Everhour history (verified against the DB: 2019 → 1495 entries, **0** non-legacy; 2021 → 1676 / 0; 2022 → 2333 / 509; 2023 → 4907 / 4433), and three surfaces gated on the legacy-free list. But **about a third of those old entries DO name a person** (2019: 518 of 1495), so the right gate is "has an author", not "isn't legacy". (1) `StudioTeamStrip` now reads `entrySumsAll` and buckets by `userId`; 2019 goes from an absent pane to **9 designers**, all archived. (2) `StatTiles`: **"Tasks worked" dropped the author gate entirely** — a task was worked whoever logged it, and it read 0 for every pre-2022 year beside thousands of hours (2016 now reads 31). **"Avg / designer" keeps only attributed hours, on BOTH sides of the fraction** — it was "0h · 0 designers" for those years and now reads 19.8h across 4 for 2016. (3) The honest part: most old hours name nobody, so the strip's cards can't add up to the period total. It now says so — **"· 6825.13h unattributed"** beside the designer count (2019, of 8223.38h), tooltipped. Without that the pane silently under-reports. (4) Also fixed the tile hover copy, which claimed the previous period was "clipped to today" even for a finished historical period — it now says "the whole of the previous period, since this one is already complete" unless the selection actually contains today.
- 2026-07-29 — v0.99.38 — **`profiles.end_date` (migration 0020, applied 2026-07-29), client chips are white capsules, billable toggle un-pilled.** (1) **Migration 0020 adds `profiles.end_date` and a `before insert or update` trigger that forces `active = false` whenever it is non-null** — in the DB rather than only the app, because Nitsan is filling these dates in by hand in the SQL editor later and no app code should be able to forget the implication. Clearing the date does **not** restore anybody; the Restore button in `/team/<id>` clears the date *and* flips `active`, and `updateProfile` in the store holds both halves of that invariant (setting `endDate` ⇒ `active: false`; `active: true` on someone with a date ⇒ `endDate: null`, or the trigger would immediately re-archive them and the button would look broken). Field lives in "HR details" beside Start date; the archived banner and the team-list row show the date when set; the studio strip's "past" tooltip reads "left <date>". `profiles` is fetched with `select("*")`, so no query needed updating. Applied and verified: the column is live and filterable, all rows `null` so far (the dates come later). (2) **`ClientChip` is now a white capsule** (`rounded-full border border-border bg-surface`, `px-1.5/px-2`) so the colour dot and the name read as one object; the link variant hovers to a brand-tinted border instead of underlining under the dot. The **donut legend rows got the same capsule** — the one other place a client name sits beside a colour dot with no container of its own. Left alone deliberately: the client-report tabs, weekly-plan filter buttons, settings list rows and global-search results, all of which already have their own frame. (3) "Billable only" lost its bordered pill — among the range pills it read as one more range button; it is now just the switch plus a label that tints when on.
- 2026-07-29 — v0.99.37 — **Studio strip includes past members; billable toggle; period nav never disappears.** (1) `StudioTeamStrip` no longer filters on `p.active` — the pane describes a *period*, and the `min > 0` gate was already the right one, so someone who left in March still shows for March. Past members get a **dashed border and a "· past" suffix** rather than a dimmed card: the hours are as real as anyone else's. This also **fixed a genuine disagreement on the page** — the strip read "7 designers" on This month while the "Avg / designer" tile divided by 8, because one archived person had logged July hours that counted in the tile and were invisible in the strip. Now 8 and 8; This year reads 14 with 6 past, matching its tile. (2) "Billable only" is a **switch** (sr-only checkbox + track/knob, so the whole pill stays one click target and keeps keyboard focus) and is the **first element in the filter row**. (3) The period arrows and **"Now" are always rendered** — the arrows used to vanish on "All time" and "Now" only appeared once you had already stepped away, so the row reflowed under the cursor. All three are now disabled + `opacity-30` instead, with a tooltip explaining why on "All time".
- 2026-07-29 — v0.99.37 — **Explained KPI tiles, projected last bucket, reports polish.** (1) New **`InfoDot`** in `ui.tsx` — a circled "i" with a real hover/focus card (a `title` attribute mangles three sentences into one delayed run). Each of the four admin KPI tiles now carries one via `StatTile`'s `info` prop: what the figure counts, what it deliberately excludes (recovered pre-Everhour rows in "Tasks worked"/"Avg / designer"), and **the exact compared window spelled out as dates**. The last tile passes `infoAlign="right"` so its card doesn't run off the page. (2) The "vs last" comparison already clipped the previous period to the same elapsed portion (`comparablePrevRange`); fixed its boundary — `today < sel.end` meant that **on the final day of a period the comparison silently reverted to the whole previous period**, reading as a collapse. Now `<=`. (3) **The running last bucket on both admin line charts is projected.** `bucketize` returns its `unit`; `bucketProjection(unit, lastKey)` gives the run-rate factor (month: `daysInMonth / today`; year: `daysInYear / dayOfYear`; **day buckets return null** — scaling "today" by the evening left in it is noise). `MultiLineChart` gained **`projection: {index, values}`**: it plots the estimate at that index, dashes the segment leading into it, italicises the label as `~Jul`, notes "dashed = Jul projected" under the headline, and shows "(X so far)" in the tooltip. The **headline total keeps reading real logged hours**; only the trend chip reads the projection, which is the point — estimated-full-July against a complete June. Verified on This year: 5453.75h headline unchanged, `~Jul` dashed on both panes. (4) Reports: **"Hours in period" is now the same `MultiLineChart`** as the home page (single "Hours" series, so no `totalSeries` needed); **"Hours by user" bars are split** billable-solid / non-billable-`bg-brand/30` — `SplitBar` gained `tone: "grey" | "blue"` (default grey keeps the client table as it was) and now titles both segments with their hours; `HBar` gained a `bar` slot to host it. **Chevrons** on every client row (display-only — the whole row already toggles) and on section sub-rows, plus a **collapse-all chevron in the table header** that reads "open" when ANY row is, so a half-expanded table closes on one click. (5) **"Coming up" lost its 30-day horizon and its title suffix** — it now shows the next 4 occasions however far off, `OCCASION_HORIZON_DAYS` is deleted, and `relativeDays()` switches to "in ~N months" past 60 days. `MEMBER_OCCASION_DAYS` (7) is untouched and still the only thing the dimming means.
- 2026-07-29 — v0.99.36 — **Donut doubled + up to 15 clients; "Hours over time" gained a billable line.** (1) `PieChart` `SIZE` 150 → **300px** and the legend is capped at the donut's height with `overflow-y-auto` — needed because `PIE_CLIENTS` went **6 → 15** (plus the "Other" tail). Verified on All time: 16 rows, scrollHeight 380 vs clientHeight 300, so it scrolls; This month has only 12 clients with hours, so no scrollbar and no "Other". (2) The admin "Hours over time" chart now draws **two lines when the page is showing everything** — All hours (brand blue) and Billable (`#16a34a`); with "Billable only" on the second line would retrace the first, so it's dropped. `perBucket` therefore carries `{minutes, billable}` per bucket. **`MultiLineChart` gained `totalSeries`**: its headline total summed EVERY series, which is right for disjoint per-client lines but double-counts here because billable ⊂ all hours (671.5h would have read 1,096h) — passing the series label makes the headline read that one series. Verified: 671.5h on This month, 87,250.8h on All time, both unchanged by the second line.
- 2026-07-29 — v0.99.35 — **Admin home restructured: no My-tasks pane, line chart, 4-card occasions carousel.** (1) The compact **My tasks pane is gone from the admin home** and **"The studio" roster took its grid slot** (admins triage from `/my-tasks`); the strip's inner grid drops `xl:grid-cols-8` → max 4 across, since it is now half-width. The member home keeps its My-tasks pane unchanged. (2) The header's **"Tasks assigned" counter is hidden for admins** (members keep it) — it belonged to the pane that left. (3) **"Hours over time" is a line chart for admins** — the same `MultiLineChart` as the pane beside it, one brand series, which also **thins the x labels** (`step = ceil(n/6)`) where the columns had rendered all 20+ daily ticks. Members keep `MiniColumnsLabeled`. (4) **"Coming up" is now a 4-card carousel**, soonest on the left, arrows stepping one card and only rendered past 4 occasions (`1–4 of N`). **Two horizons now exist, and that is the whole point of the dimming:** the admin pane looks `OCCASION_HORIZON_DAYS` (30) ahead, the member hero only `MEMBER_OCCASION_DAYS` (**7**) — so an occasion the team cannot see yet renders at `opacity-50` with a tooltip saying so. Both constants sit together at the top of the Celebrations block; change 7 to change what the studio sees. Verified live: Shaked's birthday (today) full-strength beside a dimmed "World Photography Day — 19/8 · in 21 days".
- 2026-07-29 — v0.99.35 — **Client chips wherever a bare task title was ambiguous.** Four spots, all because titles like "Homepage"/"Onboarding" repeat across clients: member home **My-tasks pane** and **"Log my hours"** rows swapped their colour dot for a real `ClientChip`; the **Time Feed timesheet** per-task sub-rows gained one (cell `max-w-64` → `max-w-80`, chip capped at `max-w-32`); and the **"Hours by client" donut legend** rows are now **links to the client page** (`PieChart` slices take an optional `href`; the aggregate "Other" slice has none and stays plain text). `link={false}` on the two home panes and the timesheet row on purpose — those rows are themselves a button/open a popup, so an `<a>` would either nest illegally or hijack the click; the donut legend is the one place a navigating name was actually asked for. Verified: clicking "No Traffic" in the legend lands on that client's page, and the timesheet sub-rows read "No Traffic · LinkedIn Banner_" / "TEMA Creative · Website Update july2026_".
- 2026-07-29 — v0.99.35 — **Admin home: "Billable only" toggle.** A pill+checkbox in the page-wide filter row (admin-only; `HomeFilter.billableOnly`, gated on `isAdmin` so a member preview can't hold it on) scopes **every** hour figure on the page to billable tasks: `StatTiles`, `MyGraphs` (hours over time + hours-by-client donut), `StudioClientTrend` and `StudioTeamStrip`. **The two billable-SHARE readouts deliberately keep all hours as their denominator** — the Billable tile and the per-designer bars — because a share of a billable-only total is 100% by construction and would say nothing; the tile's sub-label switches to "of all hours" and the first tile relabels to "Billable hours" so the scope is never ambiguous. `StatTiles` therefore tracks `curAll`/`prevAll` (unfiltered) alongside `cur`/`prev` (filtered). Verified live on This month: 671.5h → **424.5h** (63% of it), Tasks worked 51 → 32, Avg/designer 83.9 → 53.1h, the internal **Studio** client drops out of the donut entirely (No Traffic 31.5% → 48.4%), and the team strip reads billable hours against the unchanged true share (Nadav 156h·88% → 136.5h·88%).
- 2026-07-29 — v0.99.35 — **Client page task table has drag-resizable columns, like My Tasks.** `client-view.tsx` now uses the same `useColWidths`/`ResizeHandle` from `resizable.tsx` (localStorage key **`colw.client-tasks`**) for Assignee/Due/Tag/Hrs-budget; the "$" column is left fixed (one glyph, nothing to reveal). Widths reach `TaskRow` — two levels down through `SectionGroup` — via a new **`ColWidthsContext`**, for the same reason `SelectionContext` exists: threading them as props would bury the drag/drop signatures. Defaults are the px equivalents of the Tailwind widths the cells used to carry (160/64/112/144). **The resize immediately exposed the squeeze this table always had**: the name cell was `min-w-0 flex-1`, so widening a column crushed the task name to one character instead of scrolling. Fixed the same way My Tasks does it — name cells get a `min-w-32` floor and the table wrapper went `min-w-[720px]` → **`min-w-fit`**, so the table grows and the existing `overflow-x-auto` scrolls (verified: scrollWidth 1290 vs clientWidth 691). Verified by a real drag: 160 → 280px, persisted to localStorage, body rows followed the header, and task names that used to read "Car…"/"Ho…" now read in full.
- 2026-07-29 — v0.99.35 (**DEPLOYED + committed 356cea2, pushed to main**; prod verified serving v0.99.35 on tracker.studionmore.com, `vercel crons ls` reports none) — **Every live sync removed: the tracker is now the only system.** Nitsan's call, in two steps in one session — first the Google-Sheet weekly-plan bridge, then Everhour. **Deleted:** `vercel.json` (both crons — the file held nothing else, so there is no cron at all now), `src/app/api/plan-sync/`, `src/lib/weekly-plan-sync.ts` + the admin "Sync from sheet" button on the Weekly Plan header, `src/app/api/everhour-sync/`, `src/lib/everhour-sync.ts`, `src/components/everhour-sync-button.tsx` (the Time Feed "Sync Everhour" button), and — because the Everhour sync was its ONLY producer — the whole `sync_issues` surface: `/sync-issues` page, `/api/sync-issues`, the badged sidebar item, the bell's danger branch, the Client-Reports red banner, and `openSyncIssues` in `store.tsx` (incl. its boot query). **The `sync_issues` TABLE is left in the DB, untouched** — it was verified **empty (0 rows)** before removing the UI, so nothing was orphaned, and dropping it needs a migration nobody needs yet. `WEEKLY_PLAN_CSV_URL` removed from `.env.local`; `CRON_SECRET` and `EVERHOUR_API_KEY` are now unused by the app but kept locally because `scripts/sync-everhour.mjs` + `scripts/import-everhour.mjs` still use the key for a manual historical pull. **`scripts/import-weekly-plan.mjs` / `import-plan-sheet.mjs` / `sync-everhour.mjs` are deliberately KEPT** — one-off importers, run by hand, no schedule can fire them. **Owner actions DONE 2026-07-29:** the three env vars were deleted from Vercel and the Google-Sheet CSV link was unpublished — so there is no live path from either system into the tracker any more, only the hand-run scripts with the local key. Verified: tsc clean, lint **0 errors**, `npm run build` clean with none of the removed routes in the manifest, and in-browser — Weekly Plan header ends at "Columns", Time Feed header has only "Add new hours", sidebar has no "Sync issues".
- 2026-07-29 — v0.99.35 — **Time Feed day popup now names the client, not just the task.** `UserDayDetails` showed a bare colour dot + title, so "Onboarding" or "Homepage" — titles half the studio's clients share — gave no way to tell whose hours they were. The leading cell is now two lines in the same 192px: a real `ClientChip` above the task title (title now `text-foreground`, client stays muted), with a `Client · Task` hover tooltip. Verified in the browser: "Studio / Onboarding" and "No Traffic / LinkedIn Banner" on Nadav's 26 Jul cell, nothing truncated.
- 2026-07-29 — data — **2016 was missing from the finance DB entirely; imported (+1,180.50h of billing record, +847.53h into the tracker).** Nitsan spotted Quadream 2016 reading 345h in the tracker against **332h** in his sheet. Root cause: **the 0006 seed AND the granular feeder import both started at 2017**, because no 2016 workbook was on disk — so the "345h overage" was an artefact of having nothing to compare against. He supplied `PLAN_more_2016.xlsx` (now in `~/Documents/Claud cowork/financial sheets/`). **`scripts/parse-plan-2016.py`** (raw-XML, no openpyxl on this machine) → `data/finance-2016.json` → **`scripts/import-finance-2016.mjs`**: 20 clients / 117 client-month rows / **1,180.50h**, matching the sheet's own total row exactly and quadream to **332.00h** exactly, + 95 P&L rows on the existing `line_item` vocabulary. **INSERT ONLY works on the locked finance tables** — `finance_guard_locked()` is BEFORE UPDATE OR DELETE, so rows go in already `state='final'` with no unlock dance (unlike a rename, which needs final→actual→final). **Two things specific to 2016:** it is a **PARTIAL year** (the studio began trading in **May**, so only months 5–12 exist; no Jan–Apr rows are written because absent ≠ zero billed), and **per-client revenue is not in the sheet** — only hours + rate — so `revenue_gross = hours × rate` as in 2017–2026. NOTE the sheet's own "before discount" row uses a **flat ₪250 for every client** (1180.5 × 250 = 295,125) while real per-client rates run ₪200–250, so the import totals 259,245; the same divergence exists in 2017 (826,585 vs 1,011,700), so this follows the existing convention rather than inventing one. Backfill re-run for the newly comparable year: 16 clients, 40 entries, 847.53h — and **Quadream 2016 correctly received nothing**, because the per-YEAR reconciliation saw the tracker already above the billed figure. Comparison now spans 2016→2026: **2016 reads 1,181 finance vs 1,194 tracker (+13h, 1%)**. **What the 13h is:** the annual totals agree but the MONTHLY shape does not — `"Ui system - 165hrs"` is one lump with no per-comment dates, so all 165h landed in Nov on Asana's completion date while the real work spread Aug–Nov (sheet: Aug 70 / Sep 93 / Oct 90 / Nov 75; tracker: 36.6 / 102.9 / 36.5 / 165). **Title-derived hours get the total right and the month approximately** — worth remembering before trusting any single pre-2023 month. Audit fix: the expected re-home mapping still sent project `as:455542718969443` to In-reach and so failed on the deliberate merge — now maps to Quadream. PASS.
- 2026-07-29 — **OPEN CORRECTNESS ISSUE: the roll-up rule was never applied outside the 23 legacy projects.** Nitsan doubted the size of the tracker-over-finance gaps and was right to. "Children win, parent zero" reads `data/asana/parents.json`, which was only ever populated for the tasks `fetch-asana-stories.mjs` ran over — the dissolved legacy projects. The much wider `recover-title-hours.mjs` pass (+14,668h across every other client) read **titles only, with NO parent data**, so a parent whose title is a roll-up of its children was counted alongside them. **`scripts/report-rollup-risk.mjs`** quantifies it: **14,896h of 23,792h asana-recovered hours (63%) sit on tasks whose subtask shape was never fetched.** Volta, which WAS covered, shows 50/50 and 0h at risk; Mobileye 7/300 and 4,837h; Autofleet 1,806h; Blazepod 1,764h — **the three biggest exposures are the three biggest overages against the finance sheets.** This is an UPPER BOUND, not an error estimate (most tasks have no subtasks) and it cannot be separated from genuine write-offs without the real parent data. **FIX, needs `ASANA_ACCESS_TOKEN`:** re-run `fetch-asana-stories.mjs` over the **965 gids in `data/rollup-refetch-gids.csv`** to extend `parents.json`, then re-run the reconciler so the rule applies studio-wide.
- 2026-07-29 — data — **Cross-checked the whole recovery against finance-admin, then closed the remaining gap from the billing record: +18,509h.** `finance_client_monthly` (imported from the PLAN_more_YYYY.xlsx workbooks) is an INDEPENDENT record of the same years, so disagreement locates missing data. **`scripts/compare-finance-hours.mjs`** (read-only) does the comparison; the headline first read −44% for 2018–2021 and looked like a failed recovery, but the split by cause showed almost all of it was clients with **no tracker row at all** — boards that predate Asana, nothing to recover from. On shared clients the Asana recovery was accurate to **−580h across 53,513h**. Three reporting errors of mine that the split exposed: the plan sheet carries **Studio as a revenue row** while the tracker holds it non-billable, which made 2024–2026 read −20% against a column that deliberately excludes it (now its own bucket); "not in the tracker at all" actually meant "no billable hours", conflating 88 clients that never had a row with 3 that have one; and per-client matching without prefix resolution was ~90% false gaps. **Then, per Nitsan ("for pre-Everhour years what matters is hours per month/year/client; per task and per member is less important — count on finance data"), `scripts/backfill-from-finance.mjs`** created **85 archived clients + 104 holder tasks + 647 monthly entries = 18,508.97h**, dropping the unmatched figure from 13,297h to **90h**. Studio all-time 68k→**86,364h**; 2017 4,989h · 2018 6,043h · 2019 8,223h · 2020 8,189h · 2021 8,905h. **TOP-UP, NOT REPLACE** — `written = max(0, finance − already)` per client per YEAR, so every hour already recovered from a real task keeps its task, date and author; a re-run now plans 0.02h. **Two bugs caught in the first dry run, both worth remembering:** (1) In-reach and Quadream alias to one key and the client map kept whichever row it saw first, so Quadream's shortfall was measured against In-reach's 27h → a **2,312h phantom**; the whole alias GROUP must be summed. (2) Reconciling per MONTH banks every shortfall and ignores every surplus — billing months lag work months — which invented **1,223h for Mobileye and 1,198h for Autofleet**, both already above their billed hours for the year. Reconcile per year, distribute on the finance monthly shape. Every row is `legacy = true, date_estimated = true, user_id = null` (verified 647/647) so days-worked/tenure/feed/personal stats are untouched; reversal is `delete from time_entries where legacy_author_name = '(from finance plan)'`. **The audit got a real new branch, not a loosened assertion:** a finance-derived month must exist in `finance_client_monthly` for that client — stronger than the Asana window check. Client-name matching extracted to **`scripts/lib/client-names.mjs`** because it had been copied into four scripts and an alias added to one silently didn't apply in the others.
- 2026-07-29 — data — **Three client identities merged, each confirmed by Nitsan.** `Double` → **`Donsplus`** (the client renamed itself; applied by `scripts/merge-renamed-clients.mjs`). `In-reach` → **`Quadream`** (one client; some months were billed against the In-reach budget and 11 Quadream titles say so outright — `"UI/UX March 20 (Inreach Budget) - 51.75h"`. The 6 task moves are in **`data/merge-clients-inreach-quadream.sql`**, still to run — `tasks.client_id` is trigger-protected). `Raven` / `Ravוn` / `Ravin` are all **Ravin** — three spellings across the yearly workbooks, the middle one with a Hebrew vav from a keyboard-layout slip. **CHECK client_rates BEFORE renaming a client:** finance-admin resolves a rate by NAME and every row has `client_id` null, so a rename moves which rate applies. Verified no revenue moved — Donsplus/Double overlap at the same ₪300 from 2023, and the In-reach-budget tasks all sit in 2019-07…2020-05 where both tracks read ₪225. **`finance_client_monthly` rows are `state = 'final'` and `finance_guard_locked()` rejects any UPDATE that is final both before and after** — the documented unlock path is to leave the final state first, so a rename is two steps (final → actual carrying the new name → final). `client_rates` has no such trigger. The Ravin monthly rename is **still pending** — the auto-mode classifier blocked the write; its 9 `client_rates` rows WERE renamed, so until it runs those monthly rows have no matching rate row (harmless: historical revenue is stored inline on the row, and Ravin has no current-year rows). Re-run `scripts/merge-finance-client-names.mjs --apply`.
- 2026-07-29 — data — **`scripts/report-missing-clients.mjs`** → `data/missing-clients.csv`: the 91 clients the plan sheets bill for but the tracker is short on, each with a verdict and a clickable Asana link where a gid exists — **6 clients / 2,945h have a board that WAS imported and came through empty** (PlayStudios, OFFF, Stigg, Astrix, Anchor, In-reach) and are the only ones worth investigating; 20 clients / 7,721h have only tasks *naming* them, which Nitsan confirmed are **portfolio case studies on the Studio board, not client work**; 65 clients / 5,262h have no trace at all.
- 2026-07-28 — data — **The recovery had only covered the "Imported / Unsorted" bucket. +14,668h recovered from every other client's titles.** Nitsan spotted that 2020 showed <800h for a whole year, which was right: `reconcile-legacy-hours.mjs` was only ever run on the 23 legacy projects (7,948h), so **every other client's pre-Everhour Asana board was untouched** — Mobileye 4,733h, Autofleet 1,806h, Blazepod 1,764h, Studio 1,649h, Double 911h, Ception 811h… The reconciler has an `--all` flag built for exactly this and it was never used. New **`scripts/recover-title-hours.mjs`** (dry-run default) does it as a standalone pass, since the shape differs: titles only, no comments, and dates come from **Asana's `created_at` → `completed_at`** (all 969 had them) rather than a comment window. **966 tasks, 5,612 entries, 14,668.33h.** Scope is guarded to tasks with **zero** time entries, no existing `legacy_hours`, outside the 23 projects — so it cannot collide with real Everhour data. Years: 2020 768h→**4,018h**, 2019 1,517h→4,343h, 2021 805h→3,883h, 2018 1,825h→3,264h; total recovered 7,948h→**22,617h**, studio all-time 51,996h→66,665h. **A guard worth keeping:** a figure with NO `h`/`hrs` unit that is also >100h is more likely part of the name than an hour count — `"Storemaven - 404"` reads as a 404 page, and sibling tasks confirm that usage (`"404 page - 1.25h"`). 3 such tasks (632h) are SKIPPED and listed, never silently included: Storemaven 404h, `"אתר -124"`, `"Amnon's CES PPT -104"` — the latter two are plausible and can be added by hand. **Titles were NOT cleaned this pass** (renaming needs the SQL editor) and **316 budgets could not be set** for the same reason. **STILL UNRECOVERED: 1,255 tasks whose titles carry no figure** (1,223 have an `asana_gid`) — those need their comments fetched, i.e. a fresh Asana token. Review sheet at `data/title-hours-review.csv`. Audit assertion extended: an estimated date is valid if it sits inside EITHER the comment window (`activity_from/to`) OR the Asana `created_at`→`completed_at` window — the two passes used different sources.
- 2026-07-28 — v0.99.34 (deployed) — **Half the recovered history was invisible on every chart; now dated.** Migration **`0019_estimated_entry_dates.sql`** — **applied**. Of the 7,948.65h recovered, only **3,994.90h had dates**; the other **3,953.75h sat in `tasks.legacy_hours` with no day at all**, so no time-series could plot it — the early years read ~3,995h when the studio had logged nearly twice that. Those hours are undated because a task's TITLE recorded a bigger total than its comments accounted for (`"Ui system - 165hrs"`, comments totalling 8h → 157h with no date). Per Nitsan ("when it happened can be estimate"), `scripts/spread-legacy-remainder.mjs` (dry-run default) distributes the remainder across each task's **observed** window — proportionally to that task's real dated entries where they exist, evenly across the activity_from→activity_to months otherwise, else the section's `closed_on` month. **4 tasks / 195h with no date signal are left alone, never guessed.** Result: 237 entries, **3,758.75h onto the timeline**; 2016 79h→345h, 2017 81h→**1,441h**, 2018 741h→1,825h, 2019 1,203h→1,517h, 2020 440h→768h, 2021 524h→805h; studio total 48,222h→51,996h. Every such row is `date_estimated = true` with **`user_id = null`** (an estimated date must not be pinned on a named person) and shows with an asterisk + tooltip in the task panel. **The total is unchanged at 7,948.65h** — hours only moved out of `legacy_hours` into entries, so `total = Σ(entries) + legacy_hours` still holds. Reversal SQL is in the script header. **A near-miss worth remembering:** adding the new column to the store's boot query made its error fallback collapse to a column set with **no `legacy` flag**, which would have leaked ~4,000h of backfill into days-worked, tenure and the feed — it now degrades ONE column at a time (`legacy, date_estimated` → `legacy` → bare), verified against the live DB. **Audit assertions rewritten to be durable:** per-client task TOTALS were never an invariant (32 zero-hour Voyantis rows were tidied with bulk-select and turned the audit red), so the mapping is now asserted via **`project_id` → client**, which nothing modifies; Harmon.ie asserts its **TRACKED** 2103h exactly and reports recovered hours separately; the cutover rule applies only to comment-derived rows, while estimated dates must sit **inside their task's evidenced window**. Twice while writing those I forgot to SELECT the columns the new assertions read (`activity_from/to`, `closed_on`) and got false failures — check the select before believing an audit failure.
- 2026-07-28 — v0.99.33 (deployed) — **"Hours by client over time" was flat before 2022 — it picked the top 6 clients by WHOLE-RANGE total.** On "All time" those six were all modern (Studio, Checkmarx, Blazepod, Harmonie, No Traffic, Nominal) and they covered **0% of every year before 2022**, so six lines sat on zero while the studio had really logged 3,068h across 2016–2021. The real leaders then were Quadream, Cognigo (d.day labs), Volta, New Era, Anchor and Collabria. Even in modern years the six covered only 39–82% of the total, so the chart understated **every** period. `StudioClientTrend` now takes the **union of each bucket's top 2**, tops up to 7 by overall total, and adds a grey **"Other clients"** aggregate so the lines account for the whole studio total instead of silently dropping 20–60% of it. **Plus a colour bug I had caused:** `build-rehome-sql.mjs` created all 14 archived clients with the default `#9ca3af`, so Quadream, Cognigo and the new "Other clients" line rendered as three identical grey lines — the 14 are recoloured from a distinct palette (in the DB, and in the generator so a re-run can't repeat it). **NOTE for future "where's my data" questions:** the home page defaults to **This month**, and the pre-Everhour history only appears on **All time** (year buckets) or by stepping back with the left arrow on This year/month/week — "This year" is 2026 only. Verified 2019 that way: 1,203.25h, Jan 223.75h … Dec 48h, Quadream 57.6% / Cognigo 28.8%.
- 2026-07-28 — v0.99.32 (deployed) — **Former staff are now archived members you can open, with their pre-Everhour history.** Migration **`0018_people_without_accounts.sql`** — **applied**. **A profile is now a PERSON, not an account:** it drops `profiles.id references auth.users(id)`. Nitsan wanted the ~24 pre-Everhour authors on Team; the obvious route would have meant minting ~24 dormant logins on .com with invented addresses (a takeover risk if anyone later creates one of those mailboxes) purely to hold attribution. Dropping the FK is both the better model and the safer one — RLS compares `auth.uid() = id`, so a person with no auth row matches nobody and can never sign in. New `profiles.has_account` (false for these) + a CHECK that anyone accountless is also inactive, so they cannot leak into a picker or the weekly plan. `scripts/create-former-staff.mjs` (dry-run default) created 24 profiles — Edor Nisim 725h, Avishay Cohen 463h, dikla 459h, adi 336h, Itai Snapier 328h, Miri Kuntsman 294h … yam sasson 114.5h, Inbal Kim Doron **−83h** — and linked **1,346 time entries + 2,055 comments** to them; `legacy_author_name` is kept as provenance and makes it reversible. Names go in **verbatim** as Asana had them (`dikla`, `adi`, `ruth`) rather than guessing at capitalisation or surnames. **Team + member pages moved to `entrySumsAll`** so the history actually shows (the home page stays legacy-free, so no current member's days-worked or tenure moved). Three things this exposed: (1) `MemberPhoto` fell back to the shared studio cut-out, so an archived person displayed **a photo of an actual colleague** — new `fallback="initials"` prop, wired wherever `has_account === false`; (2) the member page's "Hours per month" was hardcoded to the last 12 CALENDAR months, so every former-staff chart was empty — now windowed on their last 12 months **of activity**, with year labels; (3) "Send password link" is replaced by "Kept for historical attribution — no account" (the API already refused with a 404, but offering the button was misleading). Note "Tasks" on a team card counts OPEN assigned tasks, so it correctly reads 0 for archived people. Verified: Edor Nisim shows EN initials, 725h, 3 clients (Quadream 350.5h / Yoco 305h / Anchor 69.5h), chart Mar 21→Feb 22.
- 2026-07-28 — v0.99.31 (deployed) — **Former staff are NOT members, and the task panel now says who they were.** The ~24 pre-Everhour authors (Edor Nisim 725h, Avishay Cohen 463h, dikla 459h, adi 336h, Miri Kuntsman 294h, yam sasson 114h…) have no `profiles` row by design — see 0017. Their hours live on `time_entries.legacy_author_name`. But `task-panel.tsx` rendered the author as `{user?.name}`, which resolved to **EMPTY on 2,175 of the 2,397 imported comments**, and the time-entry row showed only a blank avatar. Both now fall back to the stored name (comment byline shows it; the entry row carries it as an avatar tooltip). Verified live on Quadream "UI/UX December 19" — the 2019 thread reads "Edor Nisim 7 Dec, 18:10 — 0.75 re-made icons for marialena", "Alex Kozka 19 Dec — 17/12 0.25h (icon animation render)". **Where former staff hours ARE visible:** the Reports page per-designer bars (grouped by name, dashed avatar) and the task panel. **Where they are deliberately NOT:** Team, the home team strip, days-worked/tenure, and a client's "Hours per user" — all keyed on a profile id, which they have none of. Note `Inbal Kim Doron` nets **−83h**: she recorded the `מפתח` deductions, which is independent confirmation of that convention.
- 2026-07-28 — v0.99.30 (deployed) — **"All time" on the admin home now reaches back to 2016.** It previously stopped at Nov 2022 for two compounding reasons: Everhour itself holds nothing before 2022-11-20 (the true start of tracked time), and the 3,995h of recovered 2016–2022 history is `legacy = true`, which the home page filtered out. Studio-wide, non-per-person panes now read `entrySumsAll`: **MyGraphs (admin branch only** — the member branch stays on `entrySums`, since a backfilled 2019 row is not time they logged**)**, `StudioClientTrend`, and the **Studio hours + Billable** tiles. Per-person figures deliberately still exclude it: `StatTiles` keeps a separate `curLive` sum so **Avg / designer** divides live hours by live designers (48,216h total but 1,524.9h avg over 29) and **Tasks worked** counts only current activity; `StudioTeamStrip`, `MemberWelcome`, Team and Time Feed are untouched. **The Reports page already included the history** — it queries `time_entries` directly and never filtered on `legacy` — but its per-designer bars did `.filter(r => r.profile)`, so the 1,346 authorless rows landed in the period total while missing from the breakdown, which silently failed to add up. They are now grouped under `legacy_author_name` with a dashed avatar, and the row avatar carries the author as a tooltip. Verified: chart spans 2016→2026 (79h/81h/741h/1203h/440h/524h then live years), 2022 correctly blends 927h legacy + 5,096h live, Harmonie surfaces in Hours-by-client at 5.8%.
- 2026-07-28 — **INCIDENT + RECOVERY: 620 tasks destroyed and rebuilt. READ THIS BEFORE WRITING ANY BULK SQL.** `data/0018_rehome_unsorted.sql` (2,326 statements, `begin;` … `alter table tasks disable trigger` … 620 `update tasks set client_id` … `delete from clients` … `commit;`) was pasted into the **Supabase SQL editor**. Outcome: the 14 client INSERTs committed, **every `update tasks` did NOT take effect** (no task ended up with `legacy_title` set), and the final `delete from clients` **ran anyway** — so 620 tasks still attached to "Imported / Unsorted" were wiped by `ON DELETE CASCADE`, taking 2,397 comments, 87 sections, 35 `projects` rows and Harmon.ie's **2,103.25h of time entries**. A `do $$ … raise exception if leftover > 0` guard sat immediately before that delete and did not fire. **CONCLUSION: do NOT assume the SQL editor runs a pasted multi-statement script as one atomic transaction, and never put a cascading DELETE in the same file as the UPDATEs it depends on.** Recovery was total, from the committed `data/` dumps: `scripts/restore-unsorted.mjs` (**inserts/upserts only, service role, dry-run by default, `--apply` to write**) rebuilt projects → sections → tasks → time entries → comments straight into the correct clients, restoring Harmon.ie to exactly 2103.25h and all 2,397 comments. **It restored 627 tasks, 7 more than were destroyed** — `everhour-tasks-all.json` holds 7 zero-hour placeholder rows ("1", "2", "123", Hero…) on the Studio podcast/website boards that the original 2026 import had skipped; keeping them was safer than trying to identify exactly which 620 rows had existed, and the audit baseline is now 4513. **The `data/` dumps are what made recovery possible — never remove them from the repo.**
- 2026-07-28 — data — **Pre-Everhour recovery APPLIED: 7,948.65h.** 3,994.90h as 1,533 dated `legacy` time entries (68 of them negative `מפתח` reductions, 1,346 carrying `legacy_author_name` instead of a profile) + 3,953.75h as `tasks.legacy_hours` remainder on 150 tasks, plus 24 sections given their recovered budget/hours/closing date and 352 task names cleaned. Applied by **`scripts/apply-legacy-hours.mjs`** (dry-run default, `--apply` to write) after `reconcile-legacy-hours.mjs`. **Which columns the service role may write was established EMPIRICALLY, not assumed** (throwaway task, probed, deleted): `tasks.legacy_hours` / `activity_from` / `activity_to` / `legacy_title` **allowed**; `tasks.title` and `tasks.estimate_hours` **BLOCKED** by 0011's trigger; `sections.*` entirely allowed (no trigger on sections). So only title+budget go through the SQL editor, as `data/task-titles-cleanup.sql` — 353 UPDATEs, **no DELETE and no DDL**, and instead of disabling the trigger it does `select set_config('request.jwt.claims','{"sub":"<admin uuid>"}',true)` so `auth.uid()` resolves and `is_admin()` passes. **Prefer that set_config trick over `disable trigger` for any future admin-only bulk write.** Verified after applying: `audit-rehome.mjs` PASS, and the admin home page reads **596h / 8y4m14d / 63% / 45 / 74.5h — byte-identical to before the import** despite 3,994h of dated 2020–2022 entries now present, which is the `entrySums` legacy filter doing its job. `reconcile-legacy-hours.mjs` now scopes on the **23 legacy `projects.everhour_id` values**, not the (deleted) client — `project_id` is the only identifier that survived both the re-home and the restore.
- 2026-07-28 — v0.99.29 — **Bulk-select tasks, and the groundwork for dissolving "Imported / Unsorted".** Migration **`0016_legacy_history.sql`** — **must be applied** (the app degrades gracefully without it; the boot query falls back). (1) **`taskPatchToRow` had no `clientId` branch** (`src/lib/db.ts`), so "move a task to another client" updated local state, wrote an **empty patch object** to Postgres and silently reverted on reload — moving a task between clients had never worked. Verified fixed end-to-end on a throwaway task: moved → reloaded → still moved → ⌘Z restored both client and section. (2) **Multi-select in the client view** (admin-only, the first anywhere in the app): row checkboxes + tri-state select-all on each section header and the table header, shift-click ranges, and a sticky action bar (move to client/section, billable, mark done, delete). `updateTasksBulk` / `restoreTasksBulk` / `deleteTasksBulk` in `store.tsx` do one `.in("id", ids)` write and record **one** undo step for the whole selection (the inverse is a list of per-task patches, grouped so undo is one round-trip per distinct prior value). **Move-to-client always sets `sectionId` too** — a section belongs to exactly one client, so keeping the old id strands the tasks. Bulk delete **refuses outright** when any selected task has logged time (cascade is unrecoverable), unlike single delete which only warns. **GOTCHA:** the checkbox had to fit inside the EXISTING 36px gutter (checkbox `left-1`, grip `left-[18px]`, grip width 28→18px, full height kept). Widening `pl-9`→`pl-14` cost the task-name cell 20 of its 89px — the fixed columns already overflow the table's 720px min-width, so any gutter growth comes straight off the name. (3) **`entrySums` now EXCLUDES pre-Everhour backfill by default**; `entrySumsAll` is the opt-in for client-facing and per-task totals (client stats, reports, `taskMinutes`, TaskTable's total column). Chosen this way round deliberately: a personal or time-series surface nobody remembered to audit degrades safely, rather than silently inventing 2021 working days for someone still employed.
- 2026-07-28 — data — **The comment thread decoded; 2,397 Asana comments imported.** Migration **`0017_legacy_entry_authors.sql`** — **must be applied** with 0016 before the data SQL. (1) **`מפתח` lives in the COMMENTS, with an explicit leading minus** — `"-11.5h מפתח https://…"`, `"-2.25h מפתח על שעות אור"`. Reading those as positive was inflating every task that had one. Netting one Volta QA task properly: work 35.25h − reductions 16.5h = 18.75h against a title of **17.5h** — the hand-maintained title was right all along. 68 reduction entries are imported with **negative minutes**, which is deliberate: they are real ledger lines and dropping them while keeping the positives is what made comment totals run 4× over the studio's own figures. (2) **An hours figure must LEAD the comment** (after an optional date prefix: `"23/11 5.75h שיחה עם טדי"`). A mid-sentence number is prose, not a log — the single worst false positive was `"כל עבודה שמושקעת מעל 17.5 שעות מבוטלת…"` ("work beyond 17.5 hours is written off"), a **cap being quoted**, counted as 17.5h of work. A bare number with no unit is also rejected now. Result: title and comments agree within 15% on **70%** of the 229 tasks that have both, and the residual bias flipped to comments being *lower* — the safe direction, since the title wins. (3) **The reconciler owns the entry list.** `build-rehome-sql.mjs` used to re-derive entries from the story cache, so it emitted hours for roll-up parents and for tasks the review had left at zero, and `Σ(entries) ≠ legacy_remainder` on the same task. It now reads `data/legacy-entries.json` verbatim; the reconciler **exits non-zero** if `total ≠ attributable + remainder`. (4) **Author identity without accounts:** `profiles.id` is a FK to `auth.users`, so "one inactive profile per ex-employee" would have meant minting ~27 dormant `@studionmore.com` logins for people who left years ago. Instead `time_entries.user_id` is nullable **for legacy rows only** (CHECK: `user_id is not null or (legacy and legacy_author_name is not null)`) and the raw Asana name rides in `legacy_author_name` — real per-person, per-date history, no accounts. Only 6 of 33 comment authors match the current roster (222 of 2,397 comments); the rest are yam sasson, Edor Nisim, Miri Kuntsman, adi, ruth, dikla… **Store typing does the safety work:** `entrySums` is `UserEntrySum[]` (non-null `userId`) because filtering out `legacy` is exactly what guarantees a real user — so no personal aggregation needs a null guard, and the compiler flags any that would. Final: **7,948.65h recovered = 3,994.90h dated entries (1,533 rows) + 3,953.75h remainder**, cross-checked between the reconciler, the SQL and `audit-rehome.mjs`.
- 2026-07-28 — data — **Three conventions Nitsan supplied after reviewing the CSV, all now encoded.** (1) **ROLL-UP PARENTS — the big one.** A parent task's title figure is the SUM of its subtasks: `"Q&A Movies - 128.25h"` has 7 children (`Storyboards - 34h`, `Design (70)- 50h`, `Animation - 18h`, …) that add up to the same 128.25h, and the import flattened parents and children into sibling tasks — so counting both **doubles every rolled-up figure**. Roughly two thirds of the unsorted set are subtasks. Decision: **children win, parent contributes 0**, with a guard — a parent is only zeroed when at least one of its children is present AND carries hours (`rollup-parent-kept-no-children` flags the rest, where the parent's figure is the only surviving record). Section names follow the same rule one level up: `Movies - 171.75h` ≈ the sum of its **top-level** tasks only, which is why `sections.legacy_hours` is display-only and never added to a client total. Parent shape comes from Asana (`parent.gid`, `num_subtasks`) and is cached to `data/asana/parents.json` by `fetch-asana-stories.mjs`; **without that file the reconciler warns loudly and does NOT zero anything** rather than silently double-count. (2) **`key` / `מפתח` rows are hours REDUCTIONS, not work** — `"מפתח שנעשה - 18.25h"` is a note recording how big a reduction was. Counted in **neither** direction per Nitsan: adding overstates delivery, negating would re-deduct something the studio's sheet had already netted off. Matched by `KEY_ROW` (guarded so "Keyboard" doesn't trip it) and flagged `key-reduction-not-counted`. (3) Budget **ranges** store the **high end** in `estimate_hours` (the ceiling quoted to the client); the low end rides along in the review CSV's `budget_low` and the full range survives in `legacy_title`. Note-like rows ("--- Keys ---", "Template Instructions", "Action Items") are **kept as tasks** — they carry no hours so they cannot distort a total. Client mapping confirmed by Nitsan against Asana: the `Website (232+61/134)…` and `Movies - 171.75h` sections and the `Storyboards - 34h` subtask are all Volta Solar; `Yoco new website - 67h` is Yoco.
- 2026-07-28 — gotcha — **`node --env-file` takes the LAST duplicate of a key.** A stray `ASANA_ACCESS_TOKEN="1/your-token-here"` placeholder line appended after the real token silently shadowed it and every Asana call returned 401 with no hint that the value was wrong. When an API 401s, check for a duplicate key in `.env.local` before assuming the credential is bad.
- 2026-07-28 — data (NOT YET APPLIED) — **"Imported / Unsorted" re-home + pre-Everhour hour recovery.** 620 tasks / 87 sections / 23 legacy projects. **Asana is not needed to place them**: `tasks.project_id → projects.everhour_id` still carries the mapping and the project name IS the client name in 21 of 23 cases. Mapping → 14 NEW archived clients (Volta, Cognigo (d.day labs), Quadream, Anchor, Siteaware, New Era, Yoco, One Zero, In-reach, Empathy, CSL, PDQ, PlayStudios, Mesh) + 4 existing (Collabria 53, Harmonie 33 **incl. 2103h**, Voyantis 25, Studio 29). **Second find: the hours were never imported.** Pre-Everhour the studio logged hours in Asana task COMMENTS and hand-copied the total into the title, budget in parens — `"Logo Update (8) - 5.25h"`. `task_comments` is **empty studio-wide**; 2,782 tasks with an `asana_gid` have zero time entries. Titles alone yield **6,142h across 221 of 594 tasks**; the other 373 need the comments. **Double-counting checked and dismissed**: only 51 tasks studio-wide have both a title hour and tracked time, and those are lump backfills logged at the **2022-12-04 cutover** with identical figures, or ongoing tasks where Everhour kept counting past the frozen snapshot — tracked always wins, so the rule is *never write recovered hours to a task that has any `time_entries`*. Scripts: `lib/legacy-hours.mjs` (parser, 35 unit tests, every case a real string that broke a simpler version) · `fetch-asana-stories.mjs` (**needs `ASANA_ACCESS_TOKEN` in .env.local** — a claude.ai connector can't help, this is a node script; caches per task to `data/asana/stories/` and is resumable because Asana rate-limits hard) · `reconcile-legacy-hours.mjs` (→ `data/legacy-review-*.csv`, **CSV not XLSX**: no xlsx lib is installed and Nitsan lives in Sheets; BOM + CRLF so Excel doesn't mangle the Hebrew) · `build-rehome-sql.mjs` (→ `data/0017_rehome_unsorted.sql`) · `audit-rehome.mjs`. **WHY THE SQL FILE:** 0011's `enforce_task_member_columns` trigger blocks `client_id`/`section_id`/`title` writes for anything without an admin `auth.uid()` — **including the service role AND the SQL editor** — so the generated file disables the trigger inside the transaction and re-enables it. **INVARIANT: `task total = Σ(legacy time_entries) + legacy_hours`**; `legacy_hours` is the *unattributed remainder*, never a duplicate of the entries. Order: apply 0016 → (optional) fetch comments → reconcile → **Nitsan reviews the CSV, which then beats the parser** → build SQL → run it → `audit-rehome.mjs`. Rollback: `delete from time_entries where legacy`; names restore from `legacy_title`/`legacy_name`; moves from `project_id`, which is never modified.
- 2026-07-27 — v0.99.28 — **Collapse/expand all sections from the table header, and empty sections are dimmed.** The header's leading spacer became a chevron button aligned with the section chevrons; it collapses everything when any group is open and expands everything when all are collapsed. Required lifting each `SectionGroup`'s `open` state into `ClientView` as a `collapsed: Set<string>` — **collapsed-by-exception**, so a newly created section appears expanded, with `""` standing for the null "No section" group. `onOpen` replaces the old local `setOpen(true)` when a task is dropped in. Only groups actually on screen count toward "all" (the empty No-section group exists only mid-drag). Empty section headers render at `opacity-50`, keyed off the same `sectionIsEmpty` (measured against ALL tasks) that enables the delete button — so **dimmed always means deletable**. Verified: 31 rows → 0 → 31 with the tooltip flipping, and a throwaway empty section rendered at opacity 0.5 vs 1 for a populated one (test section removed, no leftovers).
- 2026-07-27 — v0.99.27 — **Drag handle given a real hit area; tick moved next to the task name.** The grip was a **14×14** icon at the very left edge, and since only a mousedown *on it* arms the drag, missing it by a few pixels silently cancelled the gesture — which is why dragging still felt intermittent after v0.99.26. The hit area is now the full-height **28×39** left gutter (icon still fades in on hover, hit area always present). Row indent `pl-6`→`pl-9` to clear it, and the leading cell carries `LEAD_TIGHT` (`-mr-1.5`) so the complete-tick sits **6px** from the task name and **8px** clear of the handle instead of stranded between them; the same class is applied to the table-header spacer and the section-header chevron so all three stay aligned.
- 2026-07-27 — v0.99.26 — **Fixed drag ("worked once, then stopped"), added task delete, moved 32px from Tag to Hrs/budget.** (1) **Root cause of the drag failure: the whole ROW was `draggable`.** Any press-and-move started a drag — fighting click-to-open, making text selection in the title cell impossible, and letting a short drag be delivered as a *click*, which opened the task panel whose `fixed inset-0` overlay then blocked every later drag. Now **only a mousedown on the grip may start a drag**: `armedRef` (a **ref**, not state — toggling `draggable` from mousedown races React's batching, so the attribute could still be false when the browser decides the gesture) is set by the grip, and `onDragStart` calls `preventDefault()` when it isn't set. (2) **Second cause: acceptance was gated on `e.dataTransfer.types.includes(<custom MIME>)`**, which isn't reliably populated during `dragover` across browsers — so drop targets could silently never accept. Both the row and the section now decide from `draggedTaskId`, and `text/plain` is set alongside the custom type as a payload fallback. (3) **`deleteTask`** in the store + a trash button on each row (admin, hover) — **`time_entries`/comments/attachments are ON DELETE CASCADE**, so the confirm spells out how many hours will be destroyed, and it is deliberately **NOT in the undo history** (cascaded rows can't be restored, so an "undo" would bring the task back minus its hours — worse than none). Published client reports are unaffected: they ship frozen snapshots. This existed because a section can only be deleted when empty, and there was no way to empty one. (4) Tag column w-36→w-28, Hrs/budget w-28→w-36. Verified end-to-end on a throwaway section+task: delete disabled → task deleted → delete enabled → section removed, both rows confirmed gone from the DB.
- 2026-07-27 — v0.99.25 — **Reorder tasks, rename/delete sections, and hours visible on the client table.** (1) **`BudgetBar` returned null when `estimateHours` was null** — so a task with no budget showed NOTHING in that column and its logged hours were invisible on the client table unless you opened the task. Now shows the hours alone (bar only when there's an estimate); column header renamed **Budget → Hrs/budget**. (2) **`reorderTask(movedId, beforeId)`** in the store: places a task before another within its own section and rewrites that section's positions as a dense 1..n (stable, unlike fractional/midpoint schemes that drift toward collisions). Undo restores the exact prior positions. Drop on a sibling row shows an inset top line (not a border — a border would shift the row 2px); a drag from another section is deliberately left unhandled so it bubbles to the `SectionGroup` and becomes a move-in. **Reorder is disabled while a column sort is active** — positions would change invisibly and the drop would look like it did nothing. `draggedTaskId` is mirrored in a module-level variable because `getData()` is unreadable during `dragover`, and a row must decide mid-drag whether it owns the drop. (3) **Section rename** (inline `EditableTextCell`) + **delete, admin-only and only when empty** — emptiness is computed against ALL tasks, not the filtered list, since "Show completed" would otherwise make a section of done tasks look safe to delete; the store also refuses independently. The header became a div (an editable field and a delete button can't nest in a button). **NOTE — existing data:** every task's `position` was **0** (never set by the imports), so list order was incidental; the first reorder in a section assigns real positions to all of its tasks. Ordering logic unit-tested (9 cases incl. no-op and abort guards); the drag *interaction* for reorder was not exercised in-browser.
- 2026-07-27 — v0.99.24 — **Admins can move a task between sections**, two ways. (1) **Drag** — rows in the client list are `draggable` for admins with a `GripVertical` handle that fades in on hover, absolutely positioned in a widened `pl-6` gutter so appearing doesn't shift the row. The whole `SectionGroup` (header + rows + add-row) is the drop zone, ring-highlighted while hovered, so the target is generous rather than a thin line. Uses a custom MIME `application/x-studio-task-id` so the weekly plan and report table ignore these drags, and so `dragover` can decide whether to accept (`getData()` is only readable on `drop`). Dropping on the section a task is already in is a no-op — no pointless write, no junk undo step. The **"No section" group is normally hidden when empty but renders for admins mid-drag**, otherwise there'd be no way to drag a task OUT of a section; `dragstart`/`dragend` bubble to the table so it knows a drag is running without threading state through every row. (2) **Task panel** — new **Section** row with a dropdown of that client's sections (the Client row is now just the chip); members see it read-only. Both go through `updateTask({sectionId})`, which already mapped to `section_id` and carries undo/redo. **GOTCHA discovered:** migration 0011's `enforce_task_member_columns` trigger **also blocks the SERVICE ROLE** from protected task columns — `is_admin()` reads `auth.uid()`, which a service-key connection doesn't have, so it fails with "members cannot modify protected task fields". Server-side code can insert tasks but cannot update title/section/assignee/etc.; such edits must go through a user session.
- 2026-07-27 — v0.99.23 — **Fifth occasion group: "Studio & fun days"** (`src/lib/studio-days.ts`, 8 fixed-date entries — Women's Day, World Design Day, Hummus Day, Falafel Day, Emoji Day, Photography Day, Coffee Day, Artist's Day). Plain Gregorian month/day, so they ride the same recurring `MM-DD` path as birthdays — no calendar conversion, no re-seeding. Kept deliberately short and spread Mar–Oct (Sep/Dec are already covered by the Jewish holidays); days with competing claims (pizza day is 9 Feb in the US, 17 Jan internationally) were left out rather than guessed. **Sports/awards were considered and rejected as a group**: Oscars/Emmys/NBA Finals dates are announced annually and aren't computable, so a built-in list would need yearly updating and would go silently wrong — the exact failure mode the computed holidays avoid. Add those as *custom* occasions when announced. Carousel fix: the arrows shared the title's row and cost it ~60px of a column that only has ~200px (the portrait takes 216px of a ~477px hero), truncating "Shaked's bir…" — arrows now sit on their own row flanking the dots, and the title wraps to two lines instead of truncating.
- 2026-07-27 — v0.99.22 — **Occasions.** Migration **`0015_occasions.sql`** — needed only for CUSTOM entries; everything else works without it. Four groups feed the home "Coming up" pane: **birthdays** (from `member_hr.birth_date`), **work anniversaries** (from `profiles.start_date`), **Jewish holidays**, and **custom**. Which groups show is stored in `app_settings` under key `occasion_groups` (no schema — it's already key/jsonb), edited from **Settings → Occasions** (admin only) along with add/remove of custom entries (`recurring` repeats yearly on the same month/day). (1) **Jewish holidays are COMPUTED, never seeded** — `src/lib/jewish-holidays.ts` walks the range asking ICU (`Intl.DateTimeFormat` with `ca-hebrew`) for each day's Hebrew date and matches fixed Hebrew-calendar definitions. A seeded table would need re-seeding every year and would go stale the first year nobody remembered. Verified against ICU: 1 Tishri 5787 = 2026-09-12, 15 Nisan 5787 = 2027-04-22, Purim on 14 **Adar II** (5787 is a leap year) = 2027-03-23. The three days that MOVE to avoid Shabbat (Yom HaShoah 27 Nisan, Yom HaZikaron/HaAtzmaut 4–5 Iyar) implement the Knesset rules — validated against a real checkable date: 5 Iyar 5785 fell on a Saturday and the code shifts Yom HaAtzmaut to Thu **2025-05-01**, which is when it was actually observed. (2) `/api/celebrations` merges all four sources. It reads `member_hr` with the service role but returns **only `MM-DD`** — a birthday is studio-social, a birth year is not. Pre-migration a missing `occasions` table degrades to "no custom occasions" instead of breaking every home page; likewise `/api/occasions` GET still returns the group switches (they live in app_settings) rather than failing wholesale. (3) Writes in `/api/occasions` go through the **caller's session, not the service key**, so the DB's admin-only policy is the enforcement.
- 2026-07-27 — v0.99.21 — Member home: the duplicate lower "My hours" pane (`PeriodStat`) removed — its vs-last-period figure became a delta chip on the tile above, freeing the row so both graphs sit side by side; My tasks took the slot beside "Log my hours". `PeriodStat` **deleted** (no remaining caller) along with two now-unused chart imports. Occasions moved **inside the blue hero** (below the heading, above "+ Log time") as an inline variant with arrows + dots + "in N days"; it carries its own right margin because the portrait is scaled by the pane's HEIGHT (~216px wide against a 176px container) and a filled pill slid under it and hid the date. Hero "My week" button removed.
- 2026-07-27 — v0.99.20 — Member hero at half width with four filter-scoped tiles beside it; the portrait's head breaks out of the top (anchored `top:-34/bottom:0`, not a fixed height — `items-stretch` grows the panel to match the tile column so its height isn't known at author time) and the FIGURE's right edge sits 5px inside the pane (shifted by the silhouette's measured 12.8% transparent right margin, since anchoring the image's own edge leaves a gap that moves as the panel grows). **Celebrations bugs fixed:** birthdays never showed for members (member_hr is admin-only under RLS → client-side select returned nothing) — now via `/api/celebrations`; sorting compared the formatted "D/M" string so "10/8" sorted before "9/8"; anniversary year counts were off by one for dates rolled into next year.
- 2026-07-27 — v0.99.19 — Averages to 1 decimal (`formatHoursAvg`); logged time stays at 2 places since an entry of 0.75h must read back as 0.75. Michal Ner-David's name capitalised (profile + her weekly-plan column, which is generated from the first name at creation).
- 2026-07-27 — v0.99.18 — Portrait crown un-pinched: the head layer stopped exactly at the circle's top edge, where the circle is only ~20px wide against a ~32px head, so `border-radius` shaved the crown and it read as a flat cut. The head layer now runs 14% of the diameter past the edge. Team card name to `text-xl` with Billable moved into the stats row as a `PercentRing`. **Member archive**: the active/inactive control was a coloured pill reading "active" with no affordance — now an Archive/Restore button matching the client page, plus a banner on an archived member's page; team filter says "Show archived".
- 2026-07-27 — v0.99.17 — Portraits sit on a **brand-blue** circle with the head breaking the crop. The chequered background was not CSS: `placeholder.png` was a FLATTENED export with the Photoshop transparency checkerboard baked in at alpha 255, while the genuinely transparent master (`photo_transparent.png`) sat unused beside it. `DEFAULT_TEAM_PHOTO` now points at `cutout.png`, a 640px resize of that master (371KB, was 6.7MB). New `bleed` prop draws the image twice at identical geometry — once inside the clipping circle (body), once clipped to the band above it (head) — because one oversized image would also let the shoulders spill past the circle's curve. **`placeholder.png` is now unused** and could be deleted.
- 2026-07-27 — v0.99.16 — **Hours to 2 decimals.** A logged 0.75h rendered as "0.8": the stored minutes were always exact (0.75h = 45min), `formatHoursShort` was at `toFixed(1)`. New `formatHoursDecimal(minutes, digits=2)` trims trailing zeros. **Member emails** on Team + member page via admin-only `/api/admin/member-emails` (profiles has no email column; the address lives in auth.users, so reading it there avoids a denormalised copy that drifts). Team cards: portrait left, details stacked left-aligned. Member page: both pictures moved into the header as `PictureEditBadge`, "Pictures" pane deleted. Client **archive/unarchive on the client page** (`clients.archived` and the Settings control already existed).
- 2026-07-26 — v0.99.14 (deployed, LIVE) — **`/reset` no longer shows members a bare red `{}`.** The form passed `error.message` straight through, and Supabase's own auth mailer fails two ways that are both meaningless to a member: **429** when the built-in mailer's ~2/hour cap is hit, and **500 `unexpected_failure` / "Error sending recovery email"** when custom SMTP is misconfigured — supabase-js surfaces the latter as an `AuthRetryableFetchError` whose `message` is the **literal string `"{}"`**, which rendered as a red `{}`. Both now map to a plain sentence pointing at the working path (admin → "Send password link"), plus a `console.error` with name/status, plus a guard so any other empty-ish message can't reach a member. **DIAGNOSTIC NOTE for next time:** `curl -X POST "$SUPABASE_URL/auth/v1/recover" -H "apikey: $ANON_KEY" -d '{"email":"…"}'` distinguishes the two instantly (429 = capped, 500 = SMTP broken). When this was written the project's SMTP was returning 500 on every auth email right after being configured — Resend itself was healthy (key valid, `studionmore.com` verified, `eu-west-1`), so the fault was the values in Supabase's SMTP form. Resend SMTP wants **username = the literal string `resend`** (not an email — the usual mistake), password = a `re_…` API key with send access, `smtp.resend.com`, port 465/587, sender on the verified domain. **`/api/admin/invite` is unaffected by all of this** — it calls Resend's HTTP API directly and never touches Supabase's mailer, so admin-sent links keep working even while SMTP is broken. Also confirmed by end-to-end test that the v0.99.13 `token_hash` path works: `generateLink` → `GET /auth/confirm?token_hash=…&type=recovery` → **307 → `/reset/update`**. Members holding **pre-v0.99.13 emails must be re-sent a link** — those are PKCE `?code=` links and are permanently broken.
- 2026-07-26 — v0.99.13 (deployed, LIVE) — **Admin-sent password links never worked cross-device; fixed.** The team page's "Send password link" called `supabase.auth.resetPasswordForEmail()` **from the browser**, and `@supabase/ssr`'s `createBrowserClient` defaults to **PKCE** — so the `code_verifier` was stored in the **admin's** browser and the member got a `?code=` link. Opened on the member's own device, `/auth/confirm` had no verifier, `exchangeCodeForSession` failed, and they were redirected to `/reset` — the "very first screen". **The link was never going to work**; the only reason it ever seemed to take "two emails" was the member separately clicking "First time here?" themselves, which put the verifier in *their* browser. (1) New **`POST /api/admin/invite`** (admin-only, service role): `auth.admin.generateLink({type:'recovery'})` mints the token server-side **without sending**, and we mail a **`token_hash`** link via **Resend**. `/auth/confirm` already had the `verifyOtp()` branch for this — nothing had ever used it. Carries no browser state → opens on **any** device. Also **bypasses Supabase's built-in auth mailer (~2 emails/hour project-wide)**, which had made onboarding a team impossible ("mails exceeded"). (2) Route resolves the address from **`auth.users` by profile id** (`getUserById`) — `profiles` has no email column, which is why the button used to `window.prompt` the admin to retype it. (3) `/reset` now surfaces `?error=link` ("that link couldn't be used…") instead of a blank form — `/auth/confirm` had always passed it and nothing read it. **Member-initiated `/reset` still uses the PKCE path** (same browser, so it works) and still draws on the Supabase mailer **until custom SMTP is configured** — Resend SMTP is `smtp.resend.com:465`, username the literal `resend`, password a Resend API key, sender `notifications@studionmore.com` (send-only identity — no mailbox exists, and none is needed; Resend authorizes by verified *domain*). After enabling custom SMTP, **raise Authentication → Rate Limits → email** (locked at 2/hr until then) and the **email-template editor unlocks** (it's gated behind custom SMTP — that's why the template couldn't be found/edited). **Not verified end-to-end**: no real invite was sent (needs an admin prod session) — test on your own address before mailing the team. **Deploy gotcha re-confirmed:** `npx vercel --prod` **silently fails on the shell's default Node 18** (CLI 54 needs Node 20+) — always prepend the Node 24 PATH.
- 2026-07-25 — v0.99.12 (built, NOT yet deployed) — **Timers fully removed + Everhour gaps can no longer be ignored.** Migration **`0014_retire_timers_and_sync_issues.sql`** — **must be applied before deploying**. (1) **Timers gone for good.** A prior pass removed the Track buttons but left the feature wired: `timer-widget.tsx` was still mounted in the header and a "▶ Start timer" button still sat in the task panel above the manual add form. Deleted the component, the button, and `startTimer`/`stopTimer`/`completeTimerEntry` + `runningTimer` from `store.tsx`; the boot query that hydrated a running timer is gone. Migration deletes never-stopped rows (`minutes is null` — they hold no hours, only a start time, and nothing could complete them any more), drops the `timer_or_minutes` check, and makes `time_entries.minutes` NOT NULL. `started_at` is **kept** and commented as legacy provenance (completed timer entries still carry it); drop it later if you want. Members log time manually with a mandatory description — that is now the only path.
- **(2) `sync_issues` — the Everhour gap queue.** Unmapped entries used to be counted in a `console.warn` and forgotten (one real run: 43 imported, **21 skipped**), so a published client report could quietly understate billable hours. Now **one queue row per unimportable Everhour entry** (keyed on `everhour_id`, so a nightly re-sync over an overlapping window never double-counts and an old gap never falls out of the 14-day window). `everhour-sync.ts` gained `reconcileSyncIssues`: records new gaps, bumps `last_seen_at` on known ones, **reopens** anything marked imported that reappears, leaves `ignored` alone, and **auto-closes** issues whose entry has since landed. It also fetches `/team/users` (best-effort) so the queue says the person's name, not `user 1453915`. (3) **Admin surfaces**: new `/sync-issues` page (group by Everhour task or person; **Link to a task** via TaskAutocomplete, **Create the task** pre-mapped with client/section — inherits the client's billable flag like `store.addTask` — or **Ignore** on the record; each resolution re-syncs from the oldest affected date, capped at 90 days, so hours land immediately instead of waiting for the cron). New `notifications-bell.tsx` replaces the intake-only bell: one "Waiting on you" popover for intake + sync issues, badge = total. Sidebar gets a badged "Sync issues" item. **Client Reports shows a red banner when any gap is open** — publishing freezes the numbers, so that's the moment it matters. (4) A failed queue load **never** falls through to the reassuring "nothing outstanding" empty state — an all-clear we can't stand behind is the exact failure mode this replaces. Store carries a lightweight `openSyncIssues` count (RLS-scoped; returns 0 pre-migration so the app is safe to deploy in either order, though the page 500s until 0014 is applied). Verified locally: tsc clean, lint 0 errors (58→56 warnings), no console errors, panel/bell/nav checked in the browser.
- 2026-07-25 — v0.99.11 (deployed, LIVE) — **Removed the member salary field entirely.** The studio doesn't want salary held in this system (the finance-product P&L keeps studio-level salary *expense* totals — that's intentional and out of scope here). (1) Migration **`0013_drop_member_salary.sql`** drops `member_hr.salary` + its data — **applied by owner in Supabase**. (2) Code: removed the "Salary (₪)" input + its number-coercion/`type` special-cases from the admin team profile (`team/[profileId]/page.tsx`); updated `/api/me/hr` + `hr-details-form` + `0012` comments. (3) **Seed scrubbed**: `0008_member_hr.sql` had the salary column def + the historical salary values in its `values(...)` seed — removed via a PII-safe script (dropped the `salary` CREATE-TABLE column + the salary field from all 8 seed rows and the insert/select/alias lists; verified 16→15 cols consistent across insert/select/alias/every row without surfacing any PII). **No `salary` reference remains anywhere in `src/` or `supabase/` except `finance_salaries`.** **RESIDUAL:** the *git history* still contains the pre-scrub commits of `0008` with the old salary values — true erasure needs a `git filter-repo`/BFG history rewrite + force-push (destructive to the shared remote; owner's call, not done).
- 2026-07-25 — v0.99.9 — **Automatic Everhour sync** (the team is still logging in Everhour, so the tracker pulls from it on a schedule). `src/lib/everhour-sync.ts` is a TS port of `scripts/sync-everhour.mjs` (identical behaviour: insert-only, idempotent by `everhour_id`, skips unmapped task/user and now *reports* them). `src/app/api/everhour-sync/route.ts` — GET = cron (CRON_SECRET bearer), POST = admin button; `?days=N` sets the lookback (default 3, max 90). **`vercel.json` cron added**: `0 20 * * *` (23:00 IDT **daily**, `?days=14` so it also catches back-dated edits and weekend logging). ⚠️ **The Vercel account is HOBBY: daily-only crons, max 2 total** — an every-2h schedule (`0 5-15/2 * * 0-4`) was rejected at deploy with "Hobby accounts are limited to daily cron jobs". The 2 slots are now used (plan-sync + everhour-sync). For sub-daily syncing you'd need Vercel Pro, GitHub Actions, or Supabase pg_cron hitting the endpoint; user decided nightly + the manual button is enough (2026-07-25). Admin **"Sync Everhour"** button on the Time Feed header (`everhour-sync-button.tsx`, 14-day lookback, reloads on new rows). **`EVERHOUR_API_KEY` is now SET in Vercel production** (it used to be import-only/local — the cron needs it at runtime). Retire all of this when the team actually stops using Everhour: drop the two crons from vercel.json, the button, and the env var.
- 2026-07-25 — v0.99.8 — **New-member onboarding, two member pictures, chart rebuild.** Migration **0012 applied** (`profiles.photo_url`, `member_hr.confirmed_at`). (1) **Two distinct pictures per member**: `avatar_url` (small round) and `photo_url` (studio cut-out portrait used on the home hero + team cards). `src/app/api/member-image/route.ts` uploads/clears either kind for yourself or — as an admin — for any member (`kind`, `targetId`); `picture-editor.tsx` (`PictureEditor`/`MemberPictures`) is rendered on Settings (self) and `team/[profileId]` (admin). `MemberPhoto` now takes the member's `photoUrl`, falling back to the shared `public/brand/team/placeholder.png`. (2) **HR self-service**: `src/app/api/me/hr/route.ts` lets a member read/write **their own** HR row behind a session check with a server-side column whitelist — **member_hr stays admin-only in RLS and SALARY is never returned to or writable by a member**. `hr-details-form.tsx` + new `/welcome` page (confirm details + set pictures on first sign-in), nudged by `ConfirmDetailsBanner` on the member home until `confirmed_at` is set; always editable from Settings → "My details". (3) **Add-user** modal takes a **start date** (stored via the admin users API) and routes to the new member's page on success. (4) **`MultiLineChart` rebuilt** to the new reference: area gradient fills, dashed crosshair, dots on the hovered bucket, white multi-series tooltip card (date + per-client colour bar/hours/% vs previous bucket), headline total + trend chip, line-marker legend. Admin home grid: client-trend now one column with the compact My-tasks pane beside it.
- 2026-07-25 — data — **Shaked Gozlan** onboarded: profile mapped to Everhour id `1453915`, start date `2026-07-19`, 11 entries / 38.5h imported. **Everhour fully reconciled for July**: created the 7 tasks that only existed in Everhour (DualBird "Homepage" 9.8h; Studio&more Website "Studio page" 4h / "Contact page" 1.5h / "Home page" 1.5h; JustPlay "Careers page" 1h; Blazepod "Publish" 1h; No Traffic "Homepage stockholders section" 1h) → last 10 entries (19.8h: 12.8h billable + 7h internal) imported; sync now reports **0 unknown task/user**. NOTE: despite the ~Jul 2 "Everhour retired" note, designers kept logging there through late July — worth re-running `scripts/sync-everhour.mjs` before month-end client reports until that really stops.
- 2026-07-25 — v0.99.7 (deployed) — **Public client report** (`report/[token]/public-report-view.tsx`) restyled to the refresh: serif client name, soft-shadow cards, + a summary-tile row (Delivered to date / current period / period cap / remaining, derived from the snapshot's visible billing periods; cap/remaining only render when the period has an `hourCap`). Verified against the live Baseline snapshot (230.5h delivered). Also ran a manual **Everhour sync** (`scripts/sync-everhour.mjs 2026-07-14 2026-07-25`) at the user's request — Everhour still had recent activity: **43 new time-entries inserted**, 21 already-imported skipped, **21 skipped as unknown task/user** (their Everhour task/person isn't `everhour_id`-mapped in the tracker, so those hours were NOT imported — revisit the mapping if that data is wanted). Note: last cutover guidance said Everhour was retired ~Jul 2, but designers evidently kept logging some hours there through mid/late July.
- 2026-07-25 — v0.99.6 (design refresh — **DEPLOYED, LIVE**; merged to `main`, pushed, `vercel --prod`) — Studio-wide UI restyle to the approved mockups (see the design-pilot artifact). **globals.css**: soft elevation tokens `--shadow-card`/`-hover`/`-hero` + `.shadow-card`/`.card` utils; cards moved to `rounded-2xl` + `shadow-card` app-wide. **New `MemberPhoto` component** — cut-out portraits (transparent PNG, white tee + blue &), shared `public/brand/team/placeholder.png` used for every member until per-person portraits are generated (override via `src`; falls back to initials). **Admin home** (`dashboard.tsx`): 4 KPI stat-tiles (Studio hours / Billable / Tasks worked / Avg-per-designer, delta chips, first tile brand-filled) → 2-col chart grid → new **"The studio" team strip** (per-designer hours + billable bar, links `/team/[id]`) → demoted compact My-tasks + Celebrations. Admin `PeriodStat` path no longer rendered (kept for members). **Member home**: blue welcome **hero** (cut-out photo + this-week summary + "+ Log time"/"My week" CTAs) + Hours/Billable/Days tiles + **5-column My week** (was a vertical list) + compact tasks + DayLog(`#log`)/PeriodStat/MyGraphs/Celebrations. **Team page**: photo-card grid (MemberPhoto avatars, Hours/Tasks + billable bar, active-status dot) replacing the table; serif title. **Shell** (`app-shell.tsx`): account chip + notifications **bell** (intake badge) moved to the top-right header. **My Tasks / Time Feed / Client Reports**: card chrome + serif page titles. Lint clean (0 errors). Verified live via preview + `?viewAs` for the member view. **TODO:** generate individual cut-out portraits per member (drop at `public/brand/team/<name>.png` + wire per-member `src` on `MemberPhoto`); optional restyle of the public `report/[token]` client view.
- 2026-07-24 — v0.99.5 (built, NOT yet deployed) — **Admin home redesign** (`dashboard.tsx`, admin-only; member layout untouched). Admins now get a 2-column analytics grid across the screen — `PeriodStat` ("Studio this month") + the 3 graphs (Hours over time, Hours by client donut, Hours by client over time) — then **My tasks full-width last** with **Celebrations in a ~1/3 pane to its right** (`empty:hidden` so it collapses when there are none). **My week dropped for admins** (not relevant; still on /plan). `PeriodStat` (admin) redesigned: title now a normal pane heading ("Studio · {label}", not the big serif); **hours stays big**; **billable shown only as a bigger ring** (`PercentRing`, centre figure in `.font-serif-accent`) — the separate big % number was removed; **two hour bars** — this period (solid, no numeric since the big figure is above) vs last period (dimmed `bg-brand/30`, with its value). Member `PeriodStat` unchanged. **Partial-period comparison**: the "vs last period" delta now compares like-for-like — `periodBounds`/`comparablePrevRange` truncate the previous range to the same elapsed portion (this month to the 15th vs last month to the 15th; month-end capped; complete past periods compared in full). Verified by a standalone date-math test (5 scenarios pass). **Period navigation**: ◀ ▶ arrows + period label next to the range filters (`periodOffset` state; forward disabled at current; "Now" resets; hidden for All time). Picking a range resets the offset. **Visual check still pending:** needs an admin login to eyeball (built/typechecked/lint-clean + logic-tested only).
- 2026-07-24 — v0.99.4 (built, NOT yet deployed) — **Data-integrity Phase 2** (see `FIX-PLAN.md`; no migration). (1) **Safe writes**: `store.tsx` mutations no longer fail silently — new `noteWriteError()` routes every write error (36 handlers auto-converted; the 2 `movePlanColumn` swallowers fixed) to a user-visible banner in `app-shell.tsx` ("Some changes couldn't be saved. Reload…") with Reload/dismiss. Store gained `writeError` + `dismissWriteError`. (2) **Lint green**: was 61 errors → **0 errors** (56 warnings). All 43 `no-explicit-any` fixed — `db.ts` mappers use a documented file-level boundary disable (return types are explicit) + exported `DbRow`; `store.tsx`/intake/report typed against `DbRow`/explicit shapes. The React-Compiler advisory rules (`set-state-in-effect`, `purity`, `preserve-manual-memoization`, `exhaustive-deps`, etc.) are set to **warn** in `eslint.config.mjs` (intentional client-only patterns; revisit in the perf/store-split pass) so `npm run lint` (= `eslint`) is a green error-level deploy gate. **Deferred:** 2.3 position-race helper, 2.4 dead schema-shim removal (touches boot path — left for a verified pass), 2.5 Vitest tests. **Manual check:** the write-error banner wasn't runtime-exercised (needs a logged-in session + an induced write failure).
- 2026-07-24 — v0.99.3 (deployed, LIVE) — **Security Phase 1** (see `FIX-PLAN.md`). (1) **Report leak fixed**: `report/[token]/page.tsx` now sanitizes the snapshot server-side (`sanitizeSnapshot` — physically drops hidden tasks + hidden period/week columns w/ reindex, nulls hidden estimate/total); `public-report-view.tsx` lost the client-side "Show hidden rows" reveal toggle + localStorage hidden-state (data is simply gone). Verified: live report renders, synthetic-hidden test shows no hidden data in payload. (2) **Task-write RLS**: migration `0011` trigger — members can't change protected task columns via the API anymore. (3) **Attachment API** (`api/task-attachments`): DELETE now requires uploader-or-admin (was sign-in only → IDOR); POST validates the task exists; raw Supabase errors genericized. (4) **Next.js 16.2.10→16.2.11** (core SSRF/DoS/endpoint-disclosure CVEs). (5) **Intake/avatar hardening**: `src/lib/uploads.ts` allowlist forces safe server-chosen Content-Type + rejects HTML/SVG/JS; intake adds a DB rate limit (8/link/10min). **TODO before/at deploy:** apply migration 0011 in Supabase (before deploy); residual `npm audit` highs are transitive (Next's bundled `sharp` libvips CVEs + `postcss`) — no clean non-breaking fix yet, tracked. Not yet done: private buckets + signed URLs (needs intake-queue render change), `git rm --cached data/`, `middleware.ts` reconciliation.
- 2026-07-21 — v0.99.2 (deployed) — Admin home overview: MyGraphs "Hours over time" + "Hours by client" now studio-wide for admins (scoped=all users when isAdmin, still honoring client/period filter; members unchanged = own hours). DayLog ("Log my hours") hidden for admins, replaced in left column by new `StudioClientTrend` — a multi-series `MultiLineChart` (charts.tsx) of hours per top-6 client over period-adaptive buckets, one line per client colored by client.color, hover point → tooltip (client · hours), dims other lines, color legend below. Shared `bucketize()` helper in dashboard.tsx. Verified: admin donut studio-wide (Studio 42%…), 6 trend lines, no Log-my-hours; member view unchanged.
- 2026-07-21 — v0.99.2 (deployed) — Feed: empty day cells now clickable to add hours (removed the minutes<=0 early-return) + prominent brand "Add new hours" button left of the view switcher (opens UserDayDetails for current user/today); UserDayDetails now opens with the add form expanded (adding=true default). Home: DayLog ("Log my hours") moved to the LEFT column under the My-tasks card. TaskTable client cell now links to /clients/[id] (dropped link={false}; ClientChip stopPropagation keeps row-open working). Client page (client-view.tsx): member (non-admin) restrictions — task name/due/budget read-only, mark-complete is a static indicator, billable "$" column (header+cell) and Billable-share stat pane hidden for members. "Hours per month" chart changed from bars (MiniColumnsLabeled) to new `LineChart` in charts.tsx (SVG polyline + circles; value label above each point, hover-only when >6 points).
- 2026-07-21 — v0.99.1 (deployed, LIVE) — Weekly-plan Google-Sheet bridge (one-way sheet→tracker until team edits plan in-app). `src/lib/weekly-plan-sync.ts` (TS port of scripts/import-weekly-plan.mjs; nearest-year date resolution + rolling window −30..+240 days so it never rewrites distant history / is year-unambiguous; clears only free_text+absence for the days the sheet covers + the waiting list, never task-linked entries). API `src/app/api/plan-sync` — GET=cron (CRON_SECRET bearer Vercel injects), POST=admin button. `vercel.json` cron `0 4 * * 0-4` (07:00 IDT Sun–Thu) — verified registered + endpoint returns ok. Admin "Sync from sheet" button on Weekly Plan header (reloads on success). Env vars `WEEKLY_PLAN_CSV_URL` + `CRON_SECRET` set in .env.local AND Vercel production. Verified live: 243 entries / 47 days synced, idempotent (clear=insert). Column aliases hardcoded in lib: Dmitry/Nadav/Adaya/Aki/Leeyam/Sefi/**Shaked**/**Daniel**(sheet "Daniel K")/Freelancers + Studio/Waiting list; Sofia+Liza intentionally skipped (deactivated). GOTCHA fixed: DB `absence_type` enum = vacation|sick|day_off only — sheet "מהבית/wfh" + "חצי יום/half day" are kept as free_text notes (NOT absences), else insert throws and (since delete runs first) wipes the covered days. To retire the bridge later: remove the cron from vercel.json (and optionally the button).
- 2026-07-21 — v0.99 (deployed) — Layout/table pass: Home container full-width (no max-w), right column 400→350px; TaskTable (home + my-tasks) columns retightened — Due moved right after Task, Budget+Hrs-done merged into one "Hrs/budget" cell (display-only; budget still edited in task panel), %Billable column removed, "Hrs by me"→"by me", widths narrowed to cut horizontal scroll; Time Feed page max-w 1500→1000px. Then: "% done" column removed and folded into Hrs/budget as "10/20 (50%)" — when no budget, shows hours only ("10"). PeriodStat restyled — 2-row title left ("My hours"/"Studio" big serif over greyed range) like the header stats, metrics right at text-4xl w/ full "N% vs last period" delta + "Last period" tooltip. Admin title shortened "Studio hours"→"Studio" so full-size numbers fit 350px (verified no overflow). Home chart panes renamed → "Hours over time" + "Hours by client" (range dropped from their titles).
- 2026-07-21 — v0.98 (deployed) — Home "My hours" split into TWO panes (toggle removed): "My hours — {range}" (columns) + "By client — {range}" (donut). New interactive donut in charts.tsx PieChart: legend w/ percentages on LEFT (hours hidden), donut on RIGHT, on-slice % labels for slices ≥9%, hover a slice/legend row → slice grows (RO 17.5→19.2) + tooltip shows client name + hours. Hours only surfaced on hover, per request.
- 2026-07-21 — v0.98 (deployed) — Time Feed timesheet: member's own row renders always-expanded with no fold chevron / non-clickable (isOpen = isAdmin ? expandedUsers.has : true); member subtitle drops "click your row for its tasks". Admin fold behavior unchanged.
- 2026-07-20 — v0.98 (deployed 2026-07-21) — Member permissions: task title/budget/due/billable/complete are admin-only (TaskTable + task panel; tags + figma stay member-editable); figma link gets hover pencil-edit + X-delete in both table and panel; Home right column reordered (My week first); Settings hides Clients/Tags for members + new Change-password pane (supabase.auth.updateUser) for everyone; Time Feed scoped to own hours for members (effectiveMemberFilter; member filter UI admin-only). UI-level only — no RLS change, a member could still write via API directly.
- 2026-07-20 — v0.98 (deployed 2026-07-21) — Figma round-trip design pass: `.font-serif-accent` (PP Fragment Glare) for names/big stats; Home header consolidated (greeting + tenure/tasks display stats + filters); PeriodStat serif figures w/ billable as 2nd big stat; Home chart solid brand bars w/ on-bar labels (Reports split untouched); "&more" text logo; filled borderless search; electric bg #f0f1fa. Plus admin-only **`?viewAs=<name|id>` member preview** (store.tsx swaps exposed currentUserId; banner w/ Exit in app-shell; `?viewAs=off` clears; persisted in localStorage; writes still attribute to the real admin).
- 2026-07-17 — v0.97 (deployed) — Home rework (period stat pane + My-hours tabs, tenure header); timesheet default with expandable per-task rows; native tooltips; undo/redo; report period dividers; Client Reports moved to `/client-reports`; migration 0010 applied; start dates imported from HR sheet.
